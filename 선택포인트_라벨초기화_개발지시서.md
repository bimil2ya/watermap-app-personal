# watermap-app-personal — "선택된 포인트"가 백그라운드 갱신에 초기화되는 버그 개발지시서 (v2)

**v2 변경 사항**: v1을 Codex에게 교차 검토받은 결과 차단 사유 2건이 나와 반영했다.
① 호출부 개수가 6곳이 아니라 7곳이었다(`markPlotDoneInDropdown()`의 호출을
"현재 상태"·"핵심 설계" 절의 목록에서 빠뜨림 — 배경 설명에는 있었는데 목록에서만
누락). ② 팀 전환 시 우연히 같은 포인트번호 문자열이 새 팀 목록에도 있으면, 이전
팀에서 입력 중이던 폼 내용이 `onPlotSelect()`(이 함수가 원래 하는 `_doReset()`·
유역명 재적용 없이) 없이 새 팀 소속으로 그대로 복원돼 **데이터 귀속 오류**로
이어질 수 있다는 지적 — "알려는 한계"로 남기지 말고 팀 일치 가드를 추가해 실제로
막기로 했다. 두 가지 모두 4절에 반영했다.

## 1. 배경과 목표

전체 앱 안정성 점검(2026-08-13) 중 발견: `applyLoadedPlots(plots, teamCode)`
(`watermap_V100.html:5517`)가 호출될 때마다 **현재 선택된 조사지번호를 무조건
지운다** — 이 함수는 사용자가 직접 "포인트 불러오기"를 누를 때뿐 아니라, 사용자
행동과 무관한 백그라운드 이벤트에서도 호출된다:

- `syncCompletedPlots()`(`2999`) — `online` 이벤트(네트워크 재연결)에서 자동 실행
- `markPlotDoneInDropdown()`(`5800`) — **다른** 표준지의 업로드가 성공적으로
  끝날 때마다 실행(`autoRetryUpload`/`reuploadRecord` 내부에서 호출)

**실제로 지워지는 것 — 화면 표시만이 아니다**:
```js
// applyLoadedPlots() 내부, watermap_V100.html:5602-5604
sel.style.display = 'none';
inp.style.display = 'none';
inp.value = '';                    // ← soil-plot-no 인풋의 실제 값을 무조건 비움
```
`soil-plot-no`(`id`)는 이 앱 전체에서 "지금 작업 중인 조사지번호"의 **유일한
소스**로 쓰인다 — 사진 삭제(`deletePhoto`, `5136`), 사진 이동(`4946`),
사진 파일명 생성(`4696`/`4702`/`5008`, 값이 없으면 `'NOPLOT'`으로 폴백),
`getSoilData()` 등 최소 10곳 이상이 `document.getElementById('soil-plot-no').value`를
직접 읽는다.

즉 사용자가 표준지 B를 입력하는 도중, 표준지 A의 업로드가 백그라운드에서 끝나거나
와이파이가 재연결되기만 해도 `soil-plot-no`가 조용히 `''`로 초기화된다. 그
직후 사용자가 사진을 촬영하면 `plotNo`가 빈 문자열 또는 `'NOPLOT'`으로 계산되어
**사진이 엉뚱한 키로 저장되거나 고립될 위험**이 있다 — 데이터가 눈에 보이지 않게
잘못 붙는, 가장 우려되는 유형의 버그다.

같은 함수가 호출하는 `buildCustomPlotList()`(`5618`) 끝부분도 마찬가지로 화면의
"선택된 포인트" 라벨을 무조건 placeholder로 되돌린다(`5726-5728`) — 이건
`inp.value`와 별개로 사용자에게 보이는 표시라서, `inp.value` 문제를 고치더라도
화면이 안 맞으면 사용자가 "선택이 사라졌다"고 오인해 다시 고르거나 저장을 반복할
수 있다. 두 문제를 함께 고친다.

**(v2 추가) 세 번째 문제 — 단순 복원만으로는 팀 전환 시 안전하지 않다**: 값을
그냥 복원하기만 하면, 사용자가 팀 A에서 포인트 "A-01"을 선택해 입력 중인 상태로
설정탭에서 팀 B로 전환해 "포인트 불러오기"를 눌렀을 때, 팀 B의 포인트 목록에
우연히 같은 문자열("A-01")이 있으면 그 값이 그대로 "복원"되어 **팀 B 소속으로
착각된 채 팀 A에서 입력하던 내용이 저장될 위험**이 있다. 이 경로는
`onPlotSelect()`를 거치지 않으므로 `_doReset()`도, 유역명/유역코드 재적용도
일어나지 않는다 — 단순 표시 버그가 아니라 데이터 귀속 오류로 이어질 수 있어
"알려진 한계"로 남기지 않고 팀 일치 조건으로 실제 차단한다(4.1 참고).

**목표**: 백그라운드 갱신이어도, 그리고 팀이 실제로 바뀌지 않은 한, 사용자가
현재 선택 중인 조사지번호(값 + 화면 라벨)를 잃지 않게 한다. 업로드 로직, 서버
통신, 다른 화면은 전혀 건드리지 않는다.

## 2. 범위

**포함**:
- `applyLoadedPlots()`가 `inp.value`를 비우기 **전에** 현재 값과, 그 값이
  속했던 팀(`loaded_team`)을 함께 기억해둔다.
- 목록 재구성 후, **새로 적용되는 `teamCode`가 기억해둔 팀과 같을 때만** 그
  값을 새 목록에서 찾아 복원한다(다르면 무조건 placeholder — 팀이 바뀐 것이
  확실하므로 복원 자체를 시도하지 않는다).
- `buildCustomPlotList()`가 그 복원된 값을 받아 라벨(텍스트·색상·굵기)도 함께
  복원한다 — 완료 여부(`✓` 표시)도 최신 상태로 다시 계산해서 보여준다.
- 네이티브 `<select id="soil-plot-no-select">`의 값도 함께 복원한다 —
  `onPlotSelect()`가 이미 이 둘을 항상 동기화해두는 기존 관례를 그대로 따름.

**명시적 제외**:
- `syncCompletedPlots()`/`markPlotDoneInDropdown()`/`loadPlotPoints()`
  호출부 자체는 건드리지 않는다 — 문제는 그들이 호출하는 두 함수
  (`applyLoadedPlots`/`buildCustomPlotList`) 내부에만 있고, 이 두 함수만
  고치면 7개 호출부 전부에(팀 일치 가드까지 포함해서) 자동으로 적용된다(아래
  4.3 참고 — 호출부별 특수 처리 없음).
- `fillTeamDropdowns()`는 이미 같은 문제(재구성 시 현재 값 유지)를 `currentVal`
  파라미터로 올바르게 처리하고 있다 — 이번 수정은 그 기존 패턴을 포인트
  드롭다운에도 동일하게 적용하는 것뿐, 새 기법을 도입하지 않는다.
- `_doReset()`(저장 완료 후 폼 초기화) 경로는 그대로 둔다 — 저장 후에는
  실제로 선택을 지우는 게 맞는 동작이라 이번 버그와 무관하다.

## 3. 현재 상태 (전제 조건)

- 현재 커밋: `2efff82`(원격 `origin/main` 기준).
- `applyLoadedPlots(plots, teamCode)`는 호출부 **7곳** 전부에서 인자 2개만
  받는다 — `2399`, `2999`, `5473`, `5487`, `5502`, `5800`(`markPlotDoneInDropdown()`
  내부), `6216` — 시그니처를 바꾸면 7곳을 전부 확인해야 하므로, **기존
  파라미터는 유지하고 함수 내부에서만 처리**한다. (v1에서는 5800을 배경
  설명에서만 언급하고 이 목록에서 빠뜨렸다 — Codex 지적으로 정정.)
- `buildCustomPlotList(plots, teamCode, todoCount, total)`는 호출부가
  `applyLoadedPlots()` 내부 단 한 곳(`5615`)뿐이다 — 새 파라미터를 추가해도
  다른 호출부를 놓칠 위험이 없다.
- `selectCustomPlot(val)`(`5738`)이 정상 선택 시 라벨을 그리는 방식(완료 여부에
  따라 `✓ {val}`/`{val}`, 색상 `#2E7D32`/`#E07B00`, `fontWeight:'700'`)을
  그대로 재사용한다 — 새 스타일 규칙을 만들지 않는다.
- 참고로 원래 코드(`5726-5728`)는 placeholder로 되돌릴 때 `fontWeight`를
  리셋하지 않는 자잘한 기존 버그도 갖고 있다(굵게 표시된 채로 placeholder
  텍스트만 바뀜) — 이번 수정에서 함께 정리한다(아래 4.2의 else 분기 참고).
- **(v2)** `localStorage.getItem('loaded_team')`은 `applyLoadedPlots()`가
  `inp.value`를 캡처하는 시점에는 아직 **이전 팀** 값을 갖고 있다 — 같은 함수
  안에서 `localStorage.setItem('loaded_team', teamCode)`로 새 팀 값으로
  덮어쓰는 줄(`5605`)이 캡처 이후에 실행되기 때문이다. 이 순서 덕분에 "캡처
  시점의 이전 팀 vs 이번에 적용될 새 teamCode" 비교가 별도 변수 없이도
  자연스럽게 가능하다(4.1 참고).

## 4. 핵심 설계

### 4.1 `applyLoadedPlots()` — 값 캡처 + 팀 일치 가드

```js
function applyLoadedPlots(plots, teamCode) {
  plots = normalizeLoadedPlots(plots).items;
  teamCode = (typeof teamCode === 'string' ? teamCode : '').trim();
  const sel = document.getElementById('soil-plot-no-select');
  const inp = document.getElementById('soil-plot-no');
  const currentPlotNo = inp?.value || '';                       // 비우기 전에 기억
  const previousTeam  = (localStorage.getItem('loaded_team') || '').trim(); // (v2) 아직 새 값으로 덮이기 전 — "그 선택이 속했던 팀"
  ...(기존 로직 그대로: isDoneFn, total/doneCount/todoCount, groupByWeek 등)...

  sel.innerHTML = optHtml;
  sel.style.display = 'none';
  inp.style.display = 'none';
  inp.value = '';
  localStorage.setItem('loaded_team', teamCode);                // 이 줄 이후로 loaded_team은 새 팀

  const plotMap = {};
  plots.forEach(p => { if (typeof p === 'object') plotMap[p.plotNo] = p; });
  window._plotMap = plotMap;

  // (v2) 팀이 실제로 바뀌지 않았을 때만 복원을 시도한다 — 다르면 무조건 빈 문자열
  // (buildCustomPlotList는 빈 문자열을 받으면 항상 기존과 동일하게 placeholder로 떨어짐).
  const preservePlotNo = (previousTeam === teamCode) ? currentPlotNo : '';
  buildCustomPlotList(plots, teamCode, todoCount, total, preservePlotNo);
}
```
새 전역 상태 변수는 만들지 않는다 — `soil-plot-no` 인풋과 `loaded_team`
localStorage 키 둘 다 이미 존재하는 상태를 그 시점에 잠깐 지역 변수로 담아뒀다가
비교·재적용하는 것뿐이다.

**팀 일치 가드가 7개 호출부 전부에 자동으로 올바르게 적용되는 이유**:
`2999`(`syncCompletedPlots`)·`5800`(`markPlotDoneInDropdown`)·`6216`(`loadSettings`)·
`5502`(오프라인 캐시 폴백)는 **일반적인 동일 팀 갱신 경로에서는**
`previousTeam === teamCode`가 성립해 가드를 통과한다(각각 `loaded_team`을 그대로
읽어 넘기거나, 이미 같음을 확인한 뒤 호출하기 때문 — 2차 Codex 검토에서 호출부
6곳을 하나씩 대조해 확인함, 7절 참고). 팀이 실제로 바뀌는 경로는 `loadPlotPoints()`의
성공/빈 목록 분기(`5473`, `5487`)다. **(v2, 2차 검토 반영)** "대부분 항상
통과한다"는 표현은 비동기 경합까지 고려하면 엄밀하지 않다 — 예를 들어
`syncCompletedPlots()`가 `await fetch(...)`로 대기하는 사이에 사용자가 실제로
다른 팀으로 전환하면, 재개된 호출에서는 `previousTeam !== teamCode`가 되어
가드가 (의도대로) 복원을 막는다. 즉 이 경로들에서 가드가 "항상 통과"하는 게
아니라, **팀이 정말 바뀐 경우에는 이 경로에서도 안전하게 차단된다**는 것이
정확한 설명이다 — 어느 쪽이든 위험한 방향으로 작동하지 않는다.

### 4.2 `buildCustomPlotList()` — 유효하면 복원, 아니면 기존과 동일

```js
function buildCustomPlotList(plots, teamCode, todoCount, total, preservePlotNo) {
  const dropdown = document.getElementById('custom-plot-dropdown');
  const list     = document.getElementById('custom-plot-list');
  const display  = document.getElementById('custom-plot-display');
  if (!dropdown || !list) return;
  ...(기존 로직 그대로: isDoneFn, todoPlots/donePlots, groupByWeek, DOM 목록 생성)...

  // 기존 마지막 3줄을 아래로 교체
  const restored = preservePlotNo && plots.find(p =>
    (typeof p === 'string' ? p : p.plotNo) === preservePlotNo
  );
  if (restored) {
    const inp = document.getElementById('soil-plot-no');
    const sel = document.getElementById('soil-plot-no-select');
    if (inp) inp.value = preservePlotNo;  // applyLoadedPlots가 비운 값을 복원
    if (sel) sel.value = preservePlotNo;  // onPlotSelect()가 항상 유지하던 동기화 규칙 그대로
    const isDone = typeof restored === 'object' && (restored.done === '완료' || restored.done === 'O' || restored.done === 'Y');
    display.textContent = isDone ? '✓ ' + preservePlotNo : preservePlotNo;
    display.style.color = isDone ? '#2E7D32' : '#E07B00';
    display.style.fontWeight = '700';
  } else {
    display.textContent = '포인트 선택';
    display.style.color = 'var(--text-sub)';
    display.style.fontWeight = '';           // ← 기존에 없던 리셋 추가(3절의 자잘한 기존 버그 정리)
  }
  dropdown.style.display = 'block';
}
```
`preservePlotNo`가 비었거나(팀이 바뀌었거나 원래 선택이 없었음) 새 `plots`
목록에 더 이상 없으면(서버에서 그 포인트가 빠짐) 자동으로 `else` 분기로 떨어져
기존과 동일하게 placeholder를 보여준다 — "복원 안 함"은 예외 처리가 아니라
조건식 자체가 자연스럽게 처리한다.

### 4.3 왜 호출부 7곳을 안 건드려도 되는가

7곳(`2399`, `2999`, `5473`, `5487`, `5502`, `5800`, `6216`) 전부 지금처럼
`applyLoadedPlots(무언가, teamCode)` 2-argument 호출을 그대로 유지한다.
`currentPlotNo`/`previousTeam`은 `applyLoadedPlots()` **내부에서** `inp.value`/
`loaded_team`을 읽어 직접 구하므로, 호출부는 "지금 뭐가 선택돼 있는지",
"팀이 바뀌었는지" 몰라도 된다 — 함수 자체가 자기 완결적으로 안전해진다
(`fillTeamDropdowns`가 이미 세운 "표시 함수 진입 시점에 방어한다" 원칙과
동일선상).

## 5. 알려진 한계 (문서화하고 넘어감)

- **(v2, 팀 전환 관련 항목은 4.1의 가드로 실제 차단됐으므로 이 절에서 제거함.)**
  v1에서는 "팀이 바뀌면서 우연히 같은 포인트명이 있는 경우"를 알려진 한계로
  남겼으나, Codex 검토에서 이게 데이터 귀속 오류로 이어질 수 있는 실제 위험이라는
  지적을 받아 4.1의 팀 일치 가드로 정식 처리했다 — 더는 한계가 아니다.
- **`onPlotSelect()`가 이 타이밍에 동시에 실행 중이면(사용자가 막 다른 포인트를
  클릭한 순간과 백그라운드 갱신이 겹치는 경우)** 어느 쪽이 나중에 `inp.value`를
  쓰느냐에 따라 결과가 달라질 수 있다. **(v2) Codex 검토로 확인**: JS 이벤트
  루프상 두 갱신이 같은 동기 구간에서 값을 동시에 쓰지 않는다 — `syncCompletedPlots()`
  등은 `await` 이후 재개되는 시점의 최신 `inp.value`를 캡처하고, 반대 순서로
  겹쳐도 `onPlotSelect()`가 이후에 실행되며 값을 확정하므로 별도의 동시성
  제어가 반드시 필요한 위험은 아니라고 판단, 이 항목만 알려진 한계로 남긴다.

## 6. 체크리스트

- [x] `applyLoadedPlots()`에 `currentPlotNo`/`previousTeam` 캡처 + 팀 일치 가드
      추가(4.1)
- [x] `buildCustomPlotList()`에 5번째 파라미터(`preservePlotNo`) 추가 + 마지막
      블록을 복원 조건문으로 교체(4.2), `else` 분기에 `fontWeight` 리셋 추가
- [x] `applyLoadedPlots()`의 `buildCustomPlotList(...)` 호출에 `preservePlotNo`
      인자 추가(호출부는 이 한 곳뿐)
- [x] 구문 검사: `node -e`로 4개 `<script>` 블록 파싱 확인
- [x] 로컬 브라우저 회귀 테스트(`python3 -m http.server` + devtools 콘솔):
  - [x] 포인트 목록을 불러온 뒤 하나 선택(`soil-plot-no` 값 확인) → 같은 팀으로
        `applyLoadedPlots()`를 다시 호출(`syncCompletedPlots`/
        `markPlotDoneInDropdown`과 동일 패턴) → `soil-plot-no` 값과 화면 라벨이
        그대로 유지되는지 확인(이번 수정의 핵심 시나리오) — PASS
  - [x] 선택 안 한 상태(`inp.value === ''`)에서 같은 패턴으로 갱신 → 기존과
        동일하게 placeholder로 남는지 확인(회귀 없음) — PASS
  - [x] 선택된 포인트가 갱신에서 완료 처리된 경우 → 선택이 유지되면서 라벨이
        `✓` 완료 스타일로 갱신되는지 확인 — PASS
  - [x] **(v2 핵심 시나리오)** 팀 A에서 포인트 선택 → 동일 문자열의 포인트를
        포함한 팀 B로 전환(`applyLoadedPlots(..., 'B팀')`) → `soil-plot-no`가
        복원되지 않고 placeholder로 떨어지는지, `loaded_team`이 'B팀'으로
        정상 갱신되는지 확인 — PASS
  - [x] 같은 팀 안에서 목록만 갱신되는 정상 시나리오(팀 안 바뀜)에서는 여전히
        선택이 유지되는지 확인(v2 가드가 정상 경로를 막지 않는지 회귀 확인) —
        PASS(위 첫 항목과 동일 검증)
  - [x] 같은 팀이지만 선택된 포인트가 새 목록에서 빠진 경우 → placeholder로
        자연 폴백하는지 확인 — PASS
  - [ ] 선택 후 실제로 사진을 촬영/저장 → `soil-plot-no` 값이 유지된 상태로
        정상적으로 그 표준지에 저장되는지 확인 — 사진 촬영이 필요해 자동화
        검증에서 생략(8절 참고, 별도 위험 없다고 판단)
  - [x] 기존 정상 흐름(직접 "포인트 불러오기" 클릭, 페이지 최초 로드) 회귀 없는지
        확인 — 콘솔 에러 없음
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)

## 7. Codex 교차 검토 결과

### 1차 검토 (v1 대상) — 차단 사유 있음, 아래 2건 반영 후 v2로 갱신

**차단 사유**:
- 호출부 수가 6곳이 아니라 7곳. `markPlotDoneInDropdown()` 내부
  `watermap_V100.html:5800`의 `applyLoadedPlots(...)` 호출이 3절·4.3절의
  "6개 호출부" 목록에서 빠짐(배경 설명에는 있었으나 목록 정정 필요) — 업로드
  성공 후 백그라운드 갱신의 핵심 경로이므로 반드시 목록에 포함해야 함. **(v2:
  반영 완료, 3·4.3절 "7곳"으로 정정.)**
- 팀 전환 시 같은 포인트명 충돌을 "알려진 한계"로 남기면 안 됨. 새 팀 목록에
  우연히 동일 `plotNo`가 있으면 `onPlotSelect()`(및 그에 따른 `_doReset()`,
  유역명/유역코드 재적용) 없이 이전 팀의 폼 내용이 새 팀 소속으로 복원될 수
  있어 데이터 귀속 오류 위험. 캡처 시점의 기존 `loaded_team`과 새 `teamCode`가
  같을 때만 복원하는 조건 추가 필요. **(v2: 반영 완료, 4.1의 팀 일치 가드.)**

**확인된 사항**:
- 근본 원인 진단은 대체로 정확함 — 값을 비우는 경로는 `applyLoadedPlots()`의
  `inp.value = ''`(5604)이며, `buildCustomPlotList()`는 `custom-plot-display`를
  placeholder로 되돌림(5726-5728). `_doReset()`(6323)도 모든 텍스트 인풋을
  비우지만 초기화·포인트 변경·저장 완료 후라는 의도된 경로이고, `onPlotSelect()`가
  직후 선택값을 다시 설정함(5835~5841) — 이번 버그와 무관.
- 4.1/4.2의 "값 캡처 → 목록 재구성 → 새 목록에 있을 때만 값·네이티브 select·라벨
  복원" 방식은 근본 원인에 직접 대응함. 별도 전역 상태를 만들지 않고 기존
  `soil-plot-no`를 선택 상태의 기준으로 유지하므로 적절. `display` 갱신을
  `textContent`로 하는 것도 안전.
- `_doReset()`과의 상호작용 정상 — 명시적 초기화·저장 완료 후에는 이미
  `soil-plot-no`가 빈 값이라 복원 조건이 성립하지 않고, `onPlotSelect()`는
  `_doReset()` 뒤 선택값·네이티브 select·라벨을 다시 설정하므로 기존 선택
  동작을 바꾸지 않음.
- 호출 경로별 검토: `2399`(시작 시 캐시 복원), `2999`(완료 목록 동기화),
  `5502`(오프라인 캐시 폴백), `5800`(업로드 완료 즉시 반영), `6216`(설정 로드)는
  동일 팀·동일 목록 갱신에서 선택 보존이 적절. `5473`(새 목록이 비었을 때)은
  목록 미존재로 자연스럽게 placeholder로 폴백. `5487`(서버 새 목록 로드)은
  팀 일치 가드가 있어야 안전(위 차단 사유 2번).
- `markPlotDoneInDropdown()`의 기존 후처리(5801~5806)는 새 복원 로직과
  중복되지만 충돌하지 않음 — 선택 중인 대상이 완료되면 새 로직에서 최신 완료
  스타일로 복원되고, 기존 후처리도 같은 결과를 한 번 더 적용.
- `onPlotSelect()`와 백그라운드 갱신의 "동시 실행"은 일반 이벤트 루프상 같은
  동기 구간에서 병렬로 값을 쓰지 않음 — `syncCompletedPlots()`가 `await` 뒤
  갱신하더라도 그 시점의 최신 `inp.value`를 캡처하고, 반대 순서면
  `onPlotSelect()`가 이후 값을 확정. 별도 동시성 제어가 반드시 필요한 위험은
  아님(5절에 알려진 한계로만 유지).
- `fontWeight`를 placeholder 분기에서 `''`로 되돌리는 보완은
  `selectCustomPlot('')` 및 `_doReset()`의 기존 동작과 일관되고 부작용 없음.
- 변경 범위는 DOM·localStorage 기반 포인트 표시/선택 계층에 한정됨. Google
  Apps Script/Sheets/Drive, payload, `sendToScript` 및 로컬 저장과 네트워크
  전송의 분리 원칙과 충돌하지 않음.

### 2차 검토 (v2 대상)

차단 사유 없음. 확인된 사항:
- `previousTeam` 캡처는 제안 위치에서 `localStorage.setItem('loaded_team',
  teamCode)`보다 반드시 먼저 실행된다 — 두 지점 사이에 `await`나 재진입 가능한
  호출이 없으므로 이전 팀과 새 팀을 안정적으로 비교할 수 있다.
- 7개 호출부를 하나씩 대조: `2399`(`savedTeam.trim() === TEAM_CODE.trim()` 확인
  후 호출), `2999`(`loaded_team`을 그대로 읽어 넘김), `5473`·`5487`
  (`loadPlotPoints()`가 계산한 팀 — 같은 팀 새로고침이면 통과, 실제 전환이면
  차단), `5502`(이미 `cachedTeam === teamCode` 확인 후 호출), `5800`
  (`loaded_team`을 직접 읽어 넘김), `6216`(`savedTeam`을 읽은 직후 그대로 넘김)
  — 전부 의도대로 동작. 팀 전환 시 `preservePlotNo`가 빈 문자열이 되어 새 목록에
  같은 `plotNo`가 있어도 복원되지 않고, `loaded_team`은 새 `teamCode`로 정상
  갱신됨을 확인.
- 지적: "대부분의 호출부에서 가드가 항상 통과한다"는 표현이 비동기 팀 전환
  경합까지 포함하면 엄밀하지 않음(`syncCompletedPlots()`의 `await fetch` 대기
  중 전환이 겹치는 경우) — 다만 그 경우에도 가드는 정상적으로 복원을 막으므로
  안전하다는 점은 동일. 표현을 완화하는 것이 정확함. → **반영 완료**(4.1 문단
  수정).
- `previousTeam`이 `setItem`보다 앞서야 한다는 순서 의존성은 실제 의미를 갖는
  제약이다. 향후 유지보수에서 `setItem`을 캡처 위로 옮기면 가드가 무력화된다 —
  차단 사유는 아니나 변경 금지 조건으로 유지해야 함. → **반영 완료**(4.1 코드에
  순서를 명시하는 주석 추가, 구현 시 그대로 반영됨 — 8절 참고).

## 8. 구현 및 검증 결과

**구현**: 4.1/4.2/4.3 전부 계획대로 반영. 호출부 7곳은 무변경(계획대로 —
`applyLoadedPlots`/`buildCustomPlotList` 두 함수 내부만 수정). `previousTeam`
캡처와 `setItem('loaded_team', ...)`의 순서 의존성은 코드에 인라인 주석으로
명시함(Codex 2차 검토의 "변경 금지 조건" 지적 반영).

**구문 검사**: `node -e`로 4개 `<script>` 블록 파싱 확인 — 통과.

**로컬 브라우저 실증 검증**(`python3 -m http.server` + devtools 콘솔에서
`applyLoadedPlots`/`selectCustomPlot`을 직접 호출해 시나리오 재현):
- 같은 팀에서 백그라운드 갱신(`syncCompletedPlots`/`markPlotDoneInDropdown`과
  동일 패턴) → `soil-plot-no`/네이티브 select/화면 라벨 전부 유지 — PASS
- 선택된 포인트가 그 갱신에서 완료 처리됨 → 선택 유지되며 라벨이 `✓` 완료
  스타일로 갱신 — PASS
- **v2 핵심 시나리오**: 같은 문자열의 `plotNo`가 포함된 다른 팀으로 전환 →
  `soil-plot-no`가 복원되지 않고 placeholder로 떨어짐, `fontWeight`도 리셋,
  `loaded_team`은 새 팀으로 정상 갱신 — PASS
- 선택 없는 상태에서 갱신 → 기존과 동일하게 placeholder 유지(회귀 없음) — PASS
- 같은 팀이지만 선택된 포인트가 새 목록에서 빠짐 → placeholder로 자연 폴백 — PASS

6절 체크리스트의 브라우저 회귀 테스트 항목 중 "실제로 사진을 촬영/저장"
시나리오는 사진 촬영이 필요해 이번 자동화 검증에서는 생략했다(위 5개 시나리오로
`soil-plot-no` 값 자체의 정확성은 충분히 검증됨 — 그 값을 읽는 `deletePhoto`
등의 로직은 이번 수정으로 변경되지 않았으므로 별도 회귀 위험은 없다고 판단).

콘솔 에러 확인 결과 이번 변경과 무관한 기존 항목(외부 스크립트 로드 관련, 네트워크
설정 미구성 환경에서 발생) 1건만 있었고 새로 발생한 에러는 없음.

`git commit`은 사용자 승인 후 진행.
