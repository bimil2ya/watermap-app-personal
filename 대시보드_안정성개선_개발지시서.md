# dashboard.html 안정성·코드 품질 개선 개발지시서

## 1. 배경과 목표

`dashboard.html`(조사 현황 대시보드)을 "경험 많은 개발자" 관점에서 전체 정밀 검토한 결과, 지금까지의 여러 차례 리뷰(레이스 컨디션, PIN 해시, XSS 이스케이프 등)에서 다루지 않은 새로운 항목 7개를 식별했다:

- **오류(실제 위험) 2건**: 전역 오류 안전망 부재, `AbortSignal.timeout()` 브라우저 호환성.
- **코딩 개선 2건**: 지도 관련 전역 변수 5개 통합, `openMap()` 함수 분리.
- **안정성 개선 3건**: 서비스워커 스코프의 숨은 의존성, 완료일 조회 캐싱 부재, 대시보드 전용 테스트 체크리스트 부재.

이 문서는 이 7개 항목 각각을 실제로 구현하기 위한 설계를 담는다. 사용자가 이 계획서를 내부 에이전트에게 반복 검토시킬 예정이므로, 각 항목은 **독립적으로 검증 가능**하게 설계했고, 항목별 위험도 차이를 명확히 구분해뒀다(일부는 즉시 적용 가능한 낮은 위험도 변경이고, 일부는 광범위한 리팩터링이라 신중한 검토가 필요하다).

## 2. 범위

**포함**:
- `dashboard.html`: 전역 오류 안전망, `fetchJson` 타임아웃 구현 교체, 지도 상태 전역 변수 통합, `openMap()` 함수 분리, 완료일 조회 결과 캐싱.
- 저장소 루트: `대시보드_TESTING_CHECKLIST.md` 신설.
- 서비스워커(`sw.js`) 스코프 이슈는 옵션 A(현행 유지)로 결정되어 **코드 변경 없음**(§3.5 참고).

**포함하지 않음**:
- 서버(Apps Script) 코드 변경 — 여전히 공유 백엔드는 건드리지 않는다는 원칙 유지.
- 빌드 시스템 도입, TypeScript 전환, 테스트 프레임워크(Jest 등) 도입 — 이 프로젝트는 빌드 없는 단일 정적 HTML이라는 아키텍처 제약이 있고(`CLAUDE.md`), 이를 바꾸는 건 이번 범위 밖이다. §3.7의 "테스트 체크리스트"는 자동화 테스트가 아니라 `TESTING_CHECKLIST.md`와 같은 수동 확인 목록이다.
- `watermap_V100.html` 자체의 리팩터링(그 파일은 이미 별도의 전역 오류 안전망이 있다고 파악됨 — 이번 문서는 `dashboard.html`에 동등한 보호를 추가하는 것만 다룬다).

## 3. 설계

### 3.1 전역 오류 안전망 추가 (낮은 위험도 · 즉시 적용 권장)

**현재 상태**: `dashboard.html`에는 `window.onerror`나 `unhandledrejection` 핸들러가 전혀 없다. 카카오 SDK 내부 오류나 예상 못한 서버 응답으로 어딘가에서 uncaught exception이 발생하면, 사용자는 화면이 멈춘 것처럼 보이는데 원인을 알 방법이 없다.

**설계**: `<script>` 태그 시작 부분(다른 어떤 로직보다도 먼저 — 이후 코드에서 나는 에러까지 전부 잡기 위해)에 두 개의 리스너를 추가한다.

```js
// ============================================================
// 전역 오류 안전망 — 이 스크립트의 다른 어떤 코드보다도 먼저 등록해서,
// 이후 실행되는 모든 코드(카카오 SDK 콜백 포함)에서 나는 예외를 잡는다.
// watermap_V100.html에 이미 있는 것과 동등한 보호를 대시보드에도 둔다.
// ============================================================
let _lastGlobalErrorAt = 0;
function _showGlobalErrorNote(message) {
  // 연속으로 여러 에러가 쏟아지는 상황(예: 같은 원인의 에러가 반복 발생)에서
  // error-note를 매번 덮어쓰며 깜빡이지 않도록 3초 안에 또 뜨는 건 무시한다.
  const now = Date.now();
  if (now - _lastGlobalErrorAt < 3000) return;
  _lastGlobalErrorAt = now;

  const el = document.getElementById('error-note');
  if (!el) return; // 이 핸들러 자체가 에러를 내면 안 되므로 방어적으로 확인
  el.textContent = '예상치 못한 오류가 발생했습니다: ' + message + ' (새로고침을 시도해보세요)';
  el.hidden = false;
}

window.addEventListener('error', (e) => {
  console.error('전역 오류:', e.error || e.message);
  _showGlobalErrorNote((e.error && e.error.message) || e.message || '알 수 없는 오류');
});

window.addEventListener('unhandledrejection', (e) => {
  console.error('처리되지 않은 Promise 거부:', e.reason);
  _showGlobalErrorNote((e.reason && e.reason.message) || String(e.reason) || '알 수 없는 오류');
});
```

**주의사항**:
- `_showGlobalErrorNote` 내부에서 `document.getElementById('error-note')`가 `null`일 가능성(핸들러가 DOM 로드 전에 실행되는 극히 드문 경우)을 방어한다.
- 기존에 `loadAll()`이나 `openMap()`이 자체적으로 `errorNote.hidden = false`로 정상적인 오류 메시지를 띄우는 경로와 이 전역 안전망이 충돌하지 않는지 확인 필요. **1차 검토에서 정정된 사실**: `loadAll()`은 전체가 try/catch로 감싸여 있어 재throw하지 않지만, `openMap()`은 `await loadKakaoSdk(key)` 호출 한 줄만 try/catch로 감싸여 있고 그 뒤 마커 생성·`InfoWindow`/`CustomOverlay` 생성 등 약 160줄은 **어떤 try/catch도 없이** 실행된다. 즉 "그 두 함수가 자체 try/catch로 대부분 방어돼 있다"는 전제는 `openMap()`에 대해서는 틀렸다 — 오히려 이 사실은 전역 안전망이 애초에 이 문서가 가정했던 것보다 **더 넓은 범위(openMap의 대부분)를 실질적으로 보호하게 된다**는 뜻이라, 도입 필요성은 더 커진다. 다만 결론(정상 흐름의 오류 메시지를 이 안전망이 덮어쓸 위험은 없음)은 그대로 유효하다 — `openMap()`이 이 무방비 구간에서 자체적으로 `errorNote`를 표시하는 코드가 없으므로, 이중 표시가 아니라 "지금까지 아무 신호도 없던 곳에 처음으로 신호가 생기는" 경우이기 때문이다. 3초 디바운스가 정상 오류 메시지 직후에 뜨는 별개의 uncaught 예외 메시지를 가려버릴 수 있는지는 실제 테스트로 확인한다(§4).

### 3.2 `fetchJson`의 `AbortSignal.timeout()` 호환성 개선 (낮은 위험도)

**현재 상태**:
```js
async function fetchJson(url) {
  const res = await fetch(url, { signal: AbortSignal.timeout(20000) });
  if (!res.ok) throw new Error('HTTP ' + res.status);
  return res.json();
}
```
`AbortSignal.timeout()`은 2022년 중반 이후 브라우저(Safari 16+, Firefox 100+, Chrome 103+)에서만 지원된다. 조원이 오래된 기기나 구형 인앱 브라우저로 접속하면 이 한 줄에서 `TypeError: AbortSignal.timeout is not a function`이 던져져 `fetchJson` 호출 자체가 즉시 깨진다.

**설계**: `AbortController` + `setTimeout`으로 직접 구현해 지원 범위를 넓힌다(동작은 동일 — 20초 후 요청 중단).

```js
async function fetchJson(url) {
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), 20000);
  try {
    const res = await fetch(url, { signal: controller.signal });
    if (!res.ok) throw new Error('HTTP ' + res.status);
    return await res.json();
  } finally {
    clearTimeout(timeoutId);
  }
}
```

`finally`로 `clearTimeout`을 반드시 호출해, 정상 응답이 빨리 와도 불필요한 타이머가 남아있지 않게 한다(메모리 누수는 아니지만 불필요한 타이머 콜백 예약을 피하는 좋은 습관).

**주의사항**: 타임아웃으로 중단됐을 때 `fetch`가 던지는 에러 이름이 브라우저마다 다를 수 있다(`AbortError` 등) — 기존 코드도 이 에러를 그냥 `err.message`로 사용자에게 보여주므로 별도 처리는 필요 없지만, 실제로 타임아웃을 강제로 유발해(예: `setTimeout(fn, 20001)` 같은 짧은 값으로 임시 테스트) 에러 메시지가 여전히 합리적으로 보이는지 확인한다.

### 3.3 지도 상태 전역 변수 5개를 단일 객체로 통합 (중간 위험도 · 광범위한 치환)

**현재 상태**: 아래 5개가 서로 독립된 최상위 `let`이다.
```js
let currentMap = null;
let currentMarkers = [];
let openInfoWindows = [];
let openDateLabels = [];
let mapLoadToken = 0;
```
오늘 하루 이 중 하나를 리셋하는 걸 빠뜨려서 실제로 버그가 난 적이 있다("완료일 확인 중..." 로딩 문구 고착). 5개를 하나의 상태 객체로 묶으면 "팀 전환 시 전부 리셋"을 한 곳에서 강제하기 쉬워진다.

**설계**:
```js
// 기존 5개의 let 선언을 아래 하나로 교체
const mapState = {
  map: null,
  markers: [],
  infoWindows: [],
  dateLabels: [],
  loadToken: 0,
};
```

**치환이 필요한 모든 위치** (실제 구현 시 아래 목록을 빠짐없이 확인 — 이 리팩터링의 위험은 "참조 하나를 빼먹는 것"이므로, grep으로 `currentMap\b`, `currentMarkers\b`, `openInfoWindows\b`, `openDateLabels\b`, `mapLoadToken\b`을 전부 검색해 남는 게 없는지 확인한다):

| 기존 | 신규 |
|---|---|
| `currentMap` (읽기/쓰기 전부) | `mapState.map` |
| `currentMarkers` (읽기/쓰기 전부) | `mapState.markers` |
| `openInfoWindows` (읽기/쓰기 전부) | `mapState.infoWindows` |
| `openDateLabels` (읽기/쓰기 전부) | `mapState.dateLabels` |
| `mapLoadToken` (읽기/쓰기 전부) | `mapState.loadToken` |

**참조 재할당 방식의 차이에 주의**: 지금 코드는 배열을 통째로 재할당하는 패턴(`currentMarkers = []`, `openInfoWindows = []`, `openInfoWindows = [info]`)을 쓴다. 객체 프로퍼티로 바꿔도 `mapState.markers = []` 형태는 문법적으로 동일하게 동작한다(프로퍼티 재할당이지 배열 in-place 변경이 아니어도 문제없다 — 다른 곳에서 이 배열을 별도 변수로 캡처해서 옛 배열 참조를 들고 있는 곳이 없는지만 확인하면 된다. grep 결과 그런 캡처는 없음을 이미 확인했다).

**`mapLoadToken`이 특히 주의 필요**: `const myToken = ++mapLoadToken;`이 `const myToken = ++mapState.loadToken;`으로 바뀌는데, `++` 전위 연산자가 객체 프로퍼티에도 동일하게 동작하는지(값을 증가시키고 증가된 값을 반환) 확인 — JS 스펙상 문제없이 동작하지만, 실제 구현 후 팀 전환 레이스 테스트(§4)로 재검증한다.

**이 항목의 우선순위**: 다른 항목보다 변경 범위가 넓어(파일 전체에서 5개 식별자를 전부 치환) 실수 여지가 있다. §4의 구현 순서에서 가장 마지막에, 그리고 전체 회귀 테스트를 동반해서 진행할 것을 권장한다.

### 3.4 `openMap()` 함수 분리 (높은 위험도 · 선택적, 낮은 우선순위 권장)

**현재 상태**: `openMap()`이 약 190줄이며, 세대 토큰 가드·지도 생성/재사용·마커 그리기·완료일 백그라운드 조회·날짜 드롭다운 채우기·`onchange` 핸들러 정의가 전부 한 함수 안에 있다. 오늘 이 함수를 여러 차례 수정하면서 가드(`if (myToken !== mapState.loadToken) return;`) 위치를 실수할 뻔한 적이 있었다.

**설계(참고용 — 실제 분리 여부는 §4에서 재검토)**: 아래와 같은 헬퍼로 나누는 것을 제안한다.
- `function clearPreviousTeamState()` — 마커 제거, `closeAllInfoWindows()`, 드롭다운 리셋(현재 732~744행 블록 — 단, 그 바로 앞 724~730행에 위치정보 없음 안내(`noCoordNote`) 처리가 별도 블록으로 있으므로, 실제 분리 시 이 블록의 정확한 경계를 다시 확인할 것. 1차 검토에서 지적됨).
- `function ensureMapReady(center)` — 지도 생성/재사용/배경 클릭 리스너 등록(현재 762~782행 블록), `isNewMap` 여부를 반환.
- `function drawMarkersForTeam(located)` — 마커·InfoWindow 생성 루프(현재 787~816행), `markerEntries` 배열을 반환.
- `async function loadCompletionDatesInBackground(markerEntries, myToken)` — 완료일 조회 + 정보창 갱신 + `groupsByDate` 생성(현재 818~838행), 가드 통과 여부(`boolean`)와 `groupsByDate`를 함께 반환.
- `function populateDateFilterDropdown(groupsByDate)` — 드롭다운 옵션 채우기 + `onchange` 핸들러 정의(현재 840~876행).

**이 리팩터링을 진행할 경우 반드시 지킬 것**: 이건 **순수 코드 이동**이어야 하며 로직을 조금이라도 바꾸면 안 된다. `if (myToken !== mapState.loadToken) return;` 가드는 실제로 **세 곳**이다(2차 검토에서 확인 — 현재 줄 번호 기준 709행 `catch` 블록 안, 714행 `try/catch` 직후, 827행 완료일 조회 후). 이 중 헬퍼 함수 내부로 들어가는 것은 **827행 하나뿐**이며(`loadCompletionDatesInBackground` 안에 위치), 709행과 714행은 어떤 헬퍼의 범위에도 속하지 않고 `openMap()` 본체에 그대로 남는다(709행은 `loadKakaoSdk` 실패 처리 안, 714행은 `clearPreviousTeamState` 호출보다도 앞이라 헬퍼로 옮길 대상 자체가 아니다). 실제 분리 작업 시 "가드가 몇 개이고 그중 어느 것이 헬퍼 안으로 들어가는지"를 이 문장 기준으로 다시 확인한 뒤 진행할 것 — 세 곳 중 하나라도 빠뜨리면 §3.3까지 통과한 팀 전환 레이스 방지가 다시 깨질 위험이 있다. 헬퍼로 옮겨지는 827행 가드는, 헬퍼가 "가드 실패"를 호출부에 정확히 알려서(`return null` 또는 `throw` 등으로) 호출부가 그 이후 로직(드롭다운 채우기 등)을 건너뛰게 만들어야 한다.

**권장**: 오늘 이 함수를 둘러싼 버그를 여러 번 겪은 만큼, 이 리팩터링은 **지금 당장 진행하지 말고**, 다음에 이 함수를 또 실제로 고칠 일이 생겼을 때 그 작업과 함께 진행하는 것을 권한다. §4의 구현 순서에서 최우선순위 밖에 둔다.

### 3.5 서비스워커(`sw.js`) 스코프의 숨은 의존성 (결정 완료 · 옵션 A, 코드 변경 없음)

**현재 상태 확인**: `watermap_V100.html`은 `navigator.serviceWorker.register('sw.js')`를 scope 옵션 없이 호출한다 — 기본 scope는 `sw.js`가 위치한 디렉터리(저장소 루트)이므로, 같은 디렉터리의 `dashboard.html`도 이 서비스워커의 `fetch` 이벤트 가로채기 대상이 된다(`sw.js`의 `fetch` 핸들러는 `event.request.url.startsWith(self.location.origin)`만 확인하고 특정 파일로 제한하지 않는다).

**실질적 영향**: `watermap_V100.html`을 먼저 방문한 적 있는 브라우저에서는 `dashboard.html`도 이 SW의 "완결성 검증 후 캐시" 전략(네트워크 우선, 실패 시 캐시 폴백)의 혜택을 받는다. 반면 **대시보드 링크로 곧장 들어와 `watermap_V100.html`을 한 번도 연 적 없는 브라우저**에서는 SW가 아예 등록되어 있지 않아 이 폴백이 없다 — 방문 순서에 따라 오프라인 복원력이 달라지는 숨은 의존성이다.

**옵션 (사용자 결정 필요)**:
- **옵션 A — 현행 유지**: 코드를 바꾸지 않고 이 사실만 문서에 남긴다. 서비스워커는 캐시 정책이 예민해서(과거 커밋 이력에도 "완결성 검증" 관련 버그가 있었다) 잘못 건드리면 새 배포가 즉시 반영 안 되는 등 새로운 문제가 생길 위험이 있다. 대시보드는 애초에 "가끔 열어보는 열람용" 화면이라 오프라인 지원이 필수는 아니라고 볼 수도 있다.
- **옵션 B — 대시보드에도 명시적으로 등록**: `dashboard.html`의 `initDashboard()` 또는 `<script>` 하단에 `if ('serviceWorker' in navigator) navigator.serviceWorker.register('sw.js');`를 추가해, 방문 순서와 무관하게 항상 동일한 오프라인 폴백을 받게 한다. `sw.js`의 `ASSETS`(install 시 미리 캐시하는 목록)에는 `dashboard.html`이 없지만, `fetch` 핸들러가 어차피 런타임에 모든 same-origin 200 응답을 캐시하므로 한 번 방문하면 그다음부터는 폴백이 생긴다.

**결정: 옵션 A(현행 유지)로 확정.** 실제 사용 패턴상 조원들은 거의 항상 메인 앱(`watermap_V100.html`)의 "경호버전" 배지를 거쳐 대시보드로 들어오므로, 이 순서상 문제(대시보드를 먼저 직접 여는 경우)가 실제로 벌어질 일은 거의 없다고 판단했다. 서비스워커는 이 프로젝트에서 과거 실제 버그(캐시 완결성 검증 이슈)가 났던 민감한 영역이라, 낮은 확률의 이론적 이득을 위해 지금 굳이 건드리지 않는다. **코드 변경 없음.**

### 3.6 완료일 조회 결과 캐싱 (중간 위험도)

**현재 상태**: `openMap()`이 팀을 열 때마다 그 팀의 완료 포인트 수만큼(최대 10건) `fetchCompletionDate()`를 매번 새로 호출한다. 같은 팀을 여러 번 왔다갔다 열어도 캐싱이 전혀 없어 매번 다시 조회한다. Apps Script는 동시 실행 제한이 있어, 여러 조원이 동시에 대시보드를 쓰면 이 반복 호출이 체감 속도 저하나 일부 요청 실패로 이어질 수 있다.

**참고(1차 검토에서 확인)**: 캐시 키를 `plotNo`만으로 쓰는데, 만약 서로 다른 팀이 같은 `plotNo`를 가질 수 있다면 캐시가 다른 팀 결과를 잘못 재사용할 위험이 있다. 다만 이 위험은 **캐싱이 새로 만드는 것이 아니라 기존 코드에 이미 있던 것**이다 — 캐싱 도입 전에도 `fetchCompletionDate(scriptUrl, plotNo)`는 팀 구분 없이 `plotNo`만으로 서버에 조회했다(서버의 `getSurveyData` API 자체가 팀 파라미터를 받지 않음). 캐싱은 그 결과를 5분간 재사용할 뿐이므로 이 문서가 새로 도입하는 위험이 아니다.

**설계**: 세션(페이지가 열려있는 동안) 동안만 유지되는 짧은 TTL 캐시를 추가한다. 완료일 데이터는 "그날 이후로는 안 바뀌는" 값이지만, 현장에서 방금 막 완료 처리한 조사가 아주 최근에 반영됐을 가능성을 감안해 TTL을 짧게(5분) 둔다.

```js
// 완료일 조회 결과를 세션 동안 짧게 캐싱 — 같은 팀을 반복해서 열 때마다
// 매번 Apps Script를 다시 호출하지 않기 위함. 완료일은 사실상 안 바뀌는
// 값이지만, 방금 막 현장에서 완료 처리된 조사가 아주 최근에 반영됐을 수
// 있으므로 TTL을 짧게(5분) 둔다 — 무한 캐싱은 하지 않는다.
const COMPLETION_DATE_CACHE_TTL_MS = 5 * 60 * 1000;
const completionDateCache = new Map(); // plotNo -> { value: {label, isoDate} | null, cachedAt }

async function fetchCompletionDate(scriptUrl, plotNo) {
  const cached = completionDateCache.get(plotNo);
  if (cached && Date.now() - cached.cachedAt < COMPLETION_DATE_CACHE_TTL_MS) {
    return cached.value;
  }
  let value;
  try {
    const data = await fetchJson(scriptUrl + '?action=getSurveyData&plotNo=' + encodeURIComponent(plotNo));
    if (!data || !Array.isArray(data.soilRow)) {
      value = null;
    } else {
      const isoDate = data.soilRow[3];
      const label = formatKoreanDate(isoDate);
      value = label ? { label, isoDate } : null;
    }
  } catch (e) {
    value = null;
  }
  completionDateCache.set(plotNo, { value, cachedAt: Date.now() });
  return value;
}
```

**수동 새로고침과의 상호작용**: `loadAll(true)`(새로고침 버튼)를 눌렀을 때는 사용자가 "최신 상태를 보고 싶다"는 의도가 명확하므로, 캐시를 무시하고 강제로 다시 조회해야 한다. `loadAll`에서 `isManualRefresh === true`일 때 `completionDateCache.clear();`를 호출하도록 추가한다.

```js
async function loadAll(isManualRefresh) {
  ...
  if (isManualRefresh) {
    completionDateCache.clear(); // 새로고침은 항상 최신 데이터를 봐야 하므로 캐시 무효화
  }
  ...
}
```

**주의사항**: 실패한 조회(`value = null`, 네트워크 오류 등)도 캐싱된다 — 이는 의도된 것이다(같은 팀을 5분 안에 재차 열었을 때 이미 실패했던 조회를 또 반복해서 20초씩 기다리게 하지 않기 위함). 다만 "실패"와 "완료일이 원래 없음"을 캐시 레이어가 구분하지 않는데, 이건 기존 `fetchCompletionDate`의 동작(둘 다 `null` 반환)과 동일하므로 회귀가 아니다.

### 3.7 대시보드 전용 테스트 체크리스트 문서 작성 (낮은 위험도)

**현재 상태**: `watermap_V100.html`은 `TESTING_CHECKLIST.md`가 있어 회귀를 걸러내지만, `dashboard.html`은 전용 체크리스트가 없다 — 오늘 하루 여러 레이스 컨디션 버그를 매번 즉석 브라우저 콘솔 테스트로 발견·검증했다.

**설계**: 저장소 루트에 `대시보드_TESTING_CHECKLIST.md`를 신설하고, 오늘 실제로 검증한 시나리오를 목록화한다.

```markdown
# dashboard.html 테스트 체크리스트

수정 후 아래 중 **건드린 기능과 관련된 항목만** 브라우저에서 재현해 확인한다
(watermap_V100.html의 TESTING_CHECKLIST.md와 동일한 방식 — 전체를 매번
돌리지 않는다).

## PIN 게이트
- [ ] 올바른 PIN(7119)으로 통과되는지
- [ ] 틀린 PIN이 거부되고 입력창이 비워지는지

## 팀 목록
- [ ] 포인트가 있는 팀은 진행률 바 + N/M 표시
- [ ] 포인트가 0건이고 서버 응답이 정상인 팀은 "포인트 배정 대기 중" 배지
- [ ] 포인트가 0건인데 removedCount > 0인 팀은 "⚠️ 데이터 오류" 배지

## 지도 — 기본 동작
- [ ] 팀 클릭 시 마커가 완료일 조회를 기다리지 않고 즉시 표시되는지
- [ ] 완료일 조회가 끝나면 날짜 드롭다운이 나중에 나타나는지
- [ ] 정보창에 "완료 · 날짜" 또는 "미완료"가 정확히 표시되는지
- [ ] 미완료 마커는 빨간색, 완료 마커는 초록색인지

## 지도 — 날짜 그룹
- [ ] 날짜 선택 시 그 날짜의 포인트 위에만 라벨이 뜨고, 나머지 마커는 흐려지는지
- [ ] 드롭다운을 "날짜 선택"으로 되돌리면 라벨이 없어지고 마커가 원래대로 복구되는지

## 지도 — 상호작용
- [ ] 마커 클릭 → 정보창 하나만 열리는지(이전 것은 닫히는지)
- [ ] 지도 빈 배경 클릭 → 정보창/날짜 라벨 전체 닫히고 마커 흐림 해제되는지
- [ ] 정보창 본문(카카오맵 링크) 클릭이 배경 클릭으로 전파되어 곧바로 닫히지 않는지

## 레이스 컨디션(팀 빠른 연속 전환)
- [ ] 3개 팀을 빠르게 연속 클릭했을 때 최종적으로 마지막 팀만 정확히 반영되는지
  (제목·마커·날짜 드롭다운이 전부 마지막 팀 것과 일치)
- [ ] 완료일 조회 중인 팀에서 위치 정보 없음/포인트 없음 팀으로 전환해도
  "완료일 확인 중..." 문구가 고착되지 않는지

## 예외 경로
- [ ] 카카오 JS 키가 없을 때 key-setup 화면이 뜨는지
- [ ] 위치 정보가 없는 팀(좌표 없음)을 열면 정확한 안내 문구가 뜨는지
```

## 4. 구현 순서와 검증 계획

낮은 위험도부터, 그리고 서로 독립적인 항목부터 진행한다. 각 항목은 별도 커밋으로 나눈다.

1. **§3.7 테스트 체크리스트 문서 작성** — 코드 변경이 아니므로 가장 먼저, 위험 없이 진행. 이후 항목들의 검증 기준으로 바로 사용한다.
2. **§3.1 전역 오류 안전망** — 브라우저 콘솔에서 의도적으로 예외를 던져(`setTimeout(() => { throw new Error('테스트'); }, 100)`) `error-note`에 메시지가 뜨는지 확인. 기존 정상 오류 흐름(예: 팀 목록 로드 실패)이 이 안전망과 충돌해 메시지가 이상하게 겹치지 않는지도 확인.
3. **§3.2 `fetchJson` 타임아웃 구현 교체** — 정상 응답이 여전히 잘 오는지, 그리고 (가능하다면) 짧은 타임아웃 값으로 임시로 바꿔 타임아웃 경로도 한 번 타보고 원래 값(20초)으로 되돌리는 방식으로 확인.
4. **§3.6 완료일 조회 캐싱** — 같은 팀을 두 번 연속 열었을 때 두 번째는 네트워크 요청이 없거나 훨씬 빠른지(네트워크 탭으로 확인), 새로고침 버튼을 누르면 캐시가 무시되고 다시 조회하는지 확인.
5. **§3.3 지도 상태 전역 변수 통합** — 가장 넓은 범위의 치환이므로, §4의 "레이스 컨디션" 체크리스트 전체와 "지도 — 상호작용" 체크리스트 전체를 재실행해서 회귀가 없는지 확인.
6. **§3.5 서비스워커 스코프** — 옵션 A(현행 유지)로 결정 완료. 코드 변경 없음, 검증 불필요.
7. **§3.4 `openMap()` 함수 분리** — 이번 라운드에서는 보류. 다음에 이 함수를 실제로 수정할 일이 생기면 그때 함께 진행.

## 5. 알려진 한계

- **§3.6의 캐시는 세션(페이지 리로드 전까지)에만 유효하다.** 페이지를 새로고침(F5)하면 캐시가 사라진다 — 이는 의도된 것이다(더 오래 유지하려면 `localStorage`를 써야 하는데, 그러면 "완료일이 바뀌었는데 계속 옛날 값을 보여주는" 위험이 커진다).
- **§3.1의 전역 안전망은 오류의 "원인"을 고치지 못한다.** 사용자에게 뭔가 잘못됐다는 신호만 줄 뿐이고, 근본 원인은 여전히 콘솔 로그(`console.error`)로 개발자가 직접 확인해야 한다.
- **§3.3을 완료해도 §3.4(함수 분리)는 별도 작업이다.** 두 항목을 동시에 진행하면 어느 변경 때문에 문제가 생겼는지 구분하기 어려워지므로, 순서대로 하나씩 진행하고 검증한 뒤 다음으로 넘어간다.
- **§3.5는 옵션 A(현행 유지)로 결정됐다.** 즉 `dashboard.html`을 메인 앱을 거치지 않고 곧장 여는 경우(북마크 등)에는 여전히 서비스워커의 오프라인 폴백을 받지 못한다 — 이는 받아들이기로 한 한계다. 나중에 이 사용 패턴이 실제로 문제가 되면(예: 대시보드 링크를 메인 앱과 무관하게 공유하는 경우가 늘어나면) 그때 옵션 B를 재검토한다.
