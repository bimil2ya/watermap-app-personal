# watermap-app-personal — 저장목록 "선택 전송" (체크박스로 골라서 전송) 개발지시서

## 1. 배경과 목표

여러 표준지(a, b, c, d)를 현장에서 조사·저장해두고, 인터넷이 원활한 곳에서 일부만
먼저 전송하고 나머지는 나중에 원하는 시점에 보내고 싶다는 요청. 예: a, b는 지금
전송, c, d는 보류했다가 나중에 전송.

**현재 동작**: 저장목록 탭 상단 "📤 미전송 N건 전송" 버튼(`manualSendAll()` →
`autoRetryUpload()`)은 **미완료 레코드 전체**를 예외 없이 순차 전송한다
(`watermap_V100.html:3295`, `records.filter(...)`에 개별 선택 조건이 없음). 개별
카드의 "재업로드" 버튼(`reuploadRecord(id)`)은 그 표준지 하나만 보내지만, "지금
보낼 것"을 여러 개 골라서 한 번에 보내는 중간 단계가 없다.

**목표**: 저장목록 "전체" 탭의 각 미완료 카드에 체크박스를 추가하고, 상단 전송
버튼이 **체크된 것만** 대상으로 삼도록 바꾼다. 서버로 나가는 payload 형식·개별
"재업로드" 버튼·업로드 재시도/오류기록 로직은 전혀 건드리지 않는다 — "이번에
보낼 대상을 어떻게 고르는가"라는 클라이언트 UI 레이어만 추가한다.

## 2. 범위

**포함**:
- 저장목록 "전체" 탭(`currentFilter === 'all'`) 카드에 체크박스 추가(완료된
  카드는 체크박스 자체를 노출하지 않음 — 보낼 게 없으므로).
- 상단 "미전송 N건 전송" 버튼을 "선택 N건 전송"으로 바꾸고, 체크된 레코드만
  대상으로 전송.
- "전체 선택" 토글(전부 켜기/끄기 한 번에).
- 선택 상태가 목록 재렌더링(`renderRecordsList()` 재호출) 후에도 유지되게
  하는 상태 관리.

**명시적 제외**:
- 개별 "재업로드" 버튼(`reuploadRecord(id)`) — 완전히 무변경. 체크박스와
  별개의 기존 경로 그대로 유지.
- 서버로 나가는 payload 구성, soil/tree/사진 전송·재확인·오류기록 로직
  (`autoRetryUpload()` 내부의 실제 전송 부분) — 무변경. "어떤 레코드가 이번
  루프의 `incomplete` 배열에 포함되는가"라는 필터 조건 한 줄만 바뀐다.
- 입지·토양/임목조사/사진촬영 필터 탭 — 체크박스를 추가하지 않는다. 이 탭들은
  항목별 세부 정보를 보여주는 뷰라서 체크박스를 넣으면 "전체" 탭과 뭐가
  다른지 혼란만 커진다. 선택은 "전체" 탭 하나로 통일한다.

## 3. 현재 상태 (전제 조건)

- `renderRecordsList()`(`watermap_V100.html:6408`)가 `container.innerHTML =
  list.map(r => `...`).join('')`로 "전체" 탭 카드를 매번 통째로 새로 그린다
  — 즉 순수 JS 변수가 아닌 이상, DOM에 남겨둔 상태(체크박스 checked 등)는
  다음 렌더링에서 전부 사라진다. 이 파일은 이미 이런 문제를 겪는 다른
  상태들(`currentFilter`, `_bodyScrollLocks`)을 **재렌더링과 무관하게 살아있는
  최상위 `let` 변수**에 저장해두고 렌더링 시점에 참조하는 패턴을 쓰고 있다 —
  이번에도 같은 패턴을 그대로 재사용한다(새 기법 도입 없음).
- 미완료 판정 기준은 이미 두 곳(`renderRecordsList()`의 `pendingCount` 계산,
  `autoRetryUpload()`의 `incomplete` 계산)에 사실상 동일한 코드로 중복돼
  있다: `!r.soilUploaded || !isTreeComplete(r) || !photosDone`. 이번 작업에서
  전자를 `updateSendAllBar()`라는 별도 함수로 뽑아내면서, 선택 상태 정리
  로직도 그 함수 안에 자연스럽게 들어간다(아래 4.2 참고).
- 레코드 `id`는 `Date.now()`로 생성되는 유니크 값이다(`watermap_V100.html:3885`,
  `id: existingRec2 ? existingRec2.id : Date.now()`) — 선택 상태를 이 `id`
  기준으로 저장하면 재렌더링·필터 전환과 무관하게 안정적으로 유지된다.
- `deleteRecord(id)`(`watermap_V100.html:6849`)가 확인창을 거쳐 레코드를
  지운다 — 선택 상태에도 그 id가 정리돼야 한다(4.5 참고).

## 4. 설계

### 4.1 상태 저장 방식 — "선택함" Set이 아니라 "해제함" Set (opt-out)

**핵심 설계 결정**: 어떤 레코드가 선택됐는지를 저장하는 대신, **명시적으로
체크 해제한 레코드의 id만** `deselectedRecordIds`라는 `Set`에 저장한다(opt-out
모델). 그 반대(선택된 것만 저장하는 opt-in 모델)를 쓰지 않는 이유:

- 사용자의 요청 시나리오("보통은 다 보내고, 일부만 보류")와 정확히 맞는다 —
  기본값이 "전체 선택"이어야 하는데, opt-in Set은 처음엔 비어있어서 아무것도
  선택 안 된 것처럼 보이므로 매번 사용자가 일일이 체크해야 한다.
- **"새로 저장된 레코드"를 별도로 감지할 필요가 없어진다.** opt-in
  방식이면 "이 레코드를 이전에 본 적 있는지"를 따로 추적해서 "처음 보는
  거면 기본 체크"라는 로직을 넣어야 하는데(별도의 `knownRecordIds` 같은
  추적 상태가 하나 더 필요해짐), opt-out 방식은 애초에 `Set`에 없으면
  자동으로 "선택됨"이므로 이 문제 자체가 생기지 않는다 — 새 레코드는 아무
  것도 안 해도 자동으로 선택된 상태로 나타난다.
- 기존 동작(체크박스가 없던 시절 = "항상 전체 선택")과 완전히 하위 호환된다
  — `deselectedRecordIds`가 항상 비어있으면 지금까지의 "미전송 N건 전송"과
  100% 동일하게 동작한다.

```js
// 저장목록 "전체" 탭 선택 전송 — 명시적으로 체크 해제한 레코드 id만 기록(opt-out).
// Set에 없으면 "선택된" 것으로 간주하므로, 새로 저장된 레코드는 아무 처리 없이도
// 자동으로 선택된 상태(=기존처럼 "미전송이면 다 보낸다")로 나타난다.
let deselectedRecordIds = new Set();
```
`currentFilter` 선언부(`watermap_V100.html:6388`) 바로 아래에 추가한다.

### 4.2 `updateSendAllBar()` — 기존 인라인 로직을 함수로 분리

현재 `renderRecordsList()` 맨 위(6412~6425번 줄)에 "미완료 건수 계산 + 버튼
텍스트 갱신"이 인라인으로 박혀 있다. 이걸 별도 함수로 뽑아서 ① 목록 전체
재렌더링 없이 체크박스 하나 토글할 때 버튼 텍스트만 즉시 갱신할 수 있게
하고, ② 완료·삭제된 레코드의 해제 기록을 정리하는 자리로 쓴다:

```js
function updateSendAllBar() {
  const records = getSurveyRecords();
  const incompleteRecs = records.filter(r => {
    const photosDone = !r.photoList || r.photoList.length === 0 || r.photoList.every(p => p.uploaded);
    return !r.soilUploaded || !isTreeComplete(r) || !photosDone;
  });

  // 완료되거나(더 이상 미완료 목록에 없는) 삭제된 레코드의 "해제" 기록은 정리한다 —
  // 안 지워도 다음 전송 대상 계산에는 영향 없지만(어차피 완료된 건 incomplete에서
  // 빠지므로), Set이 무한정 자라는 걸 막는다.
  const incompleteIds = new Set(incompleteRecs.map(r => r.id));
  [...deselectedRecordIds].forEach(id => { if (!incompleteIds.has(id)) deselectedRecordIds.delete(id); });

  const selectedCount = incompleteRecs.filter(r => !deselectedRecordIds.has(r.id)).length;

  const sendAllBar   = document.getElementById('send-all-bar');
  const sendAllBtn   = document.getElementById('send-all-btn');
  const sendAllCount = document.getElementById('send-all-count');
  const selectAllCb  = document.getElementById('select-all-checkbox');

  if (sendAllBar) sendAllBar.style.display = incompleteRecs.length > 0 ? 'block' : 'none';
  if (sendAllCount) sendAllCount.textContent = selectedCount;
  if (sendAllBtn) {
    sendAllBtn.disabled = _isUploading || selectedCount === 0;
    sendAllBtn.textContent = _isUploading ? '업로드 중...' : `📤 선택 ${selectedCount}건 전송`;
  }
  // "전체 선택" 체크박스 상태를 실제 선택 현황과 동기화(부분 선택이면 중간状態로 표시)
  if (selectAllCb) {
    selectAllCb.checked = incompleteRecs.length > 0 && selectedCount === incompleteRecs.length;
    selectAllCb.indeterminate = selectedCount > 0 && selectedCount < incompleteRecs.length;
  }
}
```

`renderRecordsList()`의 기존 인라인 블록(6412~6425번 줄)을 이 함수 호출
한 줄로 교체한다:
```js
function renderRecordsList() {
  const records = getSurveyRecords();
  const container = document.getElementById('records-list');
  updateSendAllBar();
  // (기존의 pendingCount/sendAllBar 관련 인라인 코드는 전부 제거 — 위 함수가 대체)
  ...
```

### 4.3 체크박스 토글 핸들러

```js
// 체크박스 하나를 토글할 때는 목록 전체를 다시 그리지 않는다 — 그러면 스크롤
// 위치·다른 카드의 포커스가 날아간다. 버튼 텍스트/카운트만 갱신하면 충분하다.
function toggleRecordSelection(id, checked) {
  if (checked) deselectedRecordIds.delete(id);
  else deselectedRecordIds.add(id);
  updateSendAllBar();
}

function toggleSelectAll(checked) {
  if (checked) {
    deselectedRecordIds.clear();
  } else {
    const records = getSurveyRecords();
    records.forEach(r => {
      const photosDone = !r.photoList || r.photoList.length === 0 || r.photoList.every(p => p.uploaded);
      if (!r.soilUploaded || !isTreeComplete(r) || !photosDone) deselectedRecordIds.add(r.id);
    });
  }
  // 개별 체크박스 DOM도 전부 바뀌어야 하므로 이번엔 전체 재렌더링이 맞다
  // (renderRecordsList가 내부에서 updateSendAllBar도 호출하므로 이 한 줄로 충분).
  renderRecordsList();
}
```

### 4.4 UI — "전체" 탭 카드에 체크박스 추가 (`watermap_V100.html:6464` 부근)

카드 헤더(표준지 번호가 표시되는 곳) 왼쪽에 체크박스를 넣는다. **완료된
카드(`allOk === true`)는 체크박스를 아예 렌더링하지 않는다** — 이미 전송된
걸 다시 고를 이유가 없고, 기존 "재업로드" 버튼도 같은 조건(`!allOk`)으로
노출 여부를 결정하고 있어 일관된 규칙이다:

```js
return `
<div class="record-item" style="padding:0;overflow:hidden;">
  <button class="record-delete" onclick="deleteRecord(${r.id})" title="삭제">×</button>
  <div style="padding:10px 40px 8px 14px;border-bottom:0.5px solid var(--gray-border);display:flex;align-items:flex-start;gap:8px;">
    ${!allOk ? `<input type="checkbox" style="margin-top:3px;width:18px;height:18px;flex-shrink:0;"
      ${!deselectedRecordIds.has(r.id) ? 'checked' : ''}
      onchange="toggleRecordSelection(${r.id}, this.checked)">` : '<span style="width:18px;flex-shrink:0;"></span>'}
    <div>
      <div style="font-size:15px;font-weight:700;color:var(--text);">표준지 ${escapeHtml(r.plotNo)}</div>
      <div style="font-size:12px;color:var(--text-sub);margin-top:2px;">${escapeHtml(r.basin || '유역 미입력')} · ${escapeHtml(r.date || '날짜 미입력')}</div>
    </div>
  </div>
  ...
```
(완료된 카드에도 `<span style="width:18px;">` 빈 자리를 둬서, 체크박스가
있는 카드와 없는 카드가 섞여도 제목 줄이 좌우로 흔들리지 않게 정렬을
맞춘다.)

### 4.5 "전체 선택" 토글 + 버튼 문구 (`watermap_V100.html:990` 부근)

```html
<div id="send-all-bar" style="display:none;margin-bottom:10px;">
  <label style="display:flex;align-items:center;gap:6px;font-size:13px;color:var(--text-sub);margin-bottom:8px;">
    <input type="checkbox" id="select-all-checkbox" onchange="toggleSelectAll(this.checked)">
    전체 선택
  </label>
  <button onclick="manualSendAll()" id="send-all-btn"
    style="width:100%;padding:14px;background:var(--green);color:white;border:none;border-radius:var(--radius);font-size:16px;font-weight:700;cursor:pointer;">
    📤 선택 <span id="send-all-count">0</span>건 전송
  </button>
</div>
```
버튼 문구를 "미전송"에서 "선택"으로 바꾼다 — 체크박스가 생긴 이상 "미전송
N건"이라고 하면 "전부 보낸다는 뜻인가, 고른 것만인가" 헷갈릴 수 있어서,
항상 정확한 "선택 N건"으로 통일한다(전체 선택 상태에서는 어차피 미전송
건수와 숫자가 같으므로 기존 문구가 완전히 사라지는 느낌도 아니다).

### 4.6 `autoRetryUpload()` 필터 조건에 선택 여부 추가 (`watermap_V100.html:3295`)

```js
const incomplete = records.filter(r => {
  const photosDone = !r.photoList || r.photoList.length === 0 || r.photoList.every(p => p.uploaded);
  return (!r.soilUploaded || !isTreeComplete(r) || !photosDone) && !deselectedRecordIds.has(r.id);
});
if (incomplete.length === 0) return;
```
`&& !deselectedRecordIds.has(r.id)` 한 조건만 추가한다. 이 아래의 전송 루프
(soil→tree→사진 순차 처리, 실패 기록, `interrupted` 처리 등)는 전혀 손대지
않는다 — "무엇을 보낼지 고르는" 단계만 바뀌고 "어떻게 보내는지"는 그대로다.

`manualSendAll()`(`watermap_V100.html:3014`) 자체는 무변경 — 이미
`autoRetryUpload()`를 그대로 호출하는 얇은 래퍼라 손댈 게 없다. 버튼이
`selectedCount === 0`일 때 이미 `disabled`(4.2) 처리되므로, "선택 없이
버튼을 누르는" 상황 자체가 UI 레벨에서 막힌다 — 별도 안내 토스트는 필요
없다고 판단한다(버튼이 눌리지 않으니 도달할 일이 없는 코드는 추가하지
않는다).

### 4.7 `deleteRecord(id)`에 선택 상태 정리 추가 (`watermap_V100.html:6849`)

```js
function deleteRecord(id) {
  ...(기존 확인창·IDB 정리 로직 그대로)...
  deselectedRecordIds.delete(id); // 삭제된 레코드의 해제 기록은 남겨둘 이유가 없음
  ...(기존 나머지 로직)...
}
```
`updateSendAllBar()`가 어차피 "완료/삭제로 더 이상 미완료 목록에 없는 id는
정리"하는 로직을 갖고 있어서(4.2) 이 줄이 없어도 다음 렌더링 때 자동으로
정리되긴 하지만, 명시적으로 그 자리에서 지워주는 게 의도를 더 분명하게
드러낸다(방어적 코딩, 비용 거의 없음).

## 5. 알려진 한계 (문서화하고 넘어감)

- **체크박스 상태는 새로고침하면 초기화된다.** `deselectedRecordIds`는 순수
  JS 메모리 변수라 `localStorage`에 영속화하지 않는다 — 페이지를 새로고침하면
  전부 다시 "전체 선택" 기본값으로 돌아간다. 이건 의도된 설계다: "지금
  이 순간 뭘 보낼지"를 고르는 일회성 선택이지, "이 레코드는 앞으로 계속
  보류"라는 영구 설정이 아니기 때문이다(사용자의 시나리오도 "나중에 원하는
  시간에 전송"이지 "이 레코드를 영원히 자동 전송 대상에서 뺀다"가 아니다).
  만약 새로고침 후에도 보류 상태를 기억해야 하는 요구가 생기면 별도 요청으로
  `localStorage` 영속화를 추가하면 된다 — 이번 범위에는 포함하지 않는다.
- **"전체" 탭에서만 체크박스가 보인다.** 입지·토양/임목조사/사진촬영 탭에서는
  체크박스가 없다 — 2절에서 밝힌 이유(혼란 방지)로 의도된 제약이다.
- 체크박스를 껐다 켰다 하는 동안 `_isUploading`이 `true`(전송 진행 중)이면
  체크박스 자체를 비활성화하지는 않는다(전송 중에는 애초에 "재업로드" 버튼도
  `disabled` 처리되지만 체크박스는 다음 전송 대상을 미리 고르는 용도로 그냥
  둔다 — 지금 전송 중인 배치에는 영향 없음, 다음 번 버튼 클릭에만 반영됨).

## 6. 체크리스트

- [ ] `deselectedRecordIds` 전역 상태 추가(4.1)
- [ ] `updateSendAllBar()` 함수 추가 + `renderRecordsList()`의 기존 인라인
      로직을 이 함수 호출로 교체(4.2)
- [ ] `toggleRecordSelection()`/`toggleSelectAll()` 함수 추가(4.3)
- [ ] "전체" 탭 카드에 체크박스 렌더링(완료 카드는 제외)(4.4)
- [ ] `send-all-bar`에 "전체 선택" 체크박스 + 버튼 문구 변경(4.5)
- [ ] `autoRetryUpload()`의 `incomplete` 필터에 선택 조건 추가(4.6)
- [ ] `deleteRecord()`에 선택 상태 정리 추가(4.7)
- [ ] 구문 검사: 5개 `<script>` 블록 파싱 확인
- [ ] 로컬 브라우저 회귀 테스트(`localStorage.survey_records`에 a/b/c/d
      4개 미완료 레코드 주입):
  - [ ] 초기 진입 시 4개 전부 체크된 상태로 보이는지("전체 선택" 체크박스도
        checked) 확인
  - [ ] c, d를 체크 해제 → "선택 2건 전송"으로 버튼 문구가 즉시(재렌더링
        없이) 바뀌는지 확인
  - [ ] 이 상태로 전송 버튼 클릭(목 서버 사용) → a, b만 실제로 전송 요청이
        나가고 c, d는 요청 자체가 발생하지 않는지 네트워크 로그로 확인
  - [ ] a, b 전송 완료 후 목록이 재렌더링되면 a, b 카드는 체크박스가 사라지고
        (완료됨) c, d는 여전히 체크 해제된 채로 남아있는지 확인
  - [ ] "전체 선택" 체크박스를 다시 눌러 전부 선택 → c, d도 체크되고 버튼이
        "선택 2건 전송"(남은 미완료 2개)으로 정확히 표시되는지 확인
  - [ ] 일부만 선택된 상태에서 "전체 선택" 체크박스가 indeterminate(중간
        상태) 표시로 보이는지 확인
  - [ ] 개별 카드의 "재업로드" 버튼이 체크박스 상태와 무관하게 기존과
        동일하게 그 레코드만 전송하는지(회귀 확인)
  - [ ] 레코드 삭제(`deleteRecord`) 시 콘솔 예외 없이 정상 처리되는지, 그
        레코드가 선택 상태 정리 대상에서도 깔끔히 빠지는지 확인
  - [ ] 미완료 레코드가 0개(전부 완료 또는 빈 목록)일 때 `send-all-bar`
        자체가 안 보이는지(회귀 확인)
  - [x] 페이지 새로고침 후 다시 미완료 레코드가 있는 상태로 진입 →
        "전체 선택" 기본값으로 돌아오는지(5절의 의도된 동작) 확인
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)

## 7. 구현·검증 결과 (서브에이전트 2라운드 검토 완료)

**구현**: 4.1~4.7 전부 계획대로 반영. 구문 검사(5개 script 블록) 통과.

**로컬 브라우저 실증 검증**(fetch monkey-patch로 실제 네트워크 요청 캡처):
체크리스트 전 항목 통과. 특히 "선택된 것만 전송" 핵심 동작은 c, d를 체크
해제한 뒤 전송 버튼을 눌러 **네트워크 요청이 정확히 a, b분(soil+tree 각
1건씩, 총 4건)만 나가고 c, d에 대한 요청은 0건**임을 직접 확인해 검증했다.

**서브에이전트 1라운드 — 실제 버그 1건 발견**: `autoRetryUpload()`가
`_isUploading`을 `true`로 바꾼 직후 `send-all-btn`의 disabled/문구를 갱신하지
않아서, 체크박스를 건드리지 않는 한 업로드 배치가 끝날 때까지 버튼이 클릭
전 상태("선택 N건 전송", 클릭 가능)로 남아있는 문제. `save-btn`/개별
`reupload-btn-*`은 정상 반영되므로 이 버튼에만 해당. 데이터 훼손·중복
전송 위험은 없지만(`manualSendAll()`의 `_isUploading` 재진입 방지로 보호됨)
"버튼이 눌렸는지 안 눌렸는지 몇 초간 알 수 없는" UX 결함 — 이번 diff가
새로 만든 문제는 아니고(이전 인라인 로직도 같은 자리에서만 갱신됐음) 체크박스
도입으로 상대적으로 더 눈에 띄게 된 기존 공백. **수정**: `beginNetworkUploadAttempt()`
직후 `updateSendAllBar()` 한 줄 추가.

**서브에이전트 2라운드 — 수정 검증**: 5초 지연 fetch 목으로 재현, 클릭
200ms 후 `text="업로드 중...", disabled=true` 확인. "루프 시작 전에
`updateSendAllBar()`를 한 번 더 부르는 것 자체의 부작용" 여부도 확인 —
`incomplete` 배열이 이 호출보다 먼저 스냅샷으로 확정되고, 함수 자체가
멱등적(여러 번 불러도 누적 부작용 없음)이라 문제없음. **지적사항 없음,
2라운드 만에 수렴.**

**Low(조치 불필요, 기록만)**: `#send-all-count` span이 `textContent`
전체 재작성으로 최초 렌더 후 DOM에서 사라지는 것(이번 diff 이전부터 있던
기존 패턴), 미완료 판정식이 4곳에 문자 그대로 중복된 것(전부 동치임을
서브에이전트가 직접 확인, 당장 조치 불필요 — 나중에 판정 기준이 바뀌면
`isRecordIncomplete(r)` 같은 공용 헬퍼로 리팩터링 고려).

## 8. Codex 교차 검토 결과

차단 사유 없음. 확인된 사항:
- opt-out 상태 관리가 요구사항에 잘 맞음 — 새 미완료 레코드 자동 선택,
  재렌더링·필터 전환에도 유지, 완료/삭제 id 정리로 메모리 누적 방지.
- 미완료 판정이 `updateSendAllBar()`/`toggleSelectAll()`/`autoRetryUpload()`
  전부에서 동일 기준(soil/tree/photos, `isTreeComplete()` 공용 사용) —
  임목 미입력 레코드를 완료로 취급하는 기존 규칙과도 일치.
- 전송 대상이 시작 시 `incomplete` 배열로 스냅샷되므로, 업로드 중 체크
  변경이 현재 배치의 soil→tree→사진 순차 처리나 `interrupted` 처리에
  영향 없이 다음 전송에만 반영됨 — 문서 의도와 일치.
- `beginNetworkUploadAttempt()` 직후 `updateSendAllBar()` 호출이 적절함
  — `_isUploading=true` 즉시 반영으로 중복 클릭 방지, `finally`에서
  `_isUploading=false` 복구 후 재렌더링으로 완료 후 상태도 정상 복원.

**반영한 개선**: 카드 체크박스에 접근 가능한 이름이 없다는 지적을 받아
`aria-label="표준지 {plotNo} 전송 선택"`을 추가함(저비용, 스크린리더
호환성 개선).

**반영하지 않은 사항**: "전체 선택"이 업로드 중에도 조작 가능해 사용자가
현재 배치까지 바뀐다고 오해할 여지가 있다는 지적 — Codex 스스로 "현재
동작 자체는 안전합니다"라고 명시했고 "향후 필요하면" 수준의 선택적
제안이라, 이번 범위에서는 반영하지 않음.

`git diff --check` 통과.
