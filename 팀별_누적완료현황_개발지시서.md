# watermap-app-personal — 설정탭에 팀별 누적 완료 현황 표시 기능 개발지시서 (v3)

**v2 변경 사항**: v1(이 문서의 기존 내용)은 이미 구현되고 서브에이전트 리뷰까지
마쳤다(아직 커밋 전). 리뷰에서 나온 지적 3가지(형식오류 건수 미노출, 팀 목록
"정상 빈 배열" vs "응답 이상" 구분 안 됨, 실패 시 시각색 없음)를 반영해 4.5의
코드를 최신 상태로 갱신했다. v2는 여기에 더해 사용자가 새로 요청한 두 가지를
추가한다: **① 팀별 주차별 완료 세분화(주차까지의 누적 포함), ② 팀명 옆 유역명
표시**. 6절이 이 부분이다. v2는 서브에이전트 5라운드 검토를 거쳐 구현·커밋
완료됐다(flex-wrap 오버플로우, `margin-left:auto` 정렬 수정 등 리뷰 중 나온
CSS 수정 포함 — 이 문서의 4.4/6.3 HTML·JS 스니펫은 v2 설계 시점 기준이라 그
이후의 세부 CSS 수정까지 전부 반영돼 있지는 않다, 실제 코드가 최종본).

**v3 변경 사항**: 사용자가 이 기능("🔄 전체 팀 현황 불러오기" 버튼)에 별도
비밀번호 게이트를 요청했다. **이는 2절의 기존 "명시적 제외" 결정(비밀번호 없이
누구나 볼 수 있게 한다)을 뒤집는 것** — 사용자가 직접 새로 요청한 사항이므로
그 결정을 이번 버전에서 갱신한다. 8절이 이번에 새로 추가된 부분이다.

## 1. 배경과 목표

사용자가 설정탭에서 "팀 목록을 하나씩 선택 → 포인트 불러오기"를 반복하면 각 팀의
완료 개수를 확인할 수 있다는 걸 확인했고, 이 조회를 매번 수동으로 반복하지 않고
**설정탭 맨 아래(전역 고정 저장바 바로 위)에 모든 팀의 누적 완료 현황을 한 번에
보여주는 기능**을 요청했다.

**중요한 전제**: 이 완료 개수는 **날짜별이 아니라 누적**이다. 서버(Apps Script)의
`getPlots` 액션(`watermap_V100.html:1465-1497`, 관리자가 배포한 서버 코드 템플릿)을
확인한 결과, `done` 여부는 "그 조사지번호가 현장조사 시트에 **한 번이라도** 올라와
있는지"로만 판정하고 업로드 날짜는 전혀 비교하지 않는다:
```js
done: uploadedPlotNos.has(r[2].toString().trim()) ? '완료' : ''
```
그래서 이번에 만드는 요약도 "오늘 몇 개"가 아니라 "지금까지 누적으로 몇 개"를
보여주는 것이며, 화면에도 이 사실을 명시해 혼동을 막는다.

**목표**: 서버 코드는 전혀 건드리지 않고(사용자는 이 백엔드의 관리자가 아님, 기존
원칙 유지), 이미 존재하는 두 액션(`getTeams`, `getPlots`)만 클라이언트에서 여러 번
호출·조합해서 팀별 누적 완료 현황을 한 화면에 보여준다.

## 2. 범위

**포함**: 설정탭(`tab-setting`) 맨 끝(관리자 섹션 밖, `</div><!-- /tab-setting -->`
바로 앞, `watermap_V100.html:1574`)에 새 카드 하나 추가:
- "🔄 전체 팀 현황 불러오기" 버튼(수동 트리거 — 자동 실행 안 함)
- 팀별 `완료 N / 전체 M` 목록 + 합계 행
- "누적 기준(날짜 무관)"이라는 캡션 텍스트 고정 표시

**명시적 제외**:
- 서버(Apps Script) 코드 수정 — 관리자 소관, 기존 원칙 그대로 유지.
- 날짜별/오늘자 집계 — 서버가 날짜 정보를 아예 안 주므로 이번 범위에서 불가능
  (필요하면 관리자에게 서버 쪽 `getPlots`가 완료일자까지 반환하도록 요청하는 것이
  유일한 방법이며, 이건 이 문서의 범위 밖).
- 관리자 비밀번호(`ADMIN_PW`, 3864)로 이 요약을 잠그는 것 — 완료 개수는 지금도
  "포인트 불러오기"로 누구나 볼 수 있는 정보라(관리자 전용 정보 아님), 관리자
  잠금(`admin-content`) 섹션 **안**으로 옮기지는 않는다. **(v3에서 갱신: 관리자
  비밀번호와는 별개로, 이 카드 하나만을 위한 독립된 비밀번호 게이트는 8절에서
  새로 추가한다 — "관리자 전용 정보가 아니다"라는 판단 자체는 그대로 유지하되,
  사용자가 이 화면을 아무나 바로 볼 수 없게 해달라고 별도로 요청했기 때문.)**

## 3. 현재 상태 (전제 조건)

- 현재 커밋: `118f53d` (원격 배포 확인됨).
- 재사용 가능한 기존 자산:
  - `normalizeCachedTeams(list)`/`normalizeLoadedPlots(list)` — 서버 응답 안전 파싱 (이미 구현됨, `watermap_V100.html:5093-5109` 부근)
  - `fetchWithTimeout(url, options, timeoutMs)` — 이미 구현된 타임아웃 fetch 래퍼(`watermap_V100.html:3943`), 지금까지는 `sendToScript`(POST) 전용으로만 쓰였지만 GET 요청에도 그대로 재사용 가능
  - `escapeHtml(s)` — innerHTML 삽입 시 이스케이프(`watermap_V100.html:6201` 부근)
  - `MASTER_SCRIPT_URL`, `localStorage.getItem('script_url') || MASTER_SCRIPT_URL` 패턴 — 기존 모든 서버 호출과 동일하게 사용
- `getPlots` 액션은 **팀 하나씩만** 조회 가능하다(`?action=getPlots&team=X`) — "전체 팀 한 번에" 같은 서버 액션은 없다. 그래서 팀 목록(`getTeams`)을 먼저 받아서, 그 각 팀에 대해 `getPlots`를 반복 호출하는 방식이 유일한 구현 경로다.

## 4. 핵심 설계

### 4.1 데이터 흐름

```
"🔄 전체 팀 현황 불러오기" 클릭
  → ① fetchWithTimeout(scriptUrl + '?action=getTeams', ..., 20000)로 팀 목록 조회
  → ② normalizeCachedTeams()로 안전 파싱
  → ③ 팀이 0개면 "팀 목록이 없습니다" 안내 후 종료
  → ④ 각 팀에 대해 Promise.allSettled로 병렬 fetchWithTimeout(getPlots&team=X, ..., 20000)
  → ⑤ 각 결과를 normalizeLoadedPlots()로 안전 파싱 → 팀별 {완료, 전체} 집계
  → ⑥ 실패한 팀은 목록에 "불러오기 실패" 표시(그 팀만, 나머지는 정상 표시)
  → ⑦ 팀별 행 + 합계 행 렌더링, escapeHtml()로 팀명 이스케이프
```

### 4.2 왜 순차가 아니라 병렬(`Promise.allSettled`)인가

팀이 여러 개면 순차 호출 시 Apps Script 콜드스타트까지 겹쳐 수십 초가 걸릴 수 있다.
`Promise.allSettled`로 병렬 실행하면 전체 소요 시간이 "가장 느린 팀 1개" 수준으로
줄어든다. `Promise.all`이 아니라 `allSettled`를 쓰는 이유는, 팀 하나의 요청이
실패해도(타임아웃·네트워크 오류) 나머지 팀 결과는 정상적으로 보여줘야 하기 때문 —
전체를 한 번에 실패 처리하면 "다른 팀은 다 성공했는데 딱 하나 때문에 전체가
안 보이는" 불필요한 실패를 만든다.

### 4.3 서버 부하 관련 판단

이 기능은 공유 Apps Script 백엔드에 팀 수만큼의 GET 요청을 한 번에 보낸다. 이미
사용자가 "팀을 하나씩 선택해서 포인트 불러오기"를 수동으로 반복하던 것과 총
요청 수는 같고(팀당 1회), **자동으로 반복 실행되지 않고 사용자가 버튼을 누를
때만 실행**되므로 기존 사용 패턴보다 서버에 새로운 부하를 추가하지 않는다고
판단한다. 다만 팀 수가 아주 많을 경우(예: 20개 이상) 동시 요청이 한꺼번에
나가는 게 부담될 수 있으니, 팀 개수가 많으면(임계값 예: 10개 초과) 경고 없이
그냥 진행하되, 관찰 결과 문제가 있으면 추후 동시 실행 개수를 제한(chunk)하는
방향으로 조정한다 — 지금은 과도한 선제 최적화를 하지 않는다.

### 4.4 UI 배치 및 HTML

`watermap_V100.html:1574`(`</div><!-- /tab-setting -->`) 바로 앞에 삽입:
```html
<div class="card" style="margin-bottom:14px;">
  <div class="card-header">
    <span class="card-header-icon">📊</span>
    <h2>팀별 누적 현황</h2>
  </div>
  <div class="card-body">
    <div style="font-size:12px;color:var(--text-sub);margin-bottom:10px;">
      전체유무와 무관하게 지금까지 누적으로 업로드된 개수입니다(날짜별 집계 아님).
    </div>
    <button onclick="loadTeamSummary()" id="load-team-summary-btn"
      style="width:100%;padding:11px;background:var(--green);color:white;border:none;border-radius:var(--radius-sm);font-size:14px;font-weight:700;cursor:pointer;">
      🔄 전체 팀 현황 불러오기
    </button>
    <div id="team-summary-status" style="display:none;margin-top:8px;font-size:12px;color:var(--text-sub);"></div>
    <div id="team-summary-list" style="margin-top:10px;"></div>
  </div>
</div>
```

### 4.5 JS 함수 설계 (v2 — 실제 구현·서브에이전트 리뷰 반영된 최신 버전)

```js
async function loadTeamSummary() {
  const scriptUrl = localStorage.getItem('script_url') || MASTER_SCRIPT_URL;
  const btn = document.getElementById('load-team-summary-btn');
  const status = document.getElementById('team-summary-status');
  const listEl = document.getElementById('team-summary-list');
  if (!btn || !status || !listEl) return;

  btn.disabled = true;
  btn.textContent = '불러오는 중...';
  status.style.display = 'block';
  status.style.color = 'var(--text-sub)'; // 이전 실행에서 남은 색상 초기화
  status.textContent = '팀 목록 확인 중...';
  listEl.innerHTML = '';

  try {
    const teamsRes = await fetchWithTimeout(scriptUrl + '?action=getTeams', {}, 20000);
    const teamsData = await teamsRes.json();
    const teamsNorm = normalizeCachedTeams(teamsData.teams);
    const teams = teamsNorm.items;

    if (teams.length === 0) {
      // 정상적으로 빈 배열(팀이 아직 없음) vs 응답 형식이 이상해서 비어버린 경우 구분
      status.style.color = 'var(--warning)';
      status.textContent = (teamsNorm.wasArray && teamsNorm.originalLength === 0)
        ? '팀 목록이 없습니다.'
        : '팀 목록 응답 형식이 올바르지 않습니다 — 관리자에게 문의해주세요.';
      return;
    }

    status.style.color = 'var(--text-sub)';
    status.textContent = teams.length + '개 팀 조회 중...';

    const results = await Promise.allSettled(teams.map(async team => {
      const url = scriptUrl + '?action=getPlots&team=' + encodeURIComponent(team);
      const res = await fetchWithTimeout(url, {}, 20000);
      const data = await res.json();
      const norm = normalizeLoadedPlots(data.plots);
      const doneCount = norm.items.filter(p => {
        const d = typeof p === 'object' ? p.done : '';
        return d === '완료' || d === 'O' || d === 'Y';
      }).length;
      return { team, total: norm.items.length, done: doneCount, removedCount: norm.removedCount };
    }));

    let totalAll = 0, doneAll = 0;
    const failedTeams = [];
    const rows = results.map((r, i) => {
      if (r.status === 'fulfilled') {
        totalAll += r.value.total;
        doneAll += r.value.done;
        const removedNote = r.value.removedCount > 0
          ? ` <span style="color:var(--warning);">(형식오류 ${r.value.removedCount}건 제외)</span>`
          : '';
        return `<div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:0.5px solid var(--gray-border);font-size:13px;">
          <span>${escapeHtml(r.value.team)}</span>
          <span>완료 ${r.value.done} / 전체 ${r.value.total}${removedNote}</span>
        </div>`;
      } else {
        failedTeams.push(teams[i]);
        console.warn('팀 현황 조회 실패:', teams[i], r.reason);
        return `<div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:0.5px solid var(--gray-border);font-size:13px;color:var(--danger);">
          <span>${escapeHtml(teams[i])}</span>
          <span>불러오기 실패</span>
        </div>`;
      }
    });

    rows.push(`<div style="display:flex;justify-content:space-between;padding:8px 0;margin-top:4px;border-top:1.5px solid var(--text);font-size:13px;font-weight:700;">
      <span>합계</span>
      <span>완료 ${doneAll} / 전체 ${totalAll}</span>
    </div>`);

    listEl.innerHTML = rows.join('');
    if (failedTeams.length > 0) {
      status.style.color = 'var(--danger)';
      status.textContent = teams.length + '개 팀 중 ' + failedTeams.length + '개 조회 실패';
    } else {
      status.style.color = 'var(--green-dark)';
      status.textContent = teams.length + '개 팀 조회 완료';
    }
  } catch (e) {
    status.style.color = 'var(--danger)';
    status.textContent = '전체 팀 현황을 불러오지 못했습니다 — 인터넷 연결을 확인해주세요.';
    console.warn('loadTeamSummary 실패:', e);
  } finally {
    btn.disabled = false;
    btn.textContent = '🔄 전체 팀 현황 불러오기';
  }
}
```

이 v1 코드는 **아래 6절의 v2 확장을 적용하면 통째로 대체**된다 — 6절이 이 함수의
최종 목표 버전이다.

## 5. 알려진 함정

- **누적 vs 날짜별 오해 방지**: UI 캡션("날짜별 집계 아님")을 반드시 유지한다 —
  안 그러면 사용자가 "오늘 몇 개 올라왔다"로 오해할 수 있다(이전 대화에서 이미
  한 번 이 오해가 있었음).
- **`getPlots` 응답이 `{}`/`[null]` 등 비정상이어도 크래시 없어야 함**: 이미
  구현된 `normalizeLoadedPlots()`를 그대로 재사용하므로, 팀 하나의 서버 응답이
  깨져 있어도 그 팀만 `total:0, done:0`으로 처리되고 전체가 죽지 않는다 —
  다만 이 경우 "실패"가 아니라 "0/0으로 조용히 표시"되므로, 진짜 실패(네트워크
  오류 등)와 "정상 응답인데 빈 배열"을 구분하지 못한다는 잔여 한계가 있다(아래
  체크리스트에서 검증).
- **`Promise.allSettled`의 rejected 사유 노출 안 함**: 지금 설계는 실패한 팀에
  "불러오기 실패"라고만 보여주고 구체적 원인(타임아웃/네트워크/파싱)은 콘솔에만
  남긴다 — 이 요약 기능은 참고용 정보이지 업로드 재시도처럼 정확한 원인 추적이
  중요한 기능이 아니라고 판단해 간단하게 유지한다.
- **버튼 연타 방지**: `btn.disabled = true`로 조회 중 중복 클릭을 막는다(기존
  `loadTeamList()`/`loadPlotPoints()`와 같은 패턴).

## 6. v2 확장 — 주차별 세분화 + 팀명 옆 유역명 표시

### 6.1 요구사항 원문과 해석

사용자 요청: "몇 주차까지 몇 개 완료됐는지" 세분화, "유역명(basin)도 팀명옆에 표시".

- **주차별 세분화**: `getPlots` 응답의 각 포인트에 이미 `week`(주차) 필드가 있다
  (`watermap_V100.html:1492`, v1 설계 때는 이 필드를 그냥 버렸었다). "몇 주차
  까지"라는 표현은 "주차별로 각각 몇 개"뿐 아니라 **그 주차까지 누적으로 몇
  개인지**까지 원하는 것으로 해석해, 주차별 행마다 해당 주차만의 완료/전체와
  그 주차까지의 누적 완료/전체를 함께 보여준다.
- **유역명 표시**: `getPlots` 응답에 `basin`(유역명) 필드도 이미 있다
  (`watermap_V100.html:1485`). 한 팀이 여러 유역에 걸쳐 포인트를 배정받을 수도
  있으므로, "팀명 옆"이라는 요청 그대로 **팀명 옆에 그 팀이 담당하는 고유
  유역명을 나열**하는 라벨로 처리한다(유역별로 그룹을 새로 나누는 것은 아님 —
  요청 문구가 "유역별로도 나눠서"가 아니라 "팀명 옆에 표시"였으므로).

두 필드 모두 **서버 호출을 추가하지 않고**(이미 `getPlots` 응답에 포함되어 있음)
클라이언트에서 집계 로직만 확장하면 된다.

### 6.2 데이터 집계 확장

팀별 결과 객체에 `basins`(고유 유역명 배열)와 `weeks`(주차별 집계 배열)를
추가한다. 주차 그룹핑·정렬 규�칙은 `applyLoadedPlots()`가 이미 쓰고 있는
`groupByWeek`/`sortedWeekKeys` 패턴(`watermap_V100.html:5441-5452`)을 그대로
재사용해서 — "week 없으면 주차미정으로 묶고, 숫자 주차는 오름차순, 주차미정은
맨 뒤"라는 기존 규칙과 다른 새 규칙을 만들지 않는다:

```js
const results = await Promise.allSettled(teams.map(async team => {
  const url = scriptUrl + '?action=getPlots&team=' + encodeURIComponent(team);
  const res = await fetchWithTimeout(url, {}, 20000);
  const data = await res.json();
  const norm = normalizeLoadedPlots(data.plots);

  const isDone = p => {
    const d = typeof p === 'object' ? p.done : '';
    return d === '완료' || d === 'O' || d === 'Y';
  };

  // 팀명 옆에 표시할 고유 유역명 (문자열 항목은 basin이 없으므로 자동 제외됨)
  const basinSet = new Set();
  norm.items.forEach(p => {
    const b = typeof p === 'object' ? (p.basin || '').toString().trim() : '';
    if (b) basinSet.add(b);
  });

  // applyLoadedPlots()와 동일한 주차 그룹핑·정렬 규칙 재사용
  const weekGroups = {};
  norm.items.forEach(p => {
    const week = (typeof p === 'object' ? p.week : '') || '주차미정';
    (weekGroups[week] = weekGroups[week] || []).push(p);
  });
  const weekKeys = Object.keys(weekGroups).sort((a, b) => (parseInt(a)||9999) - (parseInt(b)||9999));
  const weeks = weekKeys.map(week => {
    const arr = weekGroups[week];
    return { week, total: arr.length, done: arr.filter(isDone).length };
  });

  const doneCount = norm.items.filter(isDone).length;
  return {
    team,
    basins: [...basinSet],
    total: norm.items.length,
    done: doneCount,
    removedCount: norm.removedCount,
    weeks
  };
}));
```

### 6.3 렌더링 확장 — 팀 행 + 유역명 라벨 + 주차별 하위 행(누적 포함)

```js
const rows = results.map((r, i) => {
  if (r.status === 'fulfilled') {
    totalAll += r.value.total;
    doneAll += r.value.done;

    const removedNote = r.value.removedCount > 0
      ? ` <span style="color:var(--warning);">(형식오류 ${r.value.removedCount}건 제외)</span>`
      : '';
    // 유역명 라벨 — 팀에 유역 정보가 하나도 없으면 라벨 자체를 생략
    const basinLabel = r.value.basins.length > 0
      ? ` <span style="color:var(--text-sub);font-weight:400;">(${r.value.basins.map(escapeHtml).join(', ')})</span>`
      : '';

    // 주차별 하위 행 — 그 주차만의 완료/전체 + "그 주차까지"의 누적 완료/전체
    let cumDone = 0, cumTotal = 0;
    const weekRows = r.value.weeks.map(w => {
      cumDone += w.done;
      cumTotal += w.total;
      const label = w.week === '주차미정' ? '주차미정' : (w.week + '주차');
      return `<div style="display:flex;justify-content:space-between;padding:3px 0 3px 14px;font-size:12px;color:var(--text-sub);">
        <span>${escapeHtml(label)}</span>
        <span>완료 ${w.done}/${w.total} <span style="color:var(--gray-text);">(누적 ${cumDone}/${cumTotal})</span></span>
      </div>`;
    }).join('');

    return `<div style="padding:6px 0;border-bottom:0.5px solid var(--gray-border);">
      <div style="display:flex;justify-content:space-between;font-size:13px;font-weight:600;">
        <span>${escapeHtml(r.value.team)}${basinLabel}</span>
        <span>완료 ${r.value.done} / 전체 ${r.value.total}${removedNote}</span>
      </div>
      ${weekRows}
    </div>`;
  } else {
    // 실패 행은 v1과 동일 (변경 없음)
    ...
  }
});
```

### 6.4 설계 판단 근거

- **주차미정을 누적 순서 맨 뒤에 두는 이유**: `sortedWeekKeys`가 이미 "주차미정"을
  가장 뒤로 보내므로(가중치 9999), 누적 합계는 자연스럽게 숫자 주차 → 주차미정
  순으로 쌓여서 마지막 행의 누적값이 항상 그 팀의 전체 합계(`total`/`done`)와
  정확히 일치한다 — 별도 검증 로직 없이 구조 자체로 정합성이 보장된다.
- **유역명은 그룹 재구성이 아니라 라벨**: 유역별로 화면을 다시 나누면(팀 → 유역
  → 주차 3단 구조) 정보 밀도가 급격히 늘어나 설정탭 하단 요약이라는 이 기능의
  성격과 안 맞는다고 판단했다. "팀명 옆에 표시해줘"라는 요청 문구 그대로 라벨
  형태를 선택했다 — 만약 유역별로도 완전히 나눠보고 싶다면 별도 요청으로
  처리하는 게 낫다.
- **문자열 타입 포인트(레거시 형식)는 유역·주차 정보가 아예 없음**: `normalizeLoadedPlots`가
  허용하는 문자열 형태 포인트(예: 그냥 `"A-01"`)는 `basin`/`week` 필드 자체가
  없으므로 자동으로 "유역 없음"·"주차미정"으로 처리된다 — 별도 예외 처리 불필요
  (`typeof p === 'object' ? ... : ''` 가드가 이미 이 케이스를 흡수함).
- **서버 부하 변화 없음**: 이번 확장은 이미 받아온 `getPlots` 응답을 클라이언트에서
  다르게 집계하는 것뿐이라, 서버 호출 횟수·빈도는 v1과 완전히 동일하다.

## 7. v3 확장 — 비밀번호 게이트

### 7.1 요구사항

"🔄 전체 팀 현황 불러오기" 버튼을 눌러 실제 데이터를 보여주기 전에 비밀번호를
먼저 확인한다. 비밀번호는 `7119`, 힌트 문구는 "집번호4자리"를 화면에 항상
표시한다.

### 7.2 설계 — 기존 관리자 잠금 패턴 재사용

이 앱에는 이미 `admin-lock-box`/`checkAdminPw()`(`watermap_V100.html:1087-1097,
6869-6890`)라는, 비밀번호 입력칸 + 확인 버튼으로 구성된 잠금 UI 패턴이 있다.
새 비밀번호 체계를 따로 만들지 않고 **같은 패턴을 그대로 복제**해서 일관성을
유지한다 — 단, `ADMIN_PW`(3864)와는 완전히 별개의 상수(`TEAM_SUMMARY_PW`,
7119)와 별개의 DOM id를 쓴다(관리자 잠금과 이 잠금은 서로 무관하게 독립적으로
동작해야 하므로 — 관리자로 로그인했다고 이 카드가 자동으로 풀리거나, 이 카드를
풀었다고 관리자 기능이 풀리면 안 된다).

- **영속성 없음**: 관리자 잠금과 동일하게, 페이지를 새로고침하면 다시 잠긴
  상태로 돌아간다(`localStorage`에 해제 상태를 저장하지 않음) — 관리자 잠금도
  세션 내 토글일 뿐 영속화하지 않는 것과 같은 수준의 보안으로 맞춘다.
- **버튼은 기본 숨김, 잠금 해제 시에만 노출**: `load-team-summary-btn`에
  `display:none`을 기본값으로 주고, 비밀번호가 맞을 때만 `block`으로 바꾼다 —
  버튼 자체를 안 보이게 해서 "비밀번호 확인 전에는 클릭 자체가 불가능"하도록
  한다(버튼은 보이는데 클릭 시 별도 확인이 뜨는 방식보다 더 단순하고, 관리자
  잠금과 동일한 "잠금 해제 전에는 콘텐츠 자체가 안 보인다" 원칙).
- **힌트는 항상 노출**: 관리자 잠금엔 힌트가 없지만, 이번 요청은 힌트 문구를
  명시적으로 요구했으므로 잠금 박스 안에 "힌트: 집번호4자리"를 고정 텍스트로
  둔다(오답 시에도 사라지지 않음 — 관리자 잠금의 오답 메시지처럼 오답 시에만
  나타나는 게 아니라, 사용자가 매번 다시 볼 수 있게 항상 표시).

### 7.3 HTML (`watermap_V100.html:1580` 부근, 기존 버튼을 대체)

```html
<div style="font-size:12px;color:var(--text-sub);margin-bottom:10px;">
  날짜와 무관하게 지금까지 누적으로 업로드된 개수입니다(오늘 업로드한 개수가 아닙니다).
</div>
<div id="team-summary-lock-box" style="background:var(--gray-bg);border:0.5px solid var(--gray-border);border-radius:var(--radius-sm);padding:10px;display:flex;flex-wrap:wrap;align-items:center;gap:8px;justify-content:center;">
  <span style="font-size:12px;color:var(--text-sub);">🔒 비밀번호</span>
  <input type="password" id="team-summary-pw-input" placeholder="비밀번호" maxlength="10"
    style="width:100px;padding:5px 8px;border:0.5px solid var(--gray-border);border-radius:var(--radius-sm);font-size:13px;text-align:center;"
    onkeydown="if(event.key==='Enter') checkTeamSummaryPw()">
  <button onclick="checkTeamSummaryPw()"
    style="padding:5px 12px;background:var(--green);color:white;border:none;border-radius:var(--radius-sm);font-size:13px;font-weight:700;cursor:pointer;">
    확인
  </button>
  <span id="team-summary-pw-error" style="display:none;font-size:12px;color:var(--danger);width:100%;text-align:center;">틀렸습니다</span>
  <span style="font-size:11px;color:var(--text-sub);width:100%;text-align:center;">힌트: 집번호4자리</span>
</div>
<button onclick="loadTeamSummary()" id="load-team-summary-btn"
  style="display:none;width:100%;padding:11px;background:var(--green);color:white;border:none;border-radius:var(--radius-sm);font-size:14px;font-weight:700;cursor:pointer;">
  🔄 전체 팀 현황 불러오기
</button>
<div id="team-summary-status" style="display:none;margin-top:8px;font-size:12px;color:var(--text-sub);"></div>
<div id="team-summary-list" style="margin-top:10px;"></div>
```

### 7.4 JS (기존 `checkAdminPw()`/`lockAdmin()` 바로 뒤에 추가)

```js
const TEAM_SUMMARY_PW = '7119';
function checkTeamSummaryPw() {
  const input = document.getElementById('team-summary-pw-input');
  const error = document.getElementById('team-summary-pw-error');
  if (input.value === TEAM_SUMMARY_PW) {
    document.getElementById('team-summary-lock-box').style.display = 'none';
    document.getElementById('load-team-summary-btn').style.display = 'block';
    error.style.display = 'none';
    input.value = '';
  } else {
    error.style.display = 'inline';
    input.value = '';
    input.focus();
  }
}
```

`loadTeamSummary()` 함수 자체(6절)는 변경하지 않는다 — 이 게이트는 그 함수를
"언제 호출 가능하게 만드는가"만 다룰 뿐, 데이터 조회·집계·렌더링 로직과는
무관하다.

### 7.5 검증 결과 (구현 완료, 로컬 브라우저 확인)

- 초기 상태: 잠금 박스 표시, 버튼(`display:none`)은 안 보임 — 확인 완료.
- 오답 입력 → "틀렸습니다" 표시(`display:inline`), 버튼 계속 숨김 — 확인 완료.
- 정답(`7119`) 입력 → 잠금 박스 숨김, 버튼 노출(`display:block`) — 확인 완료.
- 5개 `<script>` 블록 구문 검사(`node -e` + `new Function()`) 전부 통과.
- 콘솔 에러 없음.

## 8. 체크리스트

- [ ] HTML: `tab-setting` 끝에 새 카드 추가(관리자 섹션 밖) — v1
- [ ] JS: `loadTeamSummary()` 함수 추가 — v1 기본 집계 + v2 확장(`basins`/`weeks`) 포함해서 한 번에 작성
- [ ] 구문 검사: `node -e`로 4개 `<script>` 블록 파싱 확인
- [ ] 회귀 테스트(로컬 `python3 -m http.server` + 브라우저 콘솔, `fetch` 몽키패치로 목 서버 시뮬레이션):
  - [ ] 팀 3개, 각각 정상 응답(완료/전체 다른 값) → 팀별 행 + 합계가 정확히 계산되는지 확인
  - [ ] 팀 목록이 빈 배열 → "팀 목록이 없습니다" 안내, 에러 없이 종료
  - [ ] 팀 목록 응답이 비배열(`{}`) → "응답 형식이 올바르지 않습니다" 안내로 위와 다르게 표시되는지 확인
  - [ ] 팀 중 하나만 네트워크 오류(reject) → 그 팀만 "불러오기 실패"로 표시되고 나머지 팀·합계는 정상 표시되는지 확인
  - [ ] 팀 중 하나의 `getPlots` 응답이 `{}`(비배열) → 크래시 없이 그 팀만 0/0으로 표시되는지 확인
  - [ ] 성공/부분실패/완전실패 시 상태 텍스트 색상이 각각 다르게(초록/빨강) 표시되는지, 재실행 시 이전 색이 안 남는지 확인
  - [ ] 팀 이름에 `<img src=x onerror=alert(1)>` 포함된 경우 → escapeHtml로 안전하게 표시되는지 확인
  - [ ] **(v2)** 한 팀에 유역명이 다른 포인트가 여러 개 섞여 있을 때 → 팀명 옆에 고유 유역명이 쉼표로 전부 나열되는지, 중복 유역명은 한 번만 나오는지 확인
  - [ ] **(v2)** 유역명이 전부 빈 값인 팀 → 팀명 옆에 빈 괄호 `()` 없이 라벨 자체가 생략되는지 확인
  - [ ] **(v2)** 여러 주차(예: 1주차 5개 중 3개 완료, 2주차 4개 중 4개 완료, 주차미정 2개 중 0개 완료)가 섞인 팀 → 주차별 행의 "완료/전체"와 "누적 완료/전체"가 정확히 계산되는지, 마지막 주차 행의 누적값이 팀 합계와 일치하는지 확인
  - [ ] **(v2)** 유역명·주차 값에 XSS 문자열이 섞여도 이스케이프되는지 확인
  - [ ] 버튼 연타 시 중복 요청이 안 나가는지(`disabled` 확인)
  - [ ] 실제 배포된 서버로 1회 실행해 실제 팀 수·완료 개수·주차별 값이 설정탭에서 팀별로 수동 확인한 값과 일치하는지 최종 확인
  - [x] **(v3)** 초기 진입 시 잠금 박스만 보이고 버튼(`load-team-summary-btn`)은 숨겨져 있는지 확인
  - [x] **(v3)** 오답 입력 시 "틀렸습니다" 표시, 버튼은 계속 숨김 상태인지 확인
  - [x] **(v3)** 정답(`7119`) 입력 시 잠금 박스가 사라지고 버튼이 노출되는지 확인
  - [x] **(v3)** 힌트 문구("힌트: 집번호4자리")가 항상 표시되는지 확인
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)
