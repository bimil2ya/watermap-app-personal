# watermap-app-personal — 저장목록 "실패" 배지를 "대기 중"과 구분하는 개발지시서

## 1. 배경과 목표

사용자가 실제 현장에서 "저장목록" 탭을 캡처한 두 장의 스크린샷을 근거로, "✗ 실패"
배지의 의미를 물었다. 코드를 확인한 결과 다음이 확정됐다:

- `badge(ok, label)`(`watermap_V100.html:6402-6406`)는 `ok`가 `true`가 아니면
  무조건 "✗ {label} 업로드 실패"를 빨간색으로 표시한다.
- `ok`로 넘어오는 값(`r.soilUploaded`/`r.treeUploaded`/`photoDone`/`allDone`)은
  레코드를 처음 저장한 순간부터 `false`다 — **전송을 실제로 시도했는지 여부와
  전혀 무관하게**, "아직 성공하지 못함"이면 전부 같은 `false`다.
- 즉 지금 배지는 "시도했는데 실제로 오류가 났다"(진짜 실패)와 "아직 전송 순서가
  안 와서 시도조차 안 한 상태"(대기)를 구분하지 못하고 둘 다 "실패"로 표시한다.

사용자가 보낸 두 번째 스크린샷이 이 문제의 실증 사례다: 표준지 1011-0079 카드가
"입지·토양 실패 / 임목조사 실패 / 사진 0/12 실패"를 전부 빨간색으로 보여주는데,
바로 아래 "마지막 오류" 문구가 **하나도 없다**(같은 화면의 1011-0182 카드는 실제
오류가 있어 "마지막 오류: ... Fetch is aborted"가 붙어있는 것과 대조된다). 즉 이
카드는 아무 시도도 없어서 `rec.uploadErrors`가 비어있는데도 "실패"로 잘못
표시된 것이다. 이후 시점(첫 번째 스크린샷)엔 실제로 처리가 진행돼 소일·트리가
성공하고 사진만 일부 진짜로 실패한 상태로 바뀌었다.

**목표**: 업로드 로직(언제 재시도하는지, 언제 오류로 기록하는지 등)은 전혀
건드리지 않고, **이미 쌓여있는 `rec.uploadErrors.soil`/`.tree`/`.photos` 정보를
읽어서 배지 표시만** "완료 / 실패(진짜 오류 있음) / 대기 중(아직 시도 안 함)"
3가지로 분리한다. 순수 렌더링 로직 변경이라 업로드 안정성 자체에는 영향이 없다.

## 2. 범위

**포함**: `renderRecordsList()`(`watermap_V100.html:6377` 이하) 안의 배지 렌더링
로직만.
- `badge(ok, label)` 함수를 `badge(ok, label, hasError)` 3-state로 확장
- 호출부 6곳(전체 탭 3곳: 6436/6437/6440, 입지·토양 탭 1곳: 6492, 임목조사 탭
  1곳: 6527, 사진촬영 탭 합계 1곳: 6567) 전부 `hasError` 인자 추가
- 사진촬영 탭의 개별 파일별 배지(6543-6551, `badge()`를 안 쓰고 인라인으로
  "✓ 완료"/"✗ 실패"를 직접 찍는 부분)도 동일한 3-state 원칙 적용

**명시적 제외**:
- 업로드 시도·재시도·오류 기록 로직(`autoRetryUpload`/`reuploadRecord`/
  `uploadPhotoWithRecovery`/`recordPhotoError` 등) — 전혀 수정하지 않는다.
  이 기능은 이미 `rec.uploadErrors`에 정확한 오류 유무를 기록해두고 있으므로,
  이번 작업은 그 기존 데이터를 배지에 "더 정확하게 반영"하는 것뿐이다.
- "미전송 N건 전송" 버튼 문구(`renderRecordsList()` 상단, 6382-6394번 줄의
  `pendingCount`/"미전송 N건")는 그대로 둔다. 이 문구는 "완료 안 된 것"을
  포괄적으로 세는 용도로 이미 자리 잡았고(실패든 대기든 둘 다 "미전송"에
  해당하는 게 맞는 의미), 이번 변경과 용어가 겹치지 않게 배지 쪽 새 라벨은
  "미전송"이 아니라 **"대기 중"**을 쓴다(용어 혼동 방지).

## 3. 설계

### 3.1 `hasError` 판정 기준 (필드별)

| 필드 | `hasError` 계산 |
|---|---|
| 입지·토양 | `!!r.uploadErrors?.soil` |
| 임목조사 | `!!r.uploadErrors?.tree` |
| 사진 (전체 탭·사진촬영 탭 합계 배지) | 아직 `uploaded`가 아닌 사진들 중, `r.uploadErrors?.photos`에 그 파일명의 기록이 있는 것이 하나라도 있으면 `true` |
| 사진 (사진촬영 탭 개별 파일 행) | `!!r.uploadErrors?.photos?.[p.filename]` |

레코드에 `uploadErrors` 필드 자체가 없는 과거 형식 데이터도 있을 수 있으므로
(2단계 도입 이전 저장분), 전부 `?.`(옵셔널 체이닝)로 안전하게 처리한다 —
`uploadErrors`가 없으면 모든 `hasError`가 자연히 `false`(=대기 중)가 된다.

### 3.2 `badge()` 확장

```js
// 업로드 상태 배지 (3-state: 완료 / 실패(오류 기록 있음) / 대기 중(아직 시도 안 함))
function badge(ok, label, hasError) {
  if (ok) {
    return `<span style="font-size:12px;padding:3px 10px;border-radius:20px;background:#E1F5EE;color:#0F6E56;font-weight:700;">✓ ${label} 업로드 완료</span>`;
  }
  if (hasError) {
    return `<span style="font-size:12px;padding:3px 10px;border-radius:20px;background:#FDECEA;color:#C0392B;font-weight:700;">✗ ${label} 업로드 실패</span>`;
  }
  return `<span style="font-size:12px;padding:3px 10px;border-radius:20px;background:var(--warning-bg);color:var(--warning);font-weight:700;">⏳ ${label} 대기 중</span>`;
}
```

`--warning`/`--warning-bg`(이미 존재하는 CSS 변수, `badge-warn` 클래스와 팀별
누적 현황의 "형식오류" 표기에서 이미 쓰인 색)를 재사용한다 — 새 색상 팔레트를
추가하지 않고, "심각한 오류(빨강)"와 "그냥 아직 안 된 것(경고색)"을 이 앱이 이미
쓰던 관례로 구분한다.

### 3.3 호출부 수정

**전체 탭** (6436-6440번 줄):
```js
${badge(r.soilUploaded, '입지·토양', !!r.uploadErrors?.soil)}
${treeHas ? badge(r.treeUploaded, '임목조사', !!r.uploadErrors?.tree) : '<span ...>임목 미입력</span>'}
${photos.length === 0
  ? '<span ...>사진 없음</span>'
  : badge(photoDone, '사진 ' + photos.filter(p=>p.uploaded).length + '/' + photos.length, photoHasError(r, photos))}
```

**입지·토양 탭** (6492번 줄):
```js
${badge(r.soilUploaded, '입지·토양', !!r.uploadErrors?.soil)}
```

**임목조사 탭** (6527번 줄):
```js
? badge(r.treeUploaded, '임목조사', !!r.uploadErrors?.tree)
```

**사진촬영 탭 합계** (6567번 줄):
```js
: badge(allDone, '사진 ' + doneCnt + '/' + total, photoHasError(r, photos))
```

**공용 헬퍼 `photoHasError(r, photos)`** — 전체 탭과 사진촬영 탭 합계 배지가
똑같은 "미완료 사진 중 오류기록 있는 것이 있는가"를 판정하므로 중복 작성하지
않고 하나로 뺀다(`badge()` 정의 바로 아래, `renderRecordsList()` 내부):
```js
function photoHasError(r, photos) {
  const errs = r.uploadErrors?.photos || {};
  return photos.some(p => !p.uploaded && errs[p.filename]);
}
```
여러 장 중 일부는 정말 실패했고 일부는 아직 시도 전인 "혼합" 상태에서는
"실패"(빨강)로 표시한다 — 대기보다 실패가 더 사용자가 주목해야 할 정보이므로
더 강한 신호를 우선한다는 원칙(그 대신 정확한 완료 개수 "N/M"이 이미 함께
표시되므로 정보 손실은 없다).

### 3.4 사진촬영 탭 개별 파일 행 (6543-6551번 줄)

지금은 `badge()`를 쓰지 않고 인라인으로 2-state만 처리한다. 여기도 동일하게
3-state로 바꾼다:

```js
const photoRows = photos.map(p => {
  const err = r.uploadErrors?.photos?.[p.filename];
  const state = p.uploaded ? 'done' : (err ? 'failed' : 'pending');
  const style = { done: 'background:#E1F5EE;color:#0F6E56;', failed: 'background:#FDECEA;color:#C0392B;', pending: 'background:var(--warning-bg);color:var(--warning);' }[state];
  const label = { done: '✓ 완료', failed: '✗ 실패', pending: '⏳ 대기 중' }[state];
  return `<div style="display:flex;align-items:center;justify-content:space-between;padding:4px 0;border-bottom:0.5px solid var(--gray-bg);">
    <span style="font-size:12px;color:var(--text);flex:1;word-break:break-all;">${escapeHtml(p.filename)}</span>
    <span style="flex-shrink:0;margin-left:8px;font-size:11px;padding:2px 8px;border-radius:20px;font-weight:700;${style}">
      ${label}
    </span>
  </div>`;
}).join('');
```

### 3.5 왜 "미전송"이 아니라 "대기 중"인가

이미 `renderRecordsList()` 상단(6393번 줄)이 "📤 미전송 N건 전송" 버튼 문구에서
"미전송"을 "완료되지 않은 모든 것(실패 포함)"이라는 넓은 뜻으로 쓰고 있다. 새
배지 라벨을 "미전송"으로 하면 "버튼의 미전송 N건"과 "배지의 미전송"이 서로
다른 집합을 가리키게 되어(배지 쪽은 hasError=false인 것만) 혼동을 만든다.
그래서 배지는 "대기 중"이라는 별도 용어를 쓴다.

## 4. 알려진 한계 (문서화하고 넘어감)

- **"대기 중"이 반드시 "아직 한 번도 시도 안 함"을 뜻하지는 않는다.** 예를 들어
  timeout/네트워크 오류처럼 `unconfirmedAttempt:true`인 채로 남아있는 사진은
  아직 `uploadErrors.photos`에 확정 기록이 없을 수 있다(3단계 설계상 애매한
  실패는 재확인 전까지 오류로 확정 기록하지 않는 경우가 있음) — 이 경우도
  "대기 중"으로 보일 수 있다. 이 앱의 기존 원칙(정확한 원인 추적보다 참고용
  정보로 충분)과 일치하므로, 이번 작업에서 별도로 더 세분화하지 않는다.
- 배지 색상만 바뀌는 순수 표시 변경이라, 이미 저장된 레코드의 실제 업로드
  상태(`soilUploaded` 등)에는 영향이 없다 — 새로고침해도 데이터가 달라지지
  않는다.

## 5. 체크리스트

- [x] `badge()` 3-state로 확장 + `photoHasError()` 헬퍼 추가
- [x] 6개 `badge()` 호출부에 `hasError` 인자 추가
- [x] 사진촬영 탭 개별 파일 행 3-state로 교체
- [x] 구문 검사: 5개 `<script>` 블록 파싱 확인 — 통과
- [x] 로컬 브라우저 회귀 테스트(`localStorage`에 시나리오별 레코드를 직접 주입):
  - [x] 방금 저장만 하고 전송 시도 전(모든 `uploaded:false`, `uploadErrors` 없음) → 전 필드 "⏳ 대기 중"(주황)으로 표시, 빨간 "실패"는 하나도 없는지 확인
  - [x] soil 전송을 실제로 시도했다가 실패(`uploadErrors.soil` 채워짐) → soil만 "✗ 실패"(빨강), tree/사진은 아직 대기 상태면 "⏳ 대기 중"으로 구분되는지 확인
  - [x] 사진 12장 중 3장은 실제 오류 기록 있고 9장은 아직 미시도 → 합계 배지가 "실패"(빨강)로 표시되는지(혼합 시 실패 우선 원칙), 사진촬영 탭 개별 행에서는 그 3장만 빨강·나머지 9장은 주황으로 정확히 구분되는지 확인
  - [x] 전부 성공 → 기존과 동일하게 전부 초록 "완료"인지(회귀 없음) 확인
  - [x] `uploadErrors` 필드 자체가 없는 과거 형식 레코드를 수동 주입 → 콘솔 예외 없이 "대기 중"으로 안전하게 처리되는지 확인
  - [ ] 실제 업로드 진행 중(`autoRetryUpload` 목 서버 시나리오)에 배지가 대기→(시도 중 실패 시)실패, 또는 대기→완료로 실시간 전환되는지 확인 — 정적 데이터 주입 테스트로 충분하다고 판단해 생략(배지 로직 자체는 렌더링 시점 값만 읽으므로 실시간 전환 여부와 무관)
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)

## 6. 서브에이전트 검토 결과 (1라운드, 지적사항 없음)

실제 브라우저에서 5개 시나리오를 4개 탭(전체/입지토양/임목조사/사진촬영) 전부에
렌더링해 재검증. `photoHasError`의 혼합 우선 판정, `?.` 옵셔널 체이닝의 전
경로 적용, "알려진 한계"(`unconfirmedAttempt`)의 실제 영향 범위까지 코드
추적으로 확인 — 전부 문제없음. 유일하게 언급된 사항(`--warning-bg`가 다크모드
전용 오버라이드 없음)은 이 앱의 기존 배지·토스트 색상 전반에 이미 있던
관례라 이번 변경이 새로 만든 문제가 아니라고 판단해 지적사항에서 제외됨.
1라운드 만에 지적사항 없음으로 수렴 — 반복 종료.

## 7. Codex 교차 검토 결과

차단 사유 없음. 확인된 사항:
- `ok`를 최우선, 그다음 `hasError`만 실패로 보는 판정 순서 적절. 성공 시
  오류 기록을 지우는 경로도 확인됨. 과거 데이터(`uploadErrors` 없음) 안전.
- 사진은 `uploaded:false`인 것만 오류 기록과 대조하므로, 이미 완료된 사진에
  남아있을 수 있는 오래된 오류가 합계 배지를 오염시키지 않음. 개별 행과
  합계/전체 탭 판정도 일관됨.
- "혼합 시 실패 우선"은 재시도 대상을 놓치지 않는 보수적 UX로 적절. 다만
  예: 12장 중 3장만 실패해도 배지 텍스트가 실패처럼 빠르게 읽힐 수 있어,
  향후 "실패 3 · 대기 0"처럼 개수를 병기하는 방향도 고려해볼 만하다고
  제안 — **이번 변경에는 포함하지 않음(Codex도 필수 아니라고 명시)**.
- **주석 정정 반영**: "대기 중(아직 시도 안 함)"이라는 코드 주석이 실제
  구현("오류 기록 없음")보다 의미를 과도하게 단정한다는 지적을 받아,
  `badge()` 정의부 주석을 "오류 기록 없음 — 아직 시도 전이거나
  unconfirmedAttempt처럼 결과 미확정인 경우도 포함"으로 정정함.
- **범위 밖 발견 사항(별도 태스크로 분리)**: 전체 탭의 "마지막 오류" 표시
  영역이 완료된 항목에 남은 과거 오류 기록까지 보여줄 가능성(레거시/오프라인
  큐 경로)이 있다고 지적 — 배지 판정에는 영향 없지만 초록 완료 배지와 빨간
  마지막 오류 문구가 함께 보이면 혼란스러울 수 있음. 이번 배지 작업 범위
  밖이라 별도 확인 작업으로 분리함(`task_28cbce7c`).
