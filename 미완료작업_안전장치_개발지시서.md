# 미완료 작업(draft) 안전장치 개발지시서

## 1. 배경과 목표

### 1.1 지키려는 사용 흐름

현장 조사원이 다음처럼 일할 수 있어야 한다.

1. 5번 포인트에서 정보를 입력하다가 한두 항목을 남긴 채 중단한다.
2. 6번 포인트로 넘어가 작업을 마치고 저장한다.
3. 며칠 뒤 앱을 다시 열어 5번 포인트를 불러와 남은 항목을 채우고 저장한다.

이 흐름 자체는 **현재 코드에서 이미 동작한다**(실제 브라우저에서 재현 검증 완료). 이 문서의 목적은 새 기능을 만드는 것이 아니라, **이 흐름이 어떤 상황에서도 데이터를 잃지 않고 작동하도록 방어층을 쌓는 것**이다.

### 1.2 현재 동작 요약 (검증 완료된 사실)

| 항목 | 현재 동작 | 코드 위치 |
|---|---|---|
| draft 키 | `survey_draft_<조사지번호>` — 포인트별로 독립 | `DRAFT_KEY_PREFIX` 1910행, `getDraftKey()` 1911행 |
| 저장 트리거 | ① **필수 입력 항목**(`REQUIRED_SOIL`+`REQUIRED_TREE`)을 편집할 때마다 1초 디바운스(1차 검토에서 정정 — 모든 입력 필드가 아니라 이 두 목록에만 리스너가 걸려 있음, 3092~3093행) ② 포인트 전환 직전 명시적 호출 | `scheduleDraftSave()` 3034행, `initFieldClearListeners()` 3077행, `onPlotSelect()`의 `saveDraft()` 6049행 |
| 저장 대상 | `input[type=text] / [type=number] / textarea / select` + 체크된 라디오 + 사진 파일명 목록 | `saveDraft()` 2060~2114행 |
| **저장 제외** | **`input[type=hidden]`은 선택자에 없음 → `soil-date`(조사일)는 draft에 저장되지 않음** | 2064행 선택자 |
| 앱 시작 복원 | 전체 draft 중 `_savedAt`이 가장 큰 **1개만** 자동 복원 | 2561~2573행 |
| 포인트 선택 복원 | 정식 레코드가 있으면 그것을 우선, 없을 때만 draft 복원 | `onPlotSelect()` 6084~6165행 |
| 사진 복원 | IndexedDB 원본(`idbOrigKey`) 우선, 없으면 `photo_N` 보조 캐시 + captureId 일치 시에만 | `restoreDraft()` 2162~2239행 |
| 초기화 시 draft | `_doReset()`이 폼을 먼저 비우므로(6656~6659행) 뒤이은 `clearDraft()`(6671행)는 `survey_draft__default`만 지움 — 포인트별 draft는 안전 | `_doReset()` 6645행 |
| 저장 완료 시 | `localStorage.setItem('survey_records', ...)` 성공 후 `clearDraft()`로 해당 포인트 draft 제거 | `saveData()` 4181·4231행 |

### 1.3 목표

아래 8가지 위험을 계층적 방어(defense in depth)로 덮는다. 각 층은 단독으로도 가치가 있고, 동시에 다른 층의 사각지대를 메운다.

---

## 2. 범위

**포함**: `watermap_V100.html` 한 파일. draft 저장/복원/표시/정리 로직과 저장목록 탭 UI.

**포함하지 않음**:
- 서버(Apps Script) 코드 변경 — 공유 백엔드는 건드리지 않는다는 원칙 유지.
- **조사일(`soil-date`) 값을 실제 작업일과 다르게 조작하는 기능** — 이 문서의 어떤 항목도 조사일을 사실과 다르게 기록하지 않는다. 6겹은 "언제 시작했는지"를 **추가로 보존**할 뿐, 서버로 전송되는 조사일 값에는 영향을 주지 않는다.
- 빌드 시스템·프레임워크 도입.
- `dashboard.html` 변경.

---

## 3. 위험 목록

| # | 위험 | 근거 |
|---|---|---|
| **A** | 어느 포인트를 중단했는지 잊어버림 | 미완료 draft 목록 UI가 없음. 앱 시작 시 자동 복원은 최신 1개뿐(2561~2573행) |
| **B** | 마지막 입력 1초가 유실 | `scheduleDraftSave()` 1초 디바운스(3034행). 그 안에 앱이 종료되면 미저장 |
| **C** | 저장소 자체가 유실 | draft는 localStorage 단일 저장. iOS Safari 장기 미사용 정리·저장공간 압박 시 evict 대상 |
| **D** | 저장 후 수정하다 중단한 내용이 묻힘 | `onPlotSelect()`는 정식 레코드가 있으면 draft를 시도조차 안 함(6084~6088행) |
| **E** | 빈 폼이 기존 draft를 덮어씀 | 내용 비교 없이 무조건 `setItem`(2088행) |
| **F** | 사진만 유실된 채 복원 | draft엔 파일명만, 실제 이미지는 IDB. IDB 정리 시 빈 슬롯으로 복원되고 저장 단계에서야 막힘 |
| **G** | 기기 교체 시 전부 소실 | draft는 해당 기기 브라우저에만 존재 |
| **H** | 완료된 포인트의 draft 축적 | 축적 → 저장소 압박 → C 악화 |

---

## 4. 설계 — 안전장치 8겹

### 4.1 [1겹] 미완료 작업 목록 (가시성) — 1차

**의도**: 저장목록 탭 최상단에 "작성 중 N건" 카드를 추가한다. 각 행에 조사지번호, 최초 작성일·최종 수정일, 입력 완성도(필수항목 n/m, 사진 n/12), 무결성 경고를 표시하고, 누르면 그 포인트로 이동한다.

**배치**: `tab-list`(1026행) 안, `send-all-bar`(1029행) **위**에 새 카드. 저장목록 렌더링 함수(`renderRecordsList()` 7006행)가 호출될 때 함께 갱신한다.

**완성도 계산 규칙** — draft 객체만으로 계산한다. `soil-date`는 draft에 없으므로(1.2 참고) 필수항목 카운트에서 제외한다.

```js
// draft 하나의 요약 정보를 만든다. 실제 폼을 건드리지 않고 draft 객체만 읽는다.
function summarizeDraft(plotNo, draft) {
  const requiredIds = [...REQUIRED_SOIL, ...REQUIRED_TREE]
    .map(f => f.id)
    .filter(id => id !== 'soil-date'); // hidden이라 draft에 애초에 없음 — 분모에서 제외
  const filled = requiredIds.filter(id => (draft[id] || '').toString().trim() !== '').length;
  const photoCount = (draft._photoKeys || []).length;
  const movePhotoCount = (draft._movePhotoKeys || []).length;
  // 실제 폼(594행 근처)에는 hidden input(id="soil-move-need")과 라디오 그룹(name="soil-move-need")이
  // 공존하지만, saveDraft()의 선택자(2064행)는 hidden을 제외하므로 draft['soil-move-need']는
  // 실제로는 절대 채워지지 않고 draft['radio_soil-move-need']만 채워진다(1차 검토에서 확인).
  // 그래도 두 키를 OR로 확인해두면 향후 폼 구조가 바뀌어도 안전하므로 그대로 둔다.
  const moveNeeded = draft['soil-move-need'] === '조사지이동'
    || draft['radio_soil-move-need'] === '조사지이동';
  return {
    plotNo,
    requiredFilled: filled,
    requiredTotal: requiredIds.length,
    photoCount, movePhotoCount, moveNeeded,
    firstEditedAt: draft._firstEditedAt || draft._savedAt || null, // 6겹에서 채움
    lastEditedAt: draft._savedAt || null,
  };
}
```

**목록 수집**: `localStorage`의 `survey_draft_` 접두사 키를 모두 훑되, 다음은 제외한다.
- `survey_draft__default` (번호가 없는 임시 슬롯)
- 해당 조사지번호가 이미 `survey_records`에 **정식 저장되어 있는 경우** — 시각을 비교하지 않고 무조건 제외한다. *(1~2차 한정 규칙. 3차부터는 아래 설명대로 `savedAtMs` 기준으로 정밀화된다 — §4.6 참고.)*

**1차 검토에서 정정된 사항**: 원래 "draft가 레코드보다 오래된 경우만" 제외하려 했으나, 그러려면 draft의 `_savedAt`(숫자)과 레코드의 저장 시각을 비교해야 하는데, 레코드의 `savedAt`(4181행)은 `new Date().toLocaleString('ko-KR')`로 만든 **로케일 문자열**이라 안정적으로 파싱할 수 없다. 이 문제를 해결할 숫자 타임스탬프(`savedAtMs`)는 §4.6(5겹)에서 3차에 가서야 추가된다.

**1차 임시 처리(3차에서 반드시 갱신됨)**: 1차 시점에는 "레코드보다 최신인 draft"를 구분할 방법이 없으므로, 1차 목록은 **판단을 미루고 단순하게 처리**한다 — 정식 레코드가 하나라도 있으면(최신 여부와 무관하게) 그 포인트는 일단 목록에서 뺀다. 이건 어디까지나 1~2차 구간의 **임시 단순화**이며, §4.6이 `savedAtMs`를 도입하는 3차에 반드시 이 제외 조건도 함께 갱신한다(구현 항목은 §6.3에 명시). 3차 이후에는 "정식 레코드는 있지만 그보다 최신인 draft가 있는 경우"는 다시 목록에 나타나고, 이게 §5 대응표가 명시한 대로 **위험 D의 3차 방어선**(1겹 목록 노출) 역할을 한다 — 5겹(선택 UI)이 "그 포인트를 열었을 때" 대응한다면, 1겹은 "그 포인트가 있다는 사실 자체"를 미리 알려주는 역할로 서로 보완한다.

**장점**: 위험 A를 정면 해결. 사용자가 아무것도 기억하지 않아도 된다. 완성도가 보여 "무엇이 남았는지"도 즉시 파악된다.
**단점**: draft가 이미 유실된 뒤면 목록도 비어 무력하다. 화면 요소가 늘어난다.
**상호보완**: 이 겹은 2·4·6겹이 만든 정보를 사용자에게 보여주는 **창구**다. 2겹이 데이터를 지키고, 4겹이 무결성 상태를, 6겹이 시각 정보를 제공하면 모두 이 목록 한 줄에 모인다. 반대로 이 겹이 없으면 다른 겹이 아무리 잘 지켜도 사용자가 찾지 못한다.

---

### 4.2 [2겹-A] 이탈 시점 즉시 저장 (flush) — 1차

**의도**: 1초 디바운스를 기다리지 않고, 화면을 벗어나는 순간 확정 저장한다.

```js
// 앱이 백그라운드로 가거나 페이지가 사라질 때 대기 중인 draft를 즉시 확정한다.
// beforeunload는 모바일(특히 iOS)에서 발화가 보장되지 않으므로 쓰지 않는다.
function flushDraftNow() {
  clearTimeout(_draftTimer);
  _draftTimer = null;
  try { saveDraft(); } catch (e) { console.warn('draft flush 실패', e); }
}
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'hidden') flushDraftNow();
});
window.addEventListener('pagehide', flushDraftNow);
```

**주의**: `saveDraft()`는 현재 `soil-plot-no`의 값으로 키를 결정한다(`getDraftKey()` 1911행). flush 시점에 폼이 비어 있으면 `survey_draft__default`에 빈 내용이 쓰일 수 있으므로, **반드시 3겹(4.3)과 함께 적용해야 한다.**

**장점**: 위험 B 해결. 배터리 소진·강제 종료·앱 전환에도 마지막 입력까지 남는다.
**단점**: 저장 호출 빈도가 늘어난다(단, 이탈 시점에만이라 미미).
**상호보완**: 3겹이 없으면 이 겹이 오히려 빈 내용을 덮어쓰는 사고를 늘린다 — **반드시 짝으로 간다.**

---

### 4.3 [3겹] 덮어쓰기 방지 가드 — 1차

**의도**: draft를 쓰기 전에 "지금 쓰려는 내용이 기존 draft보다 빈약한가"를 확인하고, 빈약하면 쓰지 않는다.

```js
// draft 안에서 '실제로 값이 있는 항목'의 개수 — 빈약함 판정의 유일한 기준.
// 사진은 draft가 아니라 IndexedDB에 있으므로 여기서는 파일명 목록 길이만 센다.
function draftContentScore(draft) {
  if (!draft || typeof draft !== 'object') return -1;
  let score = 0;
  Object.entries(draft).forEach(([k, v]) => {
    if (k.startsWith('_')) return;               // 메타데이터 제외
    if (typeof v !== 'string') return;
    if (v.trim() !== '') score++;
  });
  score += (draft._photoKeys || []).length;
  score += (draft._movePhotoKeys || []).length;
  return score;
}

// saveDraft() 내부, localStorage.setItem 직전에 삽입한다.
function shouldWriteDraft(key, nextDraft) {
  let prev = null;
  try { prev = JSON.parse(localStorage.getItem(key) || 'null'); } catch (e) { prev = null; }
  if (!prev) return true;                        // 기존 것이 없으면 무조건 쓴다
  const prevScore = draftContentScore(prev);
  const nextScore = draftContentScore(nextDraft);
  if (nextScore >= prevScore) return true;       // 같거나 늘었으면 정상 저장
  return false;                                  // 줄었으면 덮어쓰지 않는다(삭제는 명시적 경로로만)
}
```

**명시적 삭제 경로**: 사용자가 의도적으로 비우려면 7겹(4.7)의 "이 포인트 작성 취소" 동작을 쓴다. 그 경로는 `localStorage.removeItem()`을 직접 호출하므로 이 가드의 영향을 받지 않는다.

**기존 흐름 보존 확인**: `_doReset()`의 `clearDraft()`(6671행)는 `removeItem`이라 이 가드를 거치지 않는다. 즉 **현재의 `_default` 정리 동작은 그대로 유지된다.**

**장점**: 위험 E를 구조적으로 차단.
**단점**: 의도적 축소가 막힌다 → 7겹의 명시적 삭제로 해소.
**상호보완**: 2겹과 필수 짝. 또한 2겹-B(이중 저장, 4.4)의 "어느 쪽이 최신인가" 판정에 같은 `draftContentScore`를 재사용해 판단 기준을 하나로 통일한다.

---

### 4.4 [2겹-B] 이중 저장소 미러링 — 4차

**의도**: draft를 localStorage와 IndexedDB 양쪽에 쓰고, 복원 시 `_savedAt`이 더 최신이면서 `draftContentScore`가 더 큰 쪽을 채택한다. IDB 키는 `draft_<조사지번호>` 형식으로 기존 사진 키(`orig_`, `photo_`)와 충돌하지 않게 한다.

**장점**: 위험 C 완화. 한쪽 저장소가 정리돼도 다른 쪽에서 복구된다.
**단점**: 저장소 사용량이 두 배. 두 저장소 불일치 가능성.
**상호보완**: 불일치는 3겹의 점수 비교 로직 재사용으로 해결. 용량 증가는 7겹(수명 관리)이 상쇄한다 — **2겹-B와 7겹도 반드시 짝으로 간다.**

#### 4.4.1 상세 설계 (4차 착수용 · **4차 검토 반영판 — 구현 착수 승인**)

**B-0. 이 겹이 막는 것과 막지 못하는 것 — 쓰기와 삭제를 분리해서 본다**

`saveDraft()`는 **동기 함수**인데 `idbSet()`은 **비동기**다(1789행). 따라서 미러 조작은 `.catch()`를 단 fire-and-forget이 될 수밖에 없다. 그런데 **쓰기 유실과 삭제 유실은 피해의 종류가 완전히 다르다.** 초판은 이 둘을 같은 한계로 묶었는데, 1차 검토에서 그것이 사실 오류로 확인됐다.

| 방향 | 유실되면 | 피해 |
|---|---|---|
| 미러 **쓰기** 유실 | 미러가 낡은 채 남는다 | B-4의 "최신 우선"에서 지고 버려진다 → **무해** |
| 미러 **삭제** 유실 | 지운 draft가 미러에 남는다 | 다음 실행에서 **되살아난다** → **파괴적** |

따라서 삭제는 fire-and-forget으로 두면 안 된다 — B-3의 묘비(tombstone) 방식으로 해결한다.

- **막아주는 것**: iOS Safari의 장기 미사용 저장소 정리, localStorage만 손상·초과된 경우 — 즉 **"localStorage는 잃었지만 앱은 다음에 정상 실행되는"** 상황(위험 C의 주된 형태).
- **막지 못하는 것**: `pagehide` 직후의 즉시 종료. 그 시점에 시작한 IDB 트랜잭션은 대개 완료되지 못한다. 이 구간은 2겹-A(동기 localStorage 확정 저장)가 계속 담당한다.
- 따라서 **2겹-A를 2겹-B로 대체하지 않는다. 둘은 서로 다른 사고를 막는다.**

**B-1. 키, 충돌 검증, 그리고 없는 기능 하나**

미러 키는 `draft_<조사지번호>`. 기존 IDB 키 접두사는 `thumb_`·`thumb_move_`·`view_`·`view_move_`·`orig_`·`orig_move_`(1850~1855행)와 전역 보조 캐시 `photo_N`·`move_photo_N`뿐이라 `draft_`는 어느 것과도 겹치지 않는다. `createObjectStore('photos', { keyPath: 'key' })`(1782행)는 이름만 사진용일 뿐 범용 키-값 스토어이므로 스키마 버전 업이 필요 없다. (1차 검토에서 전수 확인됨.)

**그런데 IDB 계층에 키를 열거하는 수단이 없다.** `idbSet`/`idbGet`/`idbDelete`/`idbClear`(1789~1824행)가 전부이고 `getAllKeys`도 커서도 없다. 이대로면 **localStorage가 통째로 정리된 상황에서 어떤 키를 조회해야 하는지 알 방법이 없어**, 이 겹이 막겠다고 선언한 바로 그 상황에서 아무것도 못 한다. 다음을 신설한다.

```js
// IDB의 키 목록을 접두사로 걸러 돌려준다. 2겹-B의 병합이 이것 없이는 성립하지 않는다.
async function idbKeys(prefix) {
  const db = await openIDB();
  return new Promise((resolve, reject) => {
    const req = db.transaction('photos', 'readonly').objectStore('photos').getAllKeys();
    req.onsuccess = e => resolve((e.target.result || []).filter(k => String(k).startsWith(prefix)));
    req.onerror   = e => reject(e.target.error);
  });
}
```

**주의**: `idbClear()`(1816행)는 스토어 전체를 비운다. 현재 호출부가 없지만(전수 확인 — 6671행 근처 주석의 언급은 실제 호출이 아니다), 누군가 "사진 초기화" 용도로 추가하면 draft 미러까지 날아간다. 4차에서 `idbClear()` 위에 경고 주석을 남기고, 사진만 지우는 용도가 필요하면 `idbKeys()` 기반 선택 삭제를 쓰도록 지시한다.

**B-2. 쓰기 — 위치를 한 곳으로 못 박는다**

```js
// 절대 throw하지 않고, 절대 기다리지 않는다 — 미러 실패가 본 저장을 망치면 안 된다.
function mirrorDraftToIDB(draftKey, payload) {
  const plotNo = draftKey.slice(DRAFT_KEY_PREFIX.length);
  idbSet('draft_' + plotNo, payload).catch(e => console.warn('draft 미러 실패', e));
}
```

- **호출 위치**: `saveDraft()`의 `if (shouldWriteDraft(...))` 블록 **맨 끝, try/catch 바깥**에서 한 번만 부른다. "setItem 직후"라고만 적으면 구현자가 첫 번째 `setItem` 아래에 넣게 되는데, 그러면 **quota 재시도 경로(`evictOldestTrashedDraft()` 후의 두 번째 `setItem`)에서 미러가 누락된다** — 저장공간이 빠듯한 기기, 즉 이 겹이 가장 필요한 기기에서만 미러가 조용히 낡는다(1차 검토 지적).
- `payload`는 localStorage에 넣은 **바로 그 문자열**을 그대로 쓴다. 객체를 다시 만들면 두 저장소가 미묘하게 갈린다.
- `_default` 슬롯도 그대로 미러한다 — §7이 기록했듯 번호를 고르기 전 입력이 쌓이는 실제 작업 슬롯이다.

**B-3. 삭제 — `idbDelete`를 쓰지 않고 묘비를 남긴다 (이 겹의 최대 위험)**

미러가 남아 있으면 **지운 draft가 되살아난다.** 그런데 미러 삭제도 비동기라, 커밋 전에 앱이 죽으면 삭제가 유실된다. 특히 `_doReset()` → `clearDraft()`(6671행)는 **포인트를 고를 때마다** 돌아 `survey_draft__default`를 지우므로, 현장에서 가장 잦은 IDB 쓰기가 된다. 저장 직후의 `clearDraft()`(4231행)도 "저장 완료를 보고 앱을 닫는" 정상 흐름과 겹친다.

→ **삭제도 `idbSet`으로 통일한다.** 본문 대신 묘비를 쓴다.

```js
// 삭제를 '쓰기'로 표현한다. 쓰기 한 종류만 남으므로 순서가 자연히 보장되고,
// 커밋되지 못해도 '낡은 묘비'가 아니라 '낡은 본문'이 남을 뿐이라 _savedAt 비교로 정상 처리된다.
// 인자는 plotNo가 아니라 '실제로 지운 localStorage 키'다.
// trashDraft()는 후보 두 개(`DRAFT_KEY_PREFIX + plotNo` / `getDraftKey()`) 중 존재하는
// 쪽을 지우는데, getDraftKey()는 trim을 하지 않아(1911~1913행) 둘이 갈릴 수 있다.
// plotNo로 다시 조립하면 공백이 섞인 경우 '지운 쪽의 미러가 그대로 살아남아' 부활한다
// — §4.6.1 A-1이 이미 경고한 실패 유형이다. mirrorDraftToIDB()와 인자 규약을 맞춘다.
function tombstoneDraftInIDB(draftKey) {
  const plotNo = draftKey.slice(DRAFT_KEY_PREFIX.length);
  idbSet('draft_' + plotNo, JSON.stringify({ _deletedAt: Date.now() }))
    .catch(e => console.warn('draft 묘비 기록 실패', e));
}
```

localStorage에서 draft가 사라지는 경로는 전수 확인 결과 아래 넷뿐이다(`removeItem` 3곳 + 되돌리기 1곳, `localStorage.clear()`는 존재하지 않음).

**세 경로 모두 묘비의 인자는 `plotNo`가 아니라 실제로 지운 키다.**

| 경로 | 위치 | 처리 |
|---|---|---|
| `clearDraft()` | 2377행 (호출부 4231·6671행) | 묘비 |
| 1겹 목록의 "작성 취소" | 6959행 근처 | 묘비 |
| `trashDraft()`의 원본 제거 | §4.6.1 A-1 | 묘비 (보관본은 미러하지 않는다 — B-5) |
| `restoreTrashedDraft()` | §4.6.1 A-1 | 되살린 본문을 미러에 **다시 쓴다**(`mirrorDraftToIDB`) |

**묘비 정리**는 7겹 수명 관리에 넣는다 — `_deletedAt`가 TTL(유예 보관과 같은 14일)을 넘으면 그때 `idbDelete`로 실제 제거한다. 이 시점엔 이미 오래 전에 커밋된 묘비이므로 삭제가 유실돼도 무해하다.

**B-4. 읽기와 병합 — "최신 우선 + 이상징후 경고" (사용자 확정 규칙)**

```js
// 반환: { pick: 'local' | 'idb' | 'ask' | 'none', local, idb }
function chooseDraftSource(localRaw, idbRaw) {
  const parse = (raw) => { try { return raw ? JSON.parse(raw) : null; } catch (e) { return null; } };
  const L = parse(localRaw), I = parse(idbRaw);

  // 둘 다 못 읽었다 = 판단 불가. 호출부는 '아무것도 하지 않는다'
  // (기존 localStorage 원문을 지우지 않는다 — 3차의 _rawUnparsed 본문이 여기로 온다).
  if (!L && !I) return { pick: 'none', local: null, idb: null };

  // 묘비 처리: 묘비보다 최신인 local이 없으면 '삭제된 것'이 맞다.
  if (I && I._deletedAt) {
    if (L && (L._savedAt || 0) > I._deletedAt) return { pick: 'local', local: L, idb: I };
    return { pick: 'none', local: L, idb: I };
  }

  // _savedAt이 없는 본문은 restoreDraft()가 2122행에서 곧바로 탈락시키므로 채택하지 않는다.
  const usableL = L && L._savedAt ? L : null;
  const usableI = I && I._savedAt ? I : null;
  if (!usableL && !usableI) return { pick: 'none', local: L, idb: I };
  if (!usableI) return { pick: 'local', local: L, idb: I };
  if (!usableL) return { pick: 'idb',   local: L, idb: I };

  const lt = usableL._savedAt, it = usableI._savedAt;
  if (lt === it) return { pick: 'local', local: L, idb: I }; // 같은 시각 = 같은 내용

  const newerIsLocal = lt > it;
  const newer = newerIsLocal ? usableL : usableI;
  const older = newerIsLocal ? usableI : usableL;

  // 이상징후: 최신 쪽이 비정상적으로 비어 있으면 자동 채택하지 않는다.
  // "지워서 줄어든 것"과 "쓰다 만 기록"은 내용만으로 구분할 수 없으므로,
  // 정상적인 삭제로는 잘 나오지 않는 폭일 때만 사용자에게 묻는다.
  const ns = draftContentScore(newer), os = draftContentScore(older);
  const suspicious = (ns === 0 && os > 0) || (os > 0 && ns < os * 0.5);
  if (suspicious) return { pick: 'ask', local: L, idb: I };

  return { pick: newerIsLocal ? 'local' : 'idb', local: L, idb: I };
}
```

- **기본은 최신 우선**이다. 사용자의 마지막 행동이 진실이고, 항목이 줄어든 건 대개 "지웠기 때문"이다.
- **0.5 문턱은 임의값이다.** 4차 검증에서 "사진 여러 장 삭제", "임목 행 다수 삭제" 같은 정상 삭제가 이 문턱에 걸리는지 실측하고, 걸리면 낮춘다. 잘못된 경고는 사용자를 불필요한 선택에 묶어둔다.
- **`pick: 'ask'`는 UI를 띄우지 않는다.** 이 병합이 도는 유일한 시점은 앱 기동 중(B-4-1)이고, 기동 중에는 절대 확인창을 띄우지 않는다(B-4-3 참고 — 3차 검토에서 `confirm()` 지시를 철회하고 이 방식으로 확정했다). 최신 쪽을 자동 채택하고 탈락한 쪽은 유예 보관소에 남긴다.

**B-4-1. 병합은 "앱 시작 시 1회, 전 키 대상"으로만 한다 (핵심 결정)**

병합을 어디서 부르느냐가 이 겹의 성패를 가른다. 1차 검토가 지적한 두 가지 때문이다.

1. `restoreDraft()`(2116행)는 **자기가 직접 localStorage를 다시 읽고** `_savedAt`이 없으면 `false`를 반환한다. 병합 결과를 객체로 넘길 방법이 없다.
2. `onPlotSelect()`의 5겹 분기(6086행)와 `trashDraft()`의 키 탐색(1981행)은 **localStorage만 본다.** 미러만 남은 상태에서 정식 레코드가 있는 포인트를 열면, 선택 UI가 뜨지 않고 레코드가 폼을 덮은 뒤 메아리가 미러까지 덮어써 **유일하게 남아 있던 편집분이 그 자리에서 소멸한다.** 이 둘은 동기 코드라 `await`를 끼워 넣으면 `confirm()`·`_doReset()` 순서가 깨진다.

→ 따라서 **앱 시작 시 딱 한 번, 미러 전용 draft를 localStorage로 승격시켜 둔다.** 그 뒤로는 기존 동기 코드가 손대지 않아도 전부 localStorage만 보면 되고, 3차 로직과의 충돌면이 사라진다.

```js
// 앱 시작 초기화에서 purgeExpiredTrashedDrafts() 직후, draft 자동복원 '전'에 부른다.
// 자동복원(2561행)과 1겹 목록(6834행)이 모두 Object.keys(localStorage)만 훑으므로,
// 그 전에 승격이 끝나 있어야 미러 전용 draft가 화면에 나타난다.
async function mergeDraftMirrors() {
  let keys = [];
  try { keys = await idbKeys('draft_'); } catch (e) { console.warn('미러 키 조회 실패', e); return; }
  const plotNos = new Set([
    ...keys.map(k => k.slice('draft_'.length)),
    ...Object.keys(localStorage).filter(k => k.startsWith(DRAFT_KEY_PREFIX))
                                .map(k => k.slice(DRAFT_KEY_PREFIX.length)),
  ]);
  for (const plotNo of plotNos) {
    const lsKey = DRAFT_KEY_PREFIX + plotNo;
    let idbRaw = null;
    try { idbRaw = await idbGet('draft_' + plotNo); } catch (e) { continue; }
    const r = chooseDraftSource(localStorage.getItem(lsKey), idbRaw);
    // 'ask'도 기동 중에는 묻지 않고 최신 쪽을 채택한다(B-4-3). 탈락한 쪽은 유예 보관소로.
    const adopt = (r.pick === 'ask')
      ? ((r.local?._savedAt || 0) >= (r.idb?._savedAt || 0) ? 'local' : 'idb')
      : r.pick;
    const loserRaw = r.pick === 'ask'
      ? (adopt === 'local' ? idbRaw : localStorage.getItem(lsKey))
      : null;

    // stash는 반드시 채택 쓰기가 '성공'한 뒤에 한다. 먼저 하면 쓰기가 실패했을 때
    // 어긋남이 그대로 남아 다음 기동에도 같은 divergence로 다시 stash되고,
    // 그때마다 pushTrashSlot()의 2슬롯이 같은 내용의 중복본으로 채워져
    // 5겹에서 만든 진짜 보관본을 밀어낸다(3차 검토 지적).
    if (adopt === 'idb') {
      try { localStorage.setItem(lsKey, idbRaw); }
      catch (e) { console.warn('승격 실패', plotNo, e); continue; }   // 이 키만 건너뛴다
      if (loserRaw) stashLoserToTrash(plotNo, loserRaw, lsKey);
    } else if (adopt === 'local') {
      // mirrorDraftToIDB()는 fire-and-forget이라 커밋 여부를 알 수 없다. 'ask'가 나온
      // 경우(드물다)에 한해서만 커밋을 확인한 뒤 stash한다 — 매 기동 도는 일반 쓰기까지
      // 여기서 await하면 병합 전체가 느려진다.
      if (loserRaw) {
        try { await idbSet('draft_' + plotNo, localStorage.getItem(lsKey)); stashLoserToTrash(plotNo, loserRaw, lsKey); }
        catch (e) { console.warn('미러 갱신 실패 — stash 보류', plotNo, e); }
      } else {
        mirrorDraftToIDB(lsKey, localStorage.getItem(lsKey));
      }
    } else {
      // pick === 'none': 기존 원문을 지우지 않는다. 다만 살아 있는 본문이 있으면
      // 묘비를 본문으로 덮어 미러 보호가 끊기지 않게 한다(B-4-4).
      const raw = localStorage.getItem(lsKey);
      let d = null; try { d = raw ? JSON.parse(raw) : null; } catch (e) {}
      if (d && d._savedAt) mirrorDraftToIDB(lsKey, raw);
    }
  }
}
```

**B-4-2. 호출 규약 — 앱 기동을 IDB에 묶지 않는다 (2차 검토 반영)**

"자동복원 전에 부른다"만 적으면 **그대로는 동작하지 않는다.** 기동 핸들러가 동기이기 때문이다.

[근거] `document.addEventListener('DOMContentLoaded', () => {`(2541행)는 `async`가 아니다. 그 안 2558행에 `mergeDraftMirrors();`를 넣으면 첫 `await idbKeys()`에서 즉시 양보하고, 2561행 이하가 **승격 전 localStorage**를 훑는다. §6.4의 첫 두 항목이 겨냥하는 기능이 통째로 동작하지 않는다.

그렇다고 핸들러를 `async`로 바꿔 `await`하면 **더 나쁜 일**이 생긴다. 지금 앱 기동은 IDB에 전혀 의존하지 않는다(기동 경로의 IDB 접근은 `restoreDraft()` 안뿐이고 실패는 2238행 `catch`가 삼킨다). `await`를 넣는 순간 기동이 처음으로 IDB에 묶이는데, `openIDB()`(1775~1788행)에는 **`onblocked` 핸들러가 없어** 차단되면 Promise가 resolve도 reject도 되지 않는다 → `try/catch`로도 못 막고, 결과는 draft 복원 실패가 아니라 **포인트 목록·온라인 상태·팀코드가 전부 안 뜨는 백지 앱**이다. 같은 앱을 두 탭으로 열어두는 것만으로 재현된다.

→ **확정 규약: 기동 핸들러는 동기로 두고, 자동복원 블록만 뒤로 미룬다.**

```js
// 1) openIDB()에 안전판 한 줄을 먼저 추가한다(B-1에 포함).
req.onblocked = () => reject(new Error('IDB blocked'));

// 2) 기동 핸들러(2541행)는 async로 바꾸지 않는다.
//    2561~2578행의 '가장 최근 draft 자동복원' 블록만 아래 형태로 감싼다.
//    - 승격이 끝나면 곧바로, 늦어지면 3초 뒤에 자동복원이 반드시 실행된다.
//    - 그 외 기동 코드(updateOnlineStatus 2579행, 팀코드·loadPlotPoints 2583행 이하)는
//      지금처럼 동기로 즉시 실행된다 — IDB가 죽어도 앱은 정상 기동한다.
Promise.race([
  mergeDraftMirrors(),
  new Promise(r => setTimeout(r, 3000)),
]).catch(e => console.warn('미러 병합 실패', e))
  .finally(() => { /* 기존 2561~2578행 자동복원 블록 */ });
```

승격이 3초 안에 못 끝나도 **손실이 아니다** — 미러는 그대로 있고 다음 실행에서 다시 시도된다. 그 실행에서 자동복원이 미러 전용 draft를 못 집을 뿐이며, 1겹 목록은 이후 렌더 시점에 이미 승격이 끝나 있어 정상적으로 보인다.

**B-4-3. 기동 중에는 절대 묻지 않는다 — `ask`의 처리 (2차 검토 반영)**

`mergeDraftMirrors()`는 전 키를 도는 루프다. `confirm()`을 그 안에 두면 **어긋난 키 개수만큼 앱을 켜자마자 확인창이 뜬다.** 그 시점은 폼도 안 채워진 상태라 사용자에게 아무 맥락이 없고, 홈 화면에서 실행되는 PWA다. 게다가 사용자 제스처 없는 기동 직후의 `confirm()`은 브라우저가 자동 거부할 수 있는데 **자동 거부와 '취소'를 반환값으로 구분할 수 없어**, 매번 조용히 한쪽으로 결정된다.

→ **기동 중에는 확인창을 띄우지 않는다.** 대신:

1. `pick === 'ask'`도 **최신 쪽을 자동 채택**한다(확정 규칙과 동일).
2. **탈락한 쪽을 유예 보관소에 넣는다** — `survey_trashed_draft_<조사지번호>`. 3차의 A-6 복구 UI가 이미 있으므로 **새 UI가 필요 없고**, 사용자는 저장목록 탭에서 "최근 정리된 작성분"으로 되돌릴 수 있다. (보관은 localStorage에만 하므로 B-5와 충돌하지 않는다.) **`_default`는 예외 — 보관하지 않는다.** 지금까지 `trashDraft()`는 실제 조사지번호로만 불렸는데(`onPlotSelect()`의 `trashDraft(_p)`) B-4-3이 그 경로를 처음 열어, 내용이 적어 0.5 문턱에 쉽게 걸리는 `_default`가 보관소에 섞여 들어갈 수 있다. `_default`는 §7이 이미 "복구 경로는 자동복원 하나뿐"이라 규정한 슬롯이라 별도 보관이 의미가 없다.
3. 이미 같은 내용이 보관돼 있으면 다시 넣지 않는다 — `_savedAt`으로 중복을 판정한다(`_trashedAt`은 매번 달라지므로 비교에서 제외).
4. 1겹 목록의 해당 행에 "두 저장소 내용이 달랐음" 배지를 남긴다. **DOM 요소를 4겹과 공유하지 않는다** — `renderDraftList()`는 호출마다 `body.innerHTML`을 통째로 새로 그려 명령형으로 찍은 배지가 소멸하고, 4겹의 비동기 유실 검사(6967~6974행)가 같은 자리에 나중에 덮어써 두 경고가 서로를 지운다. 대신 채택된 draft 본문에 **플래그**(`_mirrorConflictAt`)를 남기고, 이를 화면에 실제로 그리려면 **아래 두 곳을 함께 고쳐야 한다**(4차 검토 지적 — 스니펫에만 있고 이 지시가 빠지면 플래그가 저장만 되고 어디에도 안 보인다):
   - `summarizeDraft()`(6797~6814행)의 반환 객체에 `mirrorConflict: !!draft._mirrorConflictAt`를 추가한다.
   - `renderDraftList()`의 행 템플릿(`draft-missing-warning` 옆, 6890행 근처)에 `it.mirrorConflict`일 때만 보이는 별도 요소를 추가한다: `<div class="draft-mirror-conflict-warning">두 저장소 내용이 달라 자동으로 최신 쪽을 선택했습니다</div>`. `draft-missing-warning`과 겹치지 않는 새 요소라 4겹의 비동기 갱신과 충돌하지 않는다.
   - 이 플래그는 별도 정리 로직이 필요 없다 — 사용자가 그 포인트를 한 번이라도 편집해 `saveDraft()`가 새 draft 객체를 만들면(`_firstEditedAt`만 이어받고 나머지는 새로 만드는 방식) 자연히 사라진다. "매 렌더마다 그린다"는 것은 **그 draft를 아직 한 번도 안 건드린 동안만**이라는 뜻이다.

이렇게 하면 `chooseDraftSource()`의 `'ask'`는 **"자동 채택하되 흔적을 남겨라"**라는 뜻이 되고, 기동 흐름은 어떤 경우에도 사용자 입력을 기다리지 않는다.

```js
// 탈락한 쪽을 유예 보관소에 넣는다. 슬롯 밀어내기 규칙은 trashDraft()와 똑같아야 하므로
// (최신 → '' / 직전 → '__prev'), 그 부분을 trashDraft()에서 pushTrashSlot(draftKey, body)로
// 뽑아 둘이 공유한다 — 계약은 아래 참고.
// 채택된 쪽 draft에 _mirrorConflictAt 플래그를 남기는 것도 이 함수가 함께 한다.
// '_'로 시작하므로 draftContentScore()(1922행 if (k.startsWith('_')) return;)의
// 점수 계산에는 영향을 주지 않는다. summarizeDraft()가 이 값을 실어 renderDraftList()가
// 매 렌더마다 그린다(4겹과 겹치지 않는 별도 요소 — 4.4.1 본문 참고).
// draftKey는 이 plotNo의 표준 localStorage 키(=lsKey, DRAFT_KEY_PREFIX + plotNo)다.
// mergeDraftMirrors()는 실제 저장된 키를 순회해 plotNo를 뽑아 쓰므로(337~339행) 여기서는
// getDraftKey()식 trim 불일치가 생기지 않는다 — trashDraft() 내부 검색과는 다른 상황이다.
function stashLoserToTrash(plotNo, loserRaw, draftKey) {
  if (!loserRaw || plotNo === '_default') return;

  let d = null; try { d = JSON.parse(loserRaw); } catch (e) { d = null; }
  const winnerRaw = localStorage.getItem(draftKey);
  let w = null; try { w = winnerRaw ? JSON.parse(winnerRaw) : null; } catch (e) {}

  // 중복 방지: 이미 같은 _savedAt으로 보관돼 있으면 다시 넣지 않는다.
  const existing = localStorage.getItem(TRASHED_DRAFT_PREFIX + plotNo);
  let ex = null; try { ex = existing ? JSON.parse(existing) : null; } catch (e) {}
  if (ex && d && ex._savedAt === d._savedAt) return;

  const body = d
    ? JSON.stringify(Object.assign({}, d, { _trashedAt: Date.now() }))
    : JSON.stringify({ _trashedAt: Date.now(), _rawUnparsed: loserRaw });
  const ok = pushTrashSlot(draftKey, body);   // 실패해도 throw하지 않는다 — 계약은 아래 참고
  if (ok && w) {
    w._mirrorConflictAt = Date.now();
    try { localStorage.setItem(draftKey, JSON.stringify(w)); } catch (e) {}
  }
}
```

**`pushTrashSlot(draftKey, body)`의 반환 계약 (4차 검토에서 확정)** — 현행 `trashDraft()`(1978~2013행)의 밀어내기 블록(`curKey`/`prevKey` 관리, 실패 시 `__prev` 원복)을 그대로 이 함수로 뽑아 쓰되, 반드시 **boolean을 반환하고 절대 throw하지 않는다.** 이 계약은 `pushTrashSlot()`이라는 **내부 헬퍼**만의 것이며, `trashDraft()`의 **외부 반환 계약(문자열 키 / `''` / `null`, 601~604행)은 바뀌지 않는다** — 4차 검토에서 이 연결부가 지시서에 없어 구현자가 boolean을 그대로 흘려보낼 위험이 지적됐다.

```js
function trashDraft(plotNo) {
  ...
  if (!pushTrashSlot(key, body)) return null;   // 실패 — 원본은 절대 지우지 않는다
  localStorage.removeItem(key);                 // 보관에 성공한 뒤에만 원본을 비운다
  return trashKeyOf(key, '');                   // 문자열 키를 반환한다 — pushTrashSlot()이 쓴 것과 동일한 계산.
                                                 // trashKeyOf()는 순수 함수라 재호출 비용이 없다.
}
```

`stashLoserToTrash()`도 이 계약에 맞춰 `pushTrashSlot()`의 반환값(boolean)을 검사한다(try/catch가 아니다). 아울러 **`pushTrashSlot()`에 넘기는 키는 `plotNo`로 재조립한 것이 아니라 `trashDraft()`가 1982행에서 실제로 찾은 `key`**여야 한다 — `getDraftKey()`가 trim하지 않으므로 재조립하면 §4.6.1 A-1이 이미 경고한 표기 불일치가 재발한다.

**B-4-4. 승격 실패와 미러 갱신 — 산문과 코드를 일치시킨다**

- **승격의 `localStorage.setItem`은 반드시 `try/catch`로 감싸고, 실패하면 그 키만 건너뛰고 `continue`한다.** 이게 발동하는 조건이 하필 이 겹의 핵심 시나리오다 — 용량 압박으로 정리된 기기에서 미러 전량을 다시 써 넣으면 바로 `QuotaExceededError`가 난다. 감싸지 않으면 `mergeDraftMirrors()`가 reject되고 **나머지 키의 승격도 전부 중단된다.**
- **승격 실패 시 재시도하지 않는다.** `evictOldestTrashedDraft()`로 오래된 보관본을 지우고 재시도하는 방식은 `saveDraft()`(2095~2102행)가 활성 draft를 지키기 위해 쓰는 패턴이지만, 이 자리에는 옮기지 않는다 — 같은 루프 안에서 `stashLoserToTrash()`가 방금 만든 보관본을, `evictOldestTrashedDraft()`가 "가장 오래된 것부터" 지우다가 되짚어 지우는 충돌이 생긴다(3차 검토 지적). 승격 실패는 손실이 아니다 — 미러는 그대로 남아 다음 실행에서 다시 시도된다(394행과 같은 논리).
- **`pick === 'none'`이라도 localStorage에 `_savedAt`이 있는 본문이 남아 있으면 `mirrorDraftToIDB()`로 미러를 갱신한다**(묘비를 본문으로 덮는다). 그러지 않으면: 묘비(t1) → 사용자가 복구를 눌러 `_savedAt`(t0 < t1)인 본문이 되살아남 → 재미러가 커밋 전에 죽음 → 이후 영원히 `pick:'none'`이라 **그 draft는 묘비 TTL 14일 동안 미러 보호를 못 받는다.** 데이터가 지워지진 않지만 사용자는 2중화되고 있다고 믿는다.
- 승격 후에는 기존 `restoreDraft(plotNo)`를 **그대로** 부른다. 시그니처를 바꾸지 않는 편이 회귀 위험이 작다.

**B-5. 보관본(유예 보관)은 미러하지 않는다**

`survey_trashed_draft_*`는 localStorage에만 둔다. 이유: ① 이미 "버려진" 데이터라 2중화 가치가 낮고, ② 미러까지 두면 저장 용량이 최대 4배가 되어 A-5의 압박 대응이 무의미해지며, ③ 보관본이 IDB에서 되살아나 목록에 유령처럼 나타나는 경로가 새로 생긴다.

**대가를 정확히 적어 둔다**: 5겹에서 "저장된 내용"을 고른 **그 시점부터** 탈락한 편집분은 **localStorage 단일 사본**이 되며, 8겹 내보내기에도 포함되지 않는다. 즉 이 겹은 그 순간 보호 수준을 스스로 한 단계 낮춘다. §7에 이 문장 그대로 남긴다.

**B-6. 용량 — 7겹과 반드시 짝으로**

미러는 활성 draft를 2벌로 만든다. A-5의 quota 대응은 **localStorage에만** 해당하므로 IDB 쪽 quota 초과는 `.catch()`로 넘어간다. 그래도 되는 이유는 미러가 보조이기 때문이지만, **미러가 계속 실패하는 상태를 사용자가 영영 모르는 것도 곤란하다** — 연속 실패가 일정 횟수를 넘으면 한 번만 알린다(매번 토스트는 소음).

---

### 4.5 [4겹] 복원 시 무결성 검증 — 2차

**의도**: `restoreDraft()` 완료 직후, draft가 기대하는 사진 수(`_photoKeys`/`_movePhotoKeys`)와 실제 IDB에 존재하는 수를 대조한다. 부족하면 해당 슬롯을 `photo-required-error` 클래스로 강조하고 오류 토스트를 띄운다(오류 토스트는 4.5초 유지 — `showToast()` 7604행).

**검사 시점**: `restoreDraft()`의 사진 복원 루프(2162~2239행)에서 이미 슬롯별로 `stored` 유무를 판정하고 있으므로, 그 루프에 유실 카운트를 누적하는 방식으로 **추가 IDB 조회 없이** 구현한다.

**장점**: 위험 F 해결. 지금은 모든 입력을 마치고 저장 버튼을 눌러서야 막히는데, 불러온 즉시 알면 현장에서 바로 재촬영할 수 있다.
**단점**: 경고 피로.
**상호보완**: 1겹 목록에 "⚠️ 사진 3장 유실"을 미리 표시하면, 불러온 뒤의 경고는 확인 사살 역할만 하므로 피로가 줄어든다.

---

### 4.6 [5겹] 정식 레코드 ↔ draft 우선순위 정리 — 3차

**의도**: `onPlotSelect()`(6084~6165행)에서 정식 레코드가 있어도, **그 레코드의 저장 시각보다 나중에 기록된 draft**가 있으면 사용자에게 선택지를 준다.

```
[5번] 저장된 내용과 작성 중이던 내용이 모두 있습니다.

  확인 = 작성 중이던 내용(9/13 09:15까지 작성)   ← 더 최신
  취소 = 저장된 내용(9/12 18:40 저장)
```

구현은 이 앱의 기존 관례대로 **동기 `confirm()`** 으로 한다(2390·6204·6708행). 비동기 커스텀 모달을 쓰려면 `_doReset()`(6061행)보다 앞에서 물어야 한다 — 자세한 이유와 코드는 §4.6.1 A-3.

**시각 비교 기준**: 정식 레코드는 `savedAt`(한국어 로케일 문자열, `saveData()` 4178행)이라 파싱이 불안정하다. 따라서 **레코드 쪽에 `savedAtMs`(숫자 타임스탬프)를 새로 추가**하고, 그 값이 없는 기존 레코드는 비교 불가로 간주해 **현행대로 정식 레코드를 우선**한다(하위 호환).

**단, `savedAtMs`의 역할은 "선택 UI를 띄울지 말지"까지다.** 유예 보관(§4.6.1 A-0)은 `savedAtMs` 유무와 **무관하게** 레코드로 폼을 덮기 직전에 항상 수행한다. 그렇지 않으면 3차 배포 시점에 기기에 있던 모든 레코드(= `savedAtMs`가 없는 것들)가 무방비 구간이 되어, 3차가 막으려던 손실이 그대로 남는다(2차 검토 지적).

**조사일 취급**: 어느 쪽을 고르든 조사일은 **레코드에 기록된 값을 그대로 유지**한다. draft에는 `soil-date`가 없어 그냥 두면 "작성 중이던 내용" 쪽만 오늘 날짜로 바뀌는 비대칭이 생기기 때문이다 — 구현 규칙은 §4.6.1 A-4에 있다.

```js
// saveData()의 newRecord 객체(4164~4183행)에 한 줄 추가 — 기존 draft._savedAt과 반드시 같은
// 단위(Date.now(), 밀리초 epoch)로 맞춘다. 초 단위 등 다른 단위를 쓰면 §4.1의 목록 갱신
// 비교가 깨지므로(3차 검토에서 지적) 반드시 이 형태를 그대로 쓴다.
const newRecord = {
  id: existingRec2 ? existingRec2.id : Date.now(),
  ...
  savedAt: new Date().toLocaleString('ko-KR'),   // 기존 — 화면 표시용, 그대로 유지
  savedAtMs: Date.now(),                          // 신규 — 비교 전용, draft._savedAt과 동일 단위
};
```

**§4.1(1겹) 제외 조건도 이때 함께 갱신한다(2차 검토에서 지적됨)**: 1차·2차 구간에서는 §4.1이 "정식 레코드가 있으면 무조건 목록에서 제외"라는 단순화된 조건을 썼다. `savedAtMs`가 생기는 이 시점부터, 1겹의 목록 수집 로직을 "정식 레코드의 `savedAtMs`보다 draft의 `_savedAt`이 더 최신이면 목록에 포함"으로 갱신한다(구현 항목은 §6.3에 명시). 이렇게 해야 §5 대응표가 명시한 "위험 D의 3차 방어선 = 1겹 목록 노출"이 실제로 성립한다.

**장점**: 위험 D 해결.
**단점**: 선택 UI가 하나 늘어난다.
**상호보완**: 6겹이 각 선택지에 시각 근거를 붙여 혼란을 없앤다. 시각 없이 "저장본/작성본"만 물으면 판단이 불가능하다.

---

#### 4.6.1 이 겹 자체가 만드는 위험과 그 대책 (착수 전 추가 설계 · **6차 검토 반영판 — 구현 착수 승인**)

**이 겹이 다른 겹과 다른 점**: 1·2차의 모든 겹은 기존 흐름 *옆에* 새 장치를 더하는 방식이라, 새 코드가 실패해도 기존 저장·전송은 영향을 받지 않았다. 반면 이 겹은 "포인트를 열었을 때 무엇을 화면에 띄울 것인가"라는 **기존 분기 자체를 바꾼다**. 그래서 구현 실수가 곧바로 데이터 손실로 이어질 수 있다.

**구체적 손실 시나리오**:
1. 월요일 — 5번을 완성해 정식 저장(레코드 생성).
2. 수요일 — 5번을 다시 열어 일부를 고치다 중단(레코드보다 최신인 draft 생성).
3. 목요일 — 5번을 열면 이 겹의 선택 화면이 뜬다.
4. 사용자가 "저장된 내용"(월요일 레코드)을 고르면 `_restoreRecordData()`가 폼을 월요일 내용으로 채운다.
5. **여기서 손실이 일어난다.** 이 상태에서 자동저장이 돌면 `survey_draft_5`가 월요일 내용으로 덮어써져 수요일 편집분이 사라진다.
6. 사용자가 저장 버튼을 누르지 않고 다른 포인트로 넘어가기만 해도 손실된다.

**1차 검토에서 정정된 핵심 사실 — 손실 지점은 "저장"이 아니라 "선택 직후"다**

이 문서의 초판은 대책을 "저장 성공 시 draft를 지우는 대신 보관한다"로 설계했으나, **그 시점엔 이미 늦다**는 것이 확인됐다. 근거:

- 선택 UI에서 레코드를 고르면 폼이 레코드 내용으로 채워진다. 그 뒤 draft를 덮어쓰는 경로가 **네 갈래**나 있다(4차 검토에서 1건 추가): 입력 시 1초 디바운스(`scheduleDraftSave()` 3034행, 실제 실행은 예약된 타이머 콜백), 화면 이탈 시 즉시 저장(`flushDraftNow()` 3049행), **같은 `visibilitychange`에 물린 별개 리스너의 직접 호출(3205행)**, 다른 포인트로 이동할 때 `onPlotSelect()` 맨 앞의 `saveDraft()`(6049행).
- **3겹 가드는 이걸 막지 못한다.** `shouldWriteDraft()`(1934~1942행)는 "값이 있는 항목 개수"만 비교하는데, 레코드에서 복원된 폼은 완성본이라 미완성 편집분보다 항목 수가 같거나 많다 → `nextScore >= prevScore`로 통과한다. 이는 §7이 이미 인정한 3겹의 알려진 한계가 그대로 드러나는 경우다.

**2차 검토에서 추가로 드러난 사실 — "선택 시점 보관"만으로도 부족하다**

2차 검토는 "선택 시점 보관" 설계에 **여전히 남는 손실 경로 다섯 가지**를 찾아냈다. 아래 대책 A는 그 다섯 가지를 모두 포함해 다시 쓴 것이다.

- ❶ `savedAtMs`가 없는 **기존 레코드**는 선택 UI를 띄우지 않으므로 보관도 건너뛴다 → 3차를 배포해도 기존 데이터는 무방비다. 3차 배포 시점에 기기에 있는 **모든** 레코드가 이 구간에 해당한다.
- ❷ 레코드를 복원한 폼이 곧바로 draft로 다시 쓰이는 **"레코드 메아리"** 가 생긴다 → 같은 포인트를 두 번째로 열어 다시 "저장된 내용"을 고르면 그 메아리가 보관본을 덮어 진짜 편집분이 소멸한다. 메아리는 목록 영구 오염·선택 UI 재발·앱 시작 자동복원 오염도 함께 일으킨다.
- ❸ `trashDraft()` 성공 직후 `_restoreRecordData()`가 예외로 실패하면 **되돌릴 경로가 없다**(폼은 반쯤 채워지고 draft는 이미 휴지통).
- ❹ 보관 실패 시 "중단"하면 `_doReset()`이 이미 폼을 비운 뒤라 **빈 폼**이 남고, 거기에 재입력하면 살아 있는 draft를 한 글자씩 잠식한다.
- ❺ 깨진 JSON draft 하나가 `trashDraft()`를 `false`로 만들어 **그 포인트를 영영 열 수 없게** 만든다(목록에도 안 떠서 "작성 취소"로 지울 수도 없다).

**3차 검토에서 드러난 사실 — 메아리 차단 방식(초안)이 오히려 2겹을 무력화한다**

2차 반영판은 메아리를 `saveDraft()` 잠금 + `e.isTrusted` 해제로 막으려 했다. 3차 검토는 이 방식이 **이 앱에서는 정반대로 작동한다**는 것을 코드로 확인했다 — 빠른선택 버튼·숫자 선택 시트가 합성 이벤트(`isTrusted === false`)를 쓰므로 필수 select 12개를 조작해도 잠금이 풀리지 않고, 반대로 `document` 전역 리스너라 무관한 화면 조작에는 쉽게 풀려 메아리는 그대로 통과한다. 게다가 `saveDraft()` 자체를 막는 구조라, 사진 삭제나 이동사진 임시저장이 **아무것도 저장하지 않고 성공 토스트만 띄우는** 상태가 된다. 아래 A-2는 이 방식을 폐기하고 **"암묵적 저장 3경로에만 잠금, `saveDraft()`는 절대 막지 않음"** 으로 다시 설계한 것이다. 그 밖에 ❻ 롤백이 DOM을 되돌리지 않아 무효가 되는 문제, ❼ 롤백 키를 재조립해 조용히 실패하는 문제, ❽ A-3 스니펫에 선택 UI와 `else` 분기가 빠져 있던 문제도 함께 고쳤다.

**4차 검토에서 드러난 사실 — 잠가야 할 암묵적 경로가 3개가 아니라 4개였다**

3차 반영판은 "암묵적 저장 3경로에만 잠금"으로 방향을 잡았고 그 방향 자체는 코드 전수 확인으로 옳다고 검증됐다(직접 호출 5곳은 모두 사용자 조작 직후라 막히지 않는다). 그러나 **잠글 경로를 두 개 놓쳤다**. ① `visibilitychange` 리스너가 두 개이고, `flushDraftNow` 쪽이 아닌 **3205행 리스너가 `saveDraft()`를 직접 부른다** — 레코드를 열고 아무것도 건드리지 않은 채 홈 버튼·전화 수신으로 이탈하는, 현장에서 가장 흔한 형태에서 메아리가 그대로 기록된다. ② `scheduleDraftSave()`의 **진입부**를 잠가봐야 잠금 직전에 이미 예약된 타이머는 `setTimeout(saveDraft, 1000)`으로 `saveDraft`를 곧장 실행해 우회한다. 아래 A-2 (2)는 이 둘을 포함해 네 경로로 다시 썼다. 그 밖에 A-3 스니펫의 `fmtTime()` 미정의(파일에 존재하지 않는 함수 — 실행 즉시 `ReferenceError`), `existing.soilRow[3]` 무방비 접근, `restoreDraft(val)`의 trim 불일치, `trashed === ''`일 때 빈 폼을 "되돌렸다"고 알리는 문제도 함께 고쳤다.

**중요**: 이 위험의 뿌리(위험 D)는 3차를 하지 않아도 지금 코드에 이미 존재한다. 현재는 draft를 아예 쳐다보지 않으므로 수요일 편집분이 조용히 사라진다. 3차는 그 구멍을 막으려는 시도이며, 이 겹 고유의 추가 위험은 "보호받는다고 믿게 만든 뒤 그 믿음이 버그로 배신당하는 것"이다. 따라서 아래 두 대책을 **이 겹의 필수 구성요소로 함께 구현한다.**

##### 대책 A — 탈락한 버전을 "선택하는 그 순간" 보관한다 (14일)

보관 시점을 저장이 아니라 **레코드로 폼을 덮기 직전**으로 옮긴다. 그래야 위 세 갈래 덮어쓰기 경로보다 먼저 원본을 확보할 수 있다.

**A-0. 적용 조건 — `savedAtMs` 유무와 무관하게 보관한다 (❶ 대응)**

`savedAtMs`는 **선택 UI를 띄울지 말지**를 정하는 데에만 쓴다. **보관 여부는 "레코드가 있고 활성 draft도 있는가" 하나로만 판단한다.** 즉 `savedAtMs`가 없는 기존 레코드도 `_restoreRecordData()` 직전에 반드시 보관을 거친다. 이렇게 하지 않으면 3차 배포 시점에 기기에 있던 모든 레코드가 무방비 구간이 되어, 3차의 목적 자체가 달성되지 않는다.

**A-1. 보관 함수 — 포인트당 최신 2건 슬롯**

메아리가 혹시라도 만들어졌을 때 진짜 편집분이 덮여 사라지는 것을 막기 위해, 보관 슬롯을 **포인트당 2건**으로 둔다. 무한 증식이 아니므로 용량 압박은 A-5로 통제된다.

```js
// 유예 보관 키 — DRAFT_KEY_PREFIX('survey_draft_')로 시작하지 않아야 한다.
// 앱 시작 자동복원(2561행)과 1겹 목록(renderDraftList 6832행)이 모두
// startsWith(DRAFT_KEY_PREFIX)로 키를 긁어가므로, 접두사가 겹치면
// 버려진 draft가 작성 중 목록에 되살아나는 사고가 난다.
// 'survey_trashed_draft_'는 'survey_draft_'로 시작하지 않으므로 안전하다
// (2차 검토에서 전수 확인: 해당 스캔은 위 2곳뿐이고, 파일 전체에
//  localStorage.clear()나 localStorage.key(i) 순회는 존재하지 않는다).
const TRASHED_DRAFT_PREFIX = 'survey_trashed_draft_';
const TRASHED_DRAFT_TTL_MS = 14 * 24 * 60 * 60 * 1000; // 14일

// 보관 키는 원본 키에서 접두사만 바꿔 만든다. plotNo로 새로 조립하면
// 공백이 섞인 키와 표기가 갈려 복구가 원래 슬롯으로 돌아가지 못한다.
// (6064행 `inp.value = val;`은 trim하지 않는다 — 레코드 매칭용 _target만 6041행에서 trim된다.)
// slot: ''(최신) / '__prev'(직전)
function trashKeyOf(draftKey, slot) {
  return TRASHED_DRAFT_PREFIX + draftKey.slice(DRAFT_KEY_PREFIX.length) + (slot || '');
}

// 탈락한 draft를 유예 보관소로 옮긴다.
// 반환값(3차 검토 반영 — boolean이 아니라 '실제로 기록한 키'를 돌려준다):
//   문자열 키 : 보관 성공. 롤백·복구는 이 값을 그대로 써야 표기 불일치가 생기지 않는다.
//   ''        : 보관할 draft가 애초에 없었음(정상)
//   null      : 보관 실패. 호출부는 레코드로 폼을 덮으면 안 된다(A-3).
function trashDraft(plotNo) {
  if (!plotNo) return null;
  purgeExpiredTrashedDrafts();          // A-5: 보관 전에 만료분부터 비운다
  // getDraftKey()는 trim을 하지 않으므로(1911~1914행), 폼에 앞뒤 공백이 섞여
  // 들어간 경우를 대비해 정규화한 키와 폼 기준 키를 모두 시도한다.
  const candidates = [DRAFT_KEY_PREFIX + plotNo, getDraftKey()];
  const key = candidates.find(k => k.startsWith(DRAFT_KEY_PREFIX)
                                && localStorage.getItem(k) !== null);
  if (!key) return '';                  // 보관할 draft 자체가 없음 = 실패가 아님

  const raw = localStorage.getItem(key);
  let payload = null;
  try { payload = JSON.parse(raw); } catch (e) { payload = null; }
  // ❺ 깨진 JSON도 보관 실패로 처리하지 않는다. 원문을 그대로 감싸 둔다.
  const body = payload
    ? JSON.stringify(Object.assign({}, payload, { _trashedAt: Date.now() }))
    : JSON.stringify({ _trashedAt: Date.now(), _rawUnparsed: raw });

  // 슬롯 밀어내기는 B-4-3의 stashLoserToTrash()도 그대로 써야 하므로,
  // 이 블록을 pushTrashSlot(draftKey, body)로 뽑아 둘이 공유한다(규칙이 두 군데로 갈리면 안 된다).
  const curKey = trashKeyOf(key, '');
  const prevKey = trashKeyOf(key, '__prev');
  const curBefore = localStorage.getItem(curKey);
  const prevBefore = localStorage.getItem(prevKey);
  try {
    // 최신 슬롯에 있던 것을 직전 슬롯으로 한 칸 밀고, 최신 슬롯에 새로 쓴다.
    // 메아리가 뚫고 들어오더라도 직전 슬롯에 진짜 편집분이 남는다(❷ 2중 방어).
    if (curBefore !== null) localStorage.setItem(prevKey, curBefore);
    localStorage.setItem(curKey, body);
  } catch (e) {
    // 밀어내기만 성공하고 본 기록이 실패하면 __prev에 중복본이 남아 용량만 더 먹는다.
    // 밀어내기 전 상태로 되돌린다(3차 검토 지적).
    try {
      if (prevBefore === null) localStorage.removeItem(prevKey);
      else localStorage.setItem(prevKey, prevBefore);
    } catch (e2) { /* 되돌리기까지 실패하면 더 할 수 있는 게 없다 */ }
    console.warn('draft 유예 보관 실패 — 원본을 그대로 둔다', e);
    return null;                        // 저장공간 부족 등. 원본은 절대 지우지 않는다.
  }
  localStorage.removeItem(key);         // 보관에 성공한 뒤에만 원본을 비운다
  return curKey;
}

// 유예 보관본을 활성 draft로 되돌린다(복구 UI와 ❸ 롤백이 함께 쓴다).
// trashKey는 trashDraft()의 반환값 또는 복구 UI가 목록에서 넘겨주는 실제 키를
// 그대로 받는다 — 여기서 plotNo로 재조립하지 않는다.
function restoreTrashedDraft(trashKey) {
  const raw = localStorage.getItem(trashKey);
  if (raw === null) return false;
  const draftKey = DRAFT_KEY_PREFIX + trashKey.slice(TRASHED_DRAFT_PREFIX.length)
                                              .replace(/__prev$/, '');
  try {
    const d = JSON.parse(raw);
    let body;
    if (d && d._rawUnparsed !== undefined) {
      body = d._rawUnparsed;            // 깨진 원문은 손대지 않고 그대로 되돌린다
    } else {
      delete d._trashedAt;
      body = JSON.stringify(d);
    }
    localStorage.setItem(draftKey, body);
    localStorage.removeItem(trashKey);
    return true;
  } catch (e) { console.warn('유예 보관본 복구 실패', e); return false; }
}

// 앱 시작 시 만료분 정리. TRASHED_DRAFT_PREFIX로 시작하는 키만 건드린다.
// ※ 이 접두사 문자열을 한 글자라도 잘못 적으면 활성 draft가 전부 지워진다
//   — 이 기능에서 가장 치명적인 단순 실수 유형이다. 대책 B 14단계에서 반드시 회귀 확인.
function purgeExpiredTrashedDrafts() {
  const now = Date.now();
  Object.keys(localStorage).forEach(k => {
    if (!k.startsWith(TRASHED_DRAFT_PREFIX)) return;
    try {
      const d = JSON.parse(localStorage.getItem(k));
      if (!d || !d._trashedAt || now - d._trashedAt > TRASHED_DRAFT_TTL_MS) {
        localStorage.removeItem(k);
      }
    } catch (e) { localStorage.removeItem(k); } // 파싱 불가 = 복구 가치 없음
  });
}
```

**A-2. 레코드 메아리를 애초에 만들지 않는다 (❷ 대응 · 3차 검토로 방식 전면 교체)**

메아리 하나가 네 가지 증상(보관본 덮어쓰기 / 목록 영구 오염 / 선택 UI 재발 / 앱 시작 자동복원 오염)을 동시에 만들므로, 표식을 붙여 소비처마다 걸러내는 대신 **메아리 생성 자체를 막는다.**

**3차 검토에서 폐기된 초안**: 처음에는 `saveDraft()` 맨 앞에 잠금을 두고 `e.isTrusted`로 해제하려 했다. **이 방식은 반드시 폐기한다.** 두 방향 모두 틀렸다는 것이 코드로 확인됐다.

- **너무 빡빡한 쪽**: 이 앱의 주력 입력 위젯이 **합성 이벤트**를 쓴다. 빠른선택 버튼(2672행 `sel.dispatchEvent(new Event('change', { bubbles: true }))`)과 숫자 선택 시트(2761·2781행)가 그렇고, 빠른선택이 걸린 대상은 `soil-erosion`·`soil-deposit`·`soil-wind`·`soil-rock`·`soil-weathering`·`soil-moist-a/b`·`soil-100cc-10/30`·`soil-chem-10/30` **11개 필수 select 전부**(2545~2546행)이며 `soil-depth-b`는 네이티브 select가 숨겨져 있어(711행) 시트 외의 입력 수단이 없다. `isTrusted`로 걸렀다면 **현장에서 가장 흔한 조작이 잠금을 못 풀어** 자동저장이 멎었을 것이다.
- **너무 헐거운 쪽**: `document` 전역 리스너라 draft와 무관한 조작(저장목록 전체선택 체크박스 1039행, 설정탭 팀코드 select 1097·1112행)까지 잠금을 풀어 메아리를 그대로 통과시킨다.
- 게다가 `isTrusted`의 명분이던 "복원 중 프로그램이 change를 발화한다"는 **사실이 아니다.** 파일 전체의 발화 지점은 네 곳뿐이고(2534·2623·2643·2799) 전부 사용자 조작 경로다. `_restoreRecordData()`는 `el.value` 대입과 `slot.innerHTML` 조작만 한다.

**확정 방식 — 잠금은 "암묵적 저장 3경로"에만 걸고, `saveDraft()` 자체는 절대 막지 않는다**

이것이 이번 판의 핵심이다. `saveDraft()`를 막지 않으므로 **"저장한 줄 알았는데 아무것도 저장되지 않는" 실패 모드가 원천적으로 존재할 수 없다.**

```js
// 레코드를 복원한 직후부터 '사용자가 실제로 손대기 전'까지, 암묵적 저장만 잠근다.
let _recordEchoGuard = false;

// (1) 설정 — _restoreRecordData() 함수 '말미'에 둔다.
//     호출부(5875 onPlotSelect, 5935 fetchFromSheet 두 곳뿐 — 전수 확인)에 각각 붙이면
//     반드시 하나를 빠뜨린다. 함수 말미 한 곳이면 두 경로가 모두 덮인다.
//     대기 중인 디바운스 예약도 이때 함께 없앤다 — (2)③의 콜백 가드와 '둘 다' 넣을 것.
//     콜백 가드만 두면 타이머가 살아남아 다음 해제 직후 엉뚱한 시점에 발화한다.
clearTimeout(_draftTimer);
_draftTimer = null;
_recordEchoGuard = true;

// (2) 잠금이 적용되는 곳 — 아래 네 항목이며, ②는 '잠근다'가 아니라 '호출을 없앤다'이므로
//     최종적으로 잠금 지점은 ①③④ 세 곳이다. saveDraft() 본문은 손대지 않는다.
//     4차 검토에서 '암묵적 저장 3경로'가 실은 4경로였음이 확인됐다(전수 확인:
//     saveDraft() 호출 지점은 2387·2875·3015·4352·5070·5181·5278·5832 여덟 곳이고,
//     그중 사용자 조작 없이 도는 것은 2875·3015·5832 + 디바운스 타이머 콜백이다).
//
//   ① flushDraftNow() 3049행 맨 앞:  if (_recordEchoGuard) return;
//
//   ② 3205행의 '두 번째 visibilitychange 핸들러' — A-2 초안이 놓쳤던 경로다.
//      3204~3212행에는 flushDraftNow와 별개로 등록된 리스너가 있고 그 안에서
//      saveDraft()를 직접 부른다(그래서 지금도 백그라운드 전환 때 두 번 저장된다).
//      레코드를 열고 아무것도 건드리지 않은 채 홈 버튼·전화 수신·화면 잠금으로
//      이탈하면 — 현장에서 가장 흔한 이탈 형태다 — 여기서 메아리가 그대로 기록된다.
//      → 3205행의 `saveDraft();` 호출을 **제거**한다(주변 핸들러는 syncCompletedPlots
//        용도로 그대로 둔다). 5차 검토 정정: `flushDraftNow()`로 '교체'하면 3051행
//        리스너와 합쳐 flushDraftNow가 두 번 돌고, flushDraftNow에는 중복 방지가
//        없으므로(3049~3054행) saveDraft는 여전히 2회 실행된다. 제거해야 잠금이 ①
//        하나로 진짜 일원화되고, 화면이 꺼지는 촉박한 시점의 폼 전체 직렬화도 1회로 준다.
//        3498행 주석("공용 리스너가 이미 호출하므로 여기서 또 부르면 두 번 실행된다")은
//        원래 '중복을 피하라'는 근거다 — 1차에서 3051행 리스너가 추가되며 공용 리스너가
//        둘이 된 상태이므로, 주석도 "hidden 시 draft 확정은 flushDraftNow()(3049행)가
//        담당"으로 함께 갱신한다.
//
//   ③ scheduleDraftSave() 3034행 — **진입부가 아니라 콜백에 건다.**
//      진입부만 막으면 잠금 직전에 이미 예약된 타이머가 scheduleDraftSave를
//      거치지 않고 saveDraft를 곧장 실행해 우회한다(3038행 `setTimeout(saveDraft, 1000)`).
//      재현: 포인트 A에 입력 → 1초 타이머 예약 → 1초 안에 레코드가 있는 포인트 B 선택
//           → confirm()·_restoreRecordData() → 가드 ON → 대기하던 콜백이 B의 메아리를 씀.
//      function scheduleDraftSave() {
//        clearTimeout(_draftTimer);
//        _draftTimer = setTimeout(() => { if (_recordEchoGuard) return; saveDraft(); }, 1000);
//      }
//
//   ④ onPlotSelect() 6049행:  saveDraft();  →  if (!_recordEchoGuard) saveDraft();

// (3) 해제 ① — 조사 폼의 'draft 스냅샷 대상' 필드가 바뀌면 푼다.
//     isTrusted는 쓰지 않는다(합성 이벤트가 정상 입력 수단이므로).
//     대상 범위로 한정하므로 무관한 화면 조작으로는 풀리지 않는다.
const DRAFT_FIELD_SEL = 'input[type=text], input[type=number], textarea, select, input[type=radio]';
['input', 'change'].forEach(evt => {
  document.addEventListener(evt, (e) => {
    const el = e.target;
    if (!el || !el.matches || !el.matches(DRAFT_FIELD_SEL)) return;
    if (!el.closest('#tab-soil, #tab-tree')) return;   // 저장목록·설정 탭은 제외
    if (KEEP_IDS.includes(el.id)) return;              // 설정값 필드 제외(2901행 목록 재사용)
    _recordEchoGuard = false;
  }, true); // capture 단계 — 중간에서 stopPropagation 돼도 놓치지 않는다
});

// (0) 불변식: _recordEchoGuard는 '레코드로 폼을 덮는 데 성공한 경우에만' true로 남는다.
//     화면에 draft 또는 빈 폼이 있는 상태에서는 항상 false여야 한다(A-3의 세 분기 참고).
//
// (4) 해제 ② — saveDraft()가 실제로 한 번 수행되면 푼다.
//     위치: saveDraft()의 try/catch '바깥', 함수 최하단. try 안에 두면 quota 예외 시
//     가드가 풀리지 않는다(2061~2113행이 통째로 하나의 try다).
//     사진 삭제(5267·5475행)·이동사진 임시저장(2524행)처럼 이벤트가 없는 경로는
//     스스로 saveDraft()를 직접 부르는데, (2)가 그 호출을 막지 않으므로 정상 저장되고
//     이 줄에서 잠금도 함께 풀린다. 즉 별도의 함수별 해제 코드가 필요 없다.
_recordEchoGuard = false;
```

- **`KEEP_IDS`는 `_doReset()` 안의 지역 상수(2901행)이므로**, 리스너에서 쓰려면 파일 상단의 모듈 상수로 끌어올리고 `_doReset()`이 그것을 참조하도록 바꾼다(값은 그대로 — 동작 변화 없음).
- **잔여 위험과 그 크기**: 해제 ①·②가 모두 빗나가는 조작이 있다면, 그 조작 뒤의 **디바운스 저장과 이탈 flush만** 건너뛴다(직접 저장은 계속 동작). 초안의 "전면 정지"와 달리 영향이 국소적이며, 사진 경로는 ②가, 필드 경로는 ①이 덮으므로 실제로 남는 구멍은 확인되지 않았다. 그래도 대책 B 4·6단계에서 **빠른선택 버튼·숫자 시트·사진 삭제** 세 경로를 각각 값으로 확인한다.

**A-3. 적용 지점 (5겹 선택 UI 포함 · 3차 검토 반영)**

`onPlotSelect()`의 **6084~6165행 전체**(`if (existing) { … } else { … }`)를 아래 구조로 바꾼다. `else` 분기를 빠뜨리면 **레코드가 없는 포인트의 draft 복원이 통째로 죽는다** — 1겹 목록에서 항목을 눌러 여는 주 경로(6939행)가 그것이다.

선택 UI는 이 앱의 기존 관례대로 **동기 `confirm()`**(2390·6204·6708행)으로 만든다. 비동기 커스텀 모달을 쓰려면 `_doReset()`(6061행)보다 **앞에서** 물어야 한다 — 그 뒤에 물으면 사용자가 답하는 동안 폼이 빈 상태로 남아 flush가 빈 내용을 쓰게 된다.

```js
if (existing) {
  const _p = (val || '').trim();
  const draftKey = [DRAFT_KEY_PREFIX + _p, getDraftKey()]
        .find(k => k.startsWith(DRAFT_KEY_PREFIX) && localStorage.getItem(k) !== null);

  // 5겹 선택지 — savedAtMs가 있고, 그보다 최신인 draft가 있을 때만 묻는다(§4.6).
  let useDraft = false;
  const _dp = draftKey ? draftKey.slice(DRAFT_KEY_PREFIX.length) : '';
  if (draftKey && existing.savedAtMs) {
    let dt = 0;
    try { dt = JSON.parse(localStorage.getItem(draftKey))._savedAt || 0; } catch (e) { dt = 0; }
    if (dt > existing.savedAtMs) {
      useDraft = confirm('[' + val + '] 저장된 내용과 작성 중이던 내용이 모두 있습니다.\n\n'
        + '확인 = 작성 중이던 내용(' + fmtTime(dt) + '까지 작성)\n'   // fmtTime은 아래 ※ 참고
        + '취소 = 저장된 내용(' + existing.savedAt + ' 저장)');
    }
  }

  if (useDraft) {
    // A-4: 조사일은 레코드 값을 유지한다. draft에는 soil-date가 없으므로 먼저 복원한다.
    // soilRow가 없는 깨진 레코드에서 TypeError가 나면 이 줄은 try 밖이라 onPlotSelect가
    // 통째로 중단되고 빈 폼이 남는다 — 코드베이스의 방어 관례(2404·6269·7102·4068행)를 따른다.
    // newRecord에는 date 필드도 있으므로(4166행) 그쪽을 먼저 본다.
    setSurveyDate(existing.date || existing.soilRow?.[3], { keepTodayWhenEmpty: true });
    _recordEchoGuard = false;              // 화면은 draft다 — 불변식 (0)
    // ※ restoreDraft()는 getDraftKey(plotNo)로 키를 만들며 trim하지 않는다(1982행).
    //    val을 그대로 넘기면 존재 판정은 통과하고 복원만 실패하는 불일치가 생긴다.
    restoreDraft(_dp).then(restored => { if (!restored) { updatePhotoNames(); syncToTree(); } });
    return;
  }

  // 여기서부터 '저장된 내용' 경로. savedAtMs가 없는 옛 레코드도 반드시 여기를 지난다(A-0).
  const trashed = trashDraft(_p);          // '' | 키 | null
  if (trashed === null) {
    // ❹ 보관 실패 시 '빈 폼으로 중단'하지 않는다. _doReset()이 이미 폼을 비운 뒤(6656~6659행)라
    //    빈 폼을 남기면 사용자가 처음부터 재입력하게 되고, 그 입력이 살아 있는 draft를 잠식한다.
    _recordEchoGuard = false;              // 화면은 draft다 — 불변식 (0)
    restoreDraft(_dp).then(restored => { if (!restored) { updatePhotoNames(); syncToTree(); } });
    showToast('저장공간이 부족해 불러오기를 멈췄습니다. 작성 중이던 내용을 유지합니다.', 'error');
    return;
  }
  try {
    _restoreRecordData(existing);          // 함수 말미에서 _recordEchoGuard = true (A-2)
  } catch (e) {
    // ❸ 롤백: localStorage만 되돌리면 부족하다. 폼에는 _restoreRecordData()가 중간까지
    //    채워 놓은 레코드 조각이 남아 있고 가드도 걸리지 않은 상태라, 다음 saveDraft()가
    //    되돌려 놓은 draft를 곧바로 다시 덮는다(3차 검토 지적). 화면까지 함께 되돌린다.
    console.warn('레코드 복원 실패 — 유예 보관분을 되돌린다', e);
    // _restoreRecordData()가 어디서 던졌는지에 따라 함수 말미의 가드 설정에
    // 도달했을 수도, 안 했을 수도 있다. 불확정 상태로 화면을 넘기지 않는다.
    _recordEchoGuard = false;              // 불변식 (0)
    _doReset();
    if (trashed === '') {
      // 되돌릴 draft가 애초에 없었다. 화면은 빈 폼이고 레코드도 못 읽은 상태다.
      // 이때 조사지번호를 채워두면 사용자가 그 빈 폼을 저장했을 때 saveData()가
      // 4064행 findIndex로 기존 레코드를 찾아 '빈약한 내용으로 덮어쓴다'.
      // 유예 보관은 레코드 스냅샷을 남기지 않으므로(§7) 그 손실은 되돌릴 수 없다.
      // → 번호를 비워 저장이 기존 레코드를 겨냥하지 못하게 하고 새로고침을 안내한다.
      showToast('불러오기에 실패했습니다. 저장하지 말고 앱을 새로고침해 주세요.', 'error');
      return;
    }
    const ok = restoreTrashedDraft(trashed);
    syncPlotNoDisplay(val);                // 사전 작업 ④ — value만 넣으면 화면과 어긋난다
    if (ok) {
      restoreDraft(_dp).then(restored => { if (!restored) { updatePhotoNames(); syncToTree(); } });
      showToast('불러오기에 실패했습니다. 작성 중이던 내용을 되돌렸습니다.', 'error');
    } else {
      showToast('불러오기에 실패했습니다. 저장목록의 "최근 정리된 작성분"에서 복구하세요.', 'error');
    }
    return;
  }
  showToast('[' + val + '] 이전 데이터를 불러왔습니다', 'info');
} else {
  // ★ 기존 분기를 그대로 유지한다(삭제 금지)
  restoreDraft(val).then(restored => {
    if (!restored) { updatePhotoNames(); syncToTree(); }
  });
}
```

- **※ 사전 작업 5건 — ①②는 없으면 위 스니펫이 실행 즉시 예외이고, ③④⑤는 함께 넣어야 동작이 완결된다(⑥은 철회됨)**:
  ① **`fmtTime(ms)`가 파일에 존재하지 않는다**(`grep fmtTime` 결과 0건). `renderDraftList()` 안의 지역 화살표 함수 `fmt`(6873행)를 모듈 상수 `fmtTime`으로 끌어올리고 `renderDraftList()`가 그것을 쓰도록 바꾼다. 이 줄은 `try` 밖이라 `ReferenceError`가 나면 `onPlotSelect()`가 통째로 중단되고, `_doReset()`(6061행)이 이미 지난 뒤라 **빈 폼이 남은 채 그 포인트를 영영 열 수 없게 된다.**
  ② **`setSurveyDate(v, opts)` 헬퍼를 새로 만든다** — hidden `soil-date`와 화면 `soil-date-display`(596행)를 함께 세팅한다. **빈 값 처리 규칙을 호출부마다 다르게 둬야 한다**(5차 검토 지적 — 하나로 묶으면 조사일이 조작된다):
    - `_restoreRecordData()`의 `setVal('soil-date', s[3])`(6277행) 교체분 → **`v == null`일 때만 no-op.** 기존 `setVal`의 의미(6270행 `if (el && val != null)`)를 그대로 보존한다. `s[3] === ''`인 레코드에서 falsy no-op을 하면 `_doReset()` → `setTodayDate()`(2856행)가 넣은 **오늘 날짜가 남아 그대로 저장되어**(6593행 `g('soil-date')`), §2가 금지한 "조사일을 사실과 다르게 기록"이 된다. 공란은 공란으로 두어 저장 단계 필수검사에 걸리게 하는 것이 맞다.
    - A-3의 draft 경로(`setSurveyDate(existing.date || existing.soilRow?.[3])`) → **falsy면 no-op**(오늘 날짜 유지). 여기서는 레코드가 깨져 날짜를 못 읽은 경우이므로 화면을 비우지 않는다.
    - 즉 `setSurveyDate(v, { keepTodayWhenEmpty: true|false })` 형태로 두 번째 인자를 두고, 두 호출부의 값을 위와 같이 못 박는다.
    - **표시 포맷 정규화**: 인라인 핸들러(602~609행)와 `setTodayDate()`(2856~2874행)는 모두 `YYYY-MM-DD`를 넣는다. 반면 `fetchFromSheet()`가 넘기는 `data.soilRow[3]`은 Apps Script 직렬화 값이라 ISO 타임스탬프일 수 있는데, 지금은 hidden에만 들어가 눈에 띄지 않지만 헬퍼가 `soil-date-display`까지 세팅하면 화면에 그대로 찍힌다. 헬퍼 안에서 `YYYY-MM-DD` 형태가 아니면 `String(v).slice(0, 10)`으로 정규화하고, **정규화한 같은 값을 hidden `soil-date`와 `soil-date-display`에 동일하게 넣는다.** 표시에만 정규화를 걸면 hidden은 ISO 문자열, 화면은 `YYYY-MM-DD`가 되어 A-4가 없애려던 "화면과 저장값이 갈리는" 비대칭이 되살아난다 — 서버로 실려 나가는 값은 hidden 쪽이다(6593행 `g('soil-date')`). 정상 `YYYY-MM-DD`는 정규식 분기에서 그대로 통과하므로 손상되지 않는다.
  ③ `KEEP_IDS`(2901행)를 모듈 상수로 끌어올린다(A-2의 해제 리스너가 참조).
  ④ **조사지번호 표시 동기화를 `syncPlotNoDisplay(val)` 함수로 뽑는다** — `onPlotSelect()` 6053행의 `syncPlotNoDisplay()`(`inp.value`, `sel.value`, `custom-plot-display`의 텍스트·색·굵기)이 하는 일이다. `_doReset()`이 이 표시를 '포인트 선택'으로 되돌리므로(6696~6697행), A-3의 catch 롤백에서 `soil-plot-no`의 value만 넣으면 **화면에는 포인트가 선택되지 않은 것처럼 보이면서 내부 값만 채워진** 상태가 된다. 롤백 경로에서도 같은 함수를 부른다.
  ⑤ **`fetchFromSheet()`의 `_doReset()`(6217행) 직전에 `if (!_recordEchoGuard) saveDraft();`를 추가한다** — **가드 조건을 반드시 함께 적는다**(6차 검토 지적). 조건 없이 적으면 이 호출이 A-2의 "직접 호출은 막지 않는다" 원칙에 따라 잠금을 통과하고 해제 ②까지 실행해, **레코드를 연 직후 아무것도 건드리지 않고 구글시트 불러오기를 하면 메아리가 그대로 생긴다**(그 시점 `getDraftKey()`는 아직 직전 포인트 X이고, 유예 보관으로 원본이 비어 있어 3겹 가드도 `if (!prev) return true;`로 통과한다). 조건을 붙여도 ⑤의 목적은 그대로 달성된다 — 편집이 있었다면 해제 ①이 이미 가드를 풀어 놓았기 때문이다. 6049행과 같은 형태다. 이 경로만 폼을 날리기 전 저장이 없어서, 지금은 대기 중이던 디바운스가 1초 뒤 초기화된 폼을 저장하는(= 사실상 쓰레기 기록) 상태다. 그런데 A-2 (1)이 `clearTimeout(_draftTimer)`를 넣으면 그 예약이 취소되어 **"입력 → 1초 안에 구글시트 불러오기" 조합에서 마지막 입력분이 결정적으로 사라진다.** A-2가 만드는 손실이므로 같은 차수에서 막는다.
  ⑥ ~~앱 시작 자동복원의 `_default` 제외~~ — **6차 검토에서 채택했다가 7차 검토로 철회했다. 구현하지 말 것.** `survey_draft__default`는 빈 슬롯 전용이 아니다. `REQUIRED_SOIL`(2993~3025행)의 침식·퇴적·풍화·토심·토색·건습도 등 대부분이 조사지번호와 무관하게 입력 가능하고, 그 필드들에 자동저장 리스너가 걸려 있으며(3092~3093행), `getDraftKey()`는 번호가 없으면 `_default`로 떨어진다(1912~1913행). 즉 **포인트 목록을 아직 못 받았거나 번호를 나중에 고르려는 사용자가 입지 항목부터 입력하면 그 작업이 통째로 `_default`에 쌓인다.** 그 내용의 복구 경로는 앱 시작 자동복원 **하나뿐**이다 — 1겹 목록은 이미 `_default`를 제외하므로(6834행) 목록에도 뜨지 않는다. 제외를 넣으면 ① 자동복원에서 빠져 화면에 안 뜨고 ② 목록에도 없어 존재를 알 수 없고 ③ 사용자가 포인트를 하나 고르는 순간 `_doReset()`(6061행) → `clearDraft()`(6671행)가 `survey_draft__default`를 지워 **영구 손실**이 된다. ⑥이 겨냥한 증상(빈 `_default`가 복원되며 토스트가 뜬다)은 사용자 오인일 뿐 손실이 아니므로, 위험의 비대칭이 명확하다. §7에 알려진 한계로만 남긴다.
- **`saveData()`의 `clearDraft()`(4231행)는 그대로 둔다.** 초판은 이것을 `trashDraft()`로 바꾸려 했으나 **철회한다**(§6.3의 구현 항목도 이에 맞춰 수정됨). 이유는 두 가지다. ① 저장이 성공한 시점의 draft는 그 내용이 이미 레코드에 반영됐으므로 보관 가치가 낮다. ② 그대로 두면 "보관본을 나중에 덮어쓰는" 충돌과 `plotNo`/`getDraftKey()` 간 trim 불일치가 **구조적으로 발생하지 않는다.** 결과적으로 유예 보관을 쓰는 곳은 선택 시점 한 군데뿐이다.
- **`_doReset()`의 `clearDraft()`(6671행)도 그대로 둔다.** 그 호출은 폼이 이미 비워진 뒤(6656~6659행)라 항상 `survey_draft__default`(빈 임시 슬롯)만 지운다 — 코드로 확인됨.
- **앱 시작 초기화**(2561행 근처, draft 자동복원 **직전**)에 `purgeExpiredTrashedDrafts()` 호출.

**A-4. 조사일은 어느 쪽을 고르든 레코드 값을 유지한다 — hidden과 화면 표시를 함께**

draft에는 `soil-date`가 없다(hidden이라 `saveDraft()` 선택자에서 제외 — §1.2). 그래서 draft 복원 경로에서는 `_doReset()` → `setTodayDate()`(2856행)가 넣은 **오늘 날짜**가 그대로 남는다. 지금까지는 레코드가 있는 포인트에 draft 복원 경로가 도달할 수 없어(6084행 분기) 드러나지 않았지만, **5겹이 그 경로를 처음으로 연다.** 사용자가 고른 것은 "어느 내용을 이어서 쓸까"이지 "조사일을 바꿀까"가 아니므로, 비대칭이 생기면 그것은 버그다.

- **draft를 고른 경로**: A-3의 `setSurveyDate(existing.soilRow[3])`처럼, `restoreDraft()` **전에** 레코드의 조사일을 복원한다.
- **레코드를 고른 경로도 손봐야 한다**: 현재 `_restoreRecordData()`는 `setVal('soil-date', s[3])`로 **hidden만** 복원하고 `soil-date-display`(596행)는 건드리지 않는다. 그 직전 `setTodayDate()`(2856행)가 화면 표시를 오늘로 채워둔 상태라, **화면에는 오늘이 보이고 저장되는 값만 원래 날짜**가 되는 불일치가 이미 존재한다. `_restoreRecordData()`에 `soil-date-display` 동기화를 함께 넣는다.
- 따라서 위 `setSurveyDate(v, opts)`는 **hidden `soil-date`와 `soil-date-display`를 같이 세팅하는 작은 헬퍼**로 만들고, `_restoreRecordData()`도 같은 헬퍼를 쓰게 한다(602~609행의 인라인 핸들러가 하는 일과 동일하게 맞출 것). **빈 값 처리 규칙은 호출부마다 다르다 — A-3의 사전 작업 ②를 반드시 그대로 따를 것.** 한 규칙으로 묶으면 조사일이 오늘로 둔갑하는 경로가 생긴다.
- 레코드가 없는 포인트의 draft 복원은 종전대로 오늘 날짜다 — 이건 §7이 명시한 기존 설계이며 바꾸지 않는다.

**A-5. 저장공간 압박 시 우선순위 — 버려진 사본이 살아 있는 작성분을 밀어내지 않게 한다**

유예 보관은 포인트마다 draft를 최대 2벌 14일간 추가로 점유한다. 그런데 현재 `saveDraft()`의 저장 실패는 `console.warn`으로 끝나(2091행) **사용자가 모른 채 지금 작성 중인 내용이 유실된다.**

- `trashDraft()`는 보관을 시도하기 **전에** `purgeExpiredTrashedDrafts()`를 호출한다(A-1에 반영됨).
- `saveDraft()`의 `catch`에서 저장공간 초과(`e.name === 'QuotaExceededError'` 또는 `'NS_ERROR_DOM_QUOTA_REACHED'`)를 식별하면, **`_trashedAt`이 가장 오래된 유예 보관분부터 제거하고 1회 재시도**한다. 그래도 실패하면 오류 토스트로 알린다(조용한 실패 금지).
- **재시도 범위를 반드시 한 줄로 한정한다**: `saveDraft()`는 함수 본문 전체가 하나의 `try`이므로(1946~1976행), 재시도를 `saveDraft()` 재호출로 구현하면 quota가 풀릴 때까지 재귀가 반복된다. **재시도는 `localStorage.setItem(draftKey, JSON.stringify(draft))` 그 한 줄만 다시 실행하고, `saveDraft()` 재귀 호출은 금지한다.**
- 삭제 순서는 `_trashedAt` 오름차순이다. 같은 포인트에서는 `__prev`가 먼저 지워지는 것이 정상인데, 밀려난 본문이 자기 `_trashedAt`을 그대로 갖고 이동하기 때문이다 — **밀어낼 때 `__prev`에 `_trashedAt`을 새로 찍지 말 것.**
- 즉 **활성 draft > 유예 보관분**이 항상 우선이다. 이 규칙은 저장소를 두 배로 쓰는 §4.4(2겹-B, 4차)에도 그대로 적용된다.

**A-6. 복구 경로(UI)**

1겹 목록 카드 안에 유예 보관분이 있을 때만 "최근 정리된 작성분 N건" 줄을 노출하고, 각 항목에 조사지번호·버려진 날짜·"복구" 버튼을 둔다. 복구는 그 항목의 **실제 키를 그대로 넘겨** `restoreTrashedDraft(trashKey)`를 호출하는 것이며, **같은 번호의 활성 draft가 이미 있으면 확인창으로 덮어쓸지 묻는다**(자동 덮어쓰기 금지 — 3겹의 원칙과 동일).

- **반드시 함께 고칠 것**: 현재 `renderDraftList()`는 활성 draft가 0건이면 카드를 숨기고 즉시 반환한다(6858~6859행 `card.style.display = items.length > 0 ? 'block' : 'none'; if (items.length === 0) { body.innerHTML = ''; return; }`). 그대로 두면 **"저장된 내용"을 고른 직후처럼 활성 draft가 없고 보관분만 있는 상태에서 복구 버튼에 도달할 수 없다.** 표시 조건을 `items.length > 0 || trashedItems.length > 0`으로 바꾸고 조기 반환을 제거한다.
- **표기 ①**: 복구 줄에 "사진은 함께 복구되지 않을 수 있습니다"를 명시한다(사유는 §7 — 사진 원본은 전송 완료 시 IDB에서 삭제되므로 14일을 보장하지 못한다).
- **표기 ②**: `_rawUnparsed` 항목은 "내용 확인 불가"로 따로 표시한다. `restoreDraft()`는 `if (!draft._savedAt) return false;`로 끝나므로(2122행) 깨진 원문은 복구해도 화면에 뜨지 않는다 — 복구 자체는 의미가 있으나(원문 보존) 화면 복원은 기대할 수 없다는 점을 사용자에게 알린다.

##### 대책 B — 구현 후 필수 재현 테스트 (통과 전 커밋 금지)

이 겹은 "표시된 것과 실제 채워지는 것이 어긋나는" 종류의 버그가 코드를 눈으로 봐서는 잘 안 드러난다. 따라서 §6.3의 일반 검증과 별개로, 아래 **14단계**(6-1·6-2 및 13-1~13-4 세부 포함, 총 20항목)를 실제 브라우저에서 **값 단위로 대조**해 통과시키기 전에는 커밋하지 않는다.

1. 임의 포인트 P를 완성해 정식 저장한다(레코드 생성, `savedAtMs` 기록 확인).
2. P를 다시 열어 **식별 가능한 고유값**(예: 특이사항에 `수정본-AAA`)을 넣고 저장하지 않은 채 다른 포인트로 이동한다 → `survey_draft_P`의 `_savedAt`이 레코드의 `savedAtMs`보다 큰지 값으로 확인한다.
3. P를 다시 연다 → 선택 화면이 뜨는지, **"확인 = 작성 중이던 내용 / 취소 = 저장된 내용"** 매핑이 §4.6 목업과 일치하는지 확인한다(라디오 기본선택 개념은 없다 — 확정 UI는 `confirm()`이다).
4. **"작성 중이던 것"을 고른 경우**: ① 폼의 특이사항이 정확히 `수정본-AAA`인지, ② **조사일이 1번에서 저장한 원래 날짜인지 — hidden `soil-date` 값과 화면 `soil-date-display` 표시가 둘 다**(A-4), ③ **빠른선택 버튼으로 침식상태를 바꾼 뒤** 1초 지나 `survey_draft_P`가 갱신되는지(A-2의 합성 이벤트 경로 — 여기서 텍스트 한 글자만 쳐보면 이 결함을 놓친다), ④ **숫자 선택 시트로 토심B를 바꾼 뒤**에도 같은지 확인한다.
5. 되돌아가 이번엔 **"저장된 내용"을 고른다**: ① 폼의 특이사항이 `수정본-AAA`가 **아닌** 원래 저장값인지, ② 조사일이 hidden·화면 모두 원래 날짜인지, ③ 그 즉시 유예 보관본이 생겼고 그 안의 특이사항이 **정확히 `수정본-AAA`인지 값으로** 확인한다("보관본이 존재하는지"만 보면 메아리가 보관돼도 통과해버린다), ④ `survey_draft_P`는 비었는지 확인한다.
6. 5번 상태에서 **아무것도 입력하지 않고 다른 포인트로 이동**한다(= `onPlotSelect()`의 `saveDraft()`가 도는 경로) → ① 보관본이 여전히 `수정본-AAA`인지, ② **`survey_draft_P`가 다시 생기지 않았는지**(메아리 미생성 — A-2) 확인한다.
6-1. 5번 상태를 다시 만든 뒤 **아무것도 입력하지 않고 앱을 백그라운드로 보냈다가 돌아온다**(다른 앱 전환·홈 버튼·화면 잠금) → `survey_draft_P`가 생기지 않았는지 확인한다. **3205행 경로(A-2 (2)②)는 이 단계로만 잡힌다** — 현장에서 가장 흔한 이탈 형태다.
6-2. 포인트 A의 필수 필드에 한 글자 입력하고 **1초가 지나기 전에** 레코드가 있는 포인트 B를 선택한다 → ① A의 draft는 정상 저장됐는지, ② **B의 draft(`survey_draft_B`)는 생기지 않았는지** 확인한다(예약된 디바운스 타이머가 잠금을 우회하는 경로 — A-2 (2)③).
7. 5번 상태를 다시 만든 뒤, 이번엔 **저장목록 탭에서 전체선택 체크박스를 만지고** 돌아와 다른 포인트로 이동한다 → `survey_draft_P`가 생기지 않았는지 확인한다(가드가 무관한 조작에 풀리지 않는지 — A-2의 "너무 헐거운 쪽").
8. 5번 상태에서 **사진 1장을 삭제**한다 → ① `survey_draft_P`가 **즉시** 갱신되는지(사진 경로는 `saveDraft()`를 직접 부르므로 막히면 안 된다 — A-2 (2)), ② 그 뒤 새로고침했을 때 지운 사진이 되살아나지 않는지 확인한다. **이동사진 임시저장** 버튼도 같은 방식으로 확인한다(성공 토스트만 뜨고 저장은 안 되는 상태가 아닌지).
9. **P를 세 번째로 연다**: ① 메아리가 없으므로 **선택 화면이 뜨지 않고** 레코드가 곧바로 열리는지, ② 보관본이 여전히 `수정본-AAA`인지, ③ 저장목록 탭에서 P가 "작성 중" 목록에 남아 있지 않은지, ④ 앱을 새로고침했을 때 앱 시작 자동복원이 메아리를 집어오지 않는지 확인한다.
10. 목록에서 "복구"를 눌러 `survey_draft_P`가 되살아나고 특이사항이 `수정본-AAA`인지 확인한다. **활성 draft가 0건인 상태에서도 복구 줄이 보이는지**(A-6)를 먼저 확인하고, 활성 draft가 이미 있는 상태에서 복구를 누르면 확인창이 뜨는지도 확인한다.
11. 저장공간 부족을 흉내 내어(`localStorage.setItem`을 일시적으로 throw하게 바꿔) 5번을 다시 시도한다 → ① 레코드 복원이 일어나지 않고 오류 토스트가 뜨는지, ② `survey_draft_P`의 `수정본-AAA`가 그대로 남아 있는지, ③ **폼이 빈 화면이 아니라 `수정본-AAA`가 보이는 상태인지**(❹) 확인한다.
12. `_restoreRecordData`를 일시적으로 throw하게 만들어 5번을 시도한다 → ① 롤백이 돌아 `survey_draft_P`가 되살아나고 오류 토스트가 뜨는지, ② **폼에도 `수정본-AAA`가 보이는지**, ③ 그 상태로 다른 포인트로 이동한 뒤에도 `survey_draft_P`가 `수정본-AAA` 그대로인지(반쯤 채워진 레코드 조각에 덮이지 않는지 — 3차 검토 지적) 확인한다.
13. **하위 호환**: `savedAtMs`가 없는 옛 레코드로 열어본다 → ① 선택 화면 없이 종전대로 레코드가 곧바로 열리는지, ② 그럼에도 **그 포인트의 draft가 유예 보관소에 보관됐는지**(A-0) 확인한다. 이어서 **설정탭의 구글시트 불러오기(`fetchFromSheet()` 6223행)** 로 폼을 채운 뒤 다른 포인트로 이동해 메아리가 생기지 않는지도 확인한다(A-2 (1)의 함수 말미 설정이 두 경로를 모두 덮는지).
13-1. 어떤 포인트에 값을 입력하고 **1초가 지나기 전에 구글시트 불러오기**를 실행한다 → 불러오기 직전에 입력한 값이 그 포인트의 draft에 **남아 있는지** 확인한다(사전 작업 ⑤가 없으면 `clearTimeout`이 예약을 지워 확정적으로 사라진다). 이어서 **레코드를 연 직후 아무것도 건드리지 않고** 불러오기를 실행한다 → 그 포인트의 draft가 **생기지 않는지** 확인한다(사전 작업 ⑤의 `if (!_recordEchoGuard)` 조건이 실제로 붙었는지).
13-2. **draft가 없는 포인트**에서 `_restoreRecordData`가 throw하도록 만들어 연다(`trashed === ''` 분기) → ① 조사지번호가 비워지는지, ② 그 상태로 저장을 눌러도 **기존 레코드가 빈약한 내용으로 덮이지 않는지** 확인한다.
13-3. `soilRow`도 `date`도 없는 **깨진 레코드**에서 "작성 중이던 내용"을 고른다 → 예외 없이 draft가 정상 복원되고 조사일은 오늘로 유지되는지 확인한다.
13-4. **`soilRow[3]`이 빈 문자열인 레코드**를 "저장된 내용"으로 연다 → 조사일이 **공란으로 남는지**(오늘로 채워지지 않는지) hidden·화면 양쪽 값으로 확인하고, 저장을 누르면 필수검사에 걸리는지 확인한다(사전 작업 ②의 규칙 분리가 실제로 지켜졌는지).
14. **회귀(가장 치명적인 단순 실수 유형)**: 유예 보관분이 있는 상태로 앱을 새로 열어 `purgeExpiredTrashedDrafts()`를 태운 뒤, **기존 활성 draft가 하나도 사라지지 않았는지** 개수와 값으로 확인한다. 아울러 깨진 JSON을 `survey_draft_Q`에 심어두고 Q의 레코드를 열어 → **Q가 정상적으로 열리는지**(❺) 확인한다.

---

### 4.7 [6겹] 이력 정보 보존 — 3차

**의도**: `saveDraft()`가 `_savedAt`(최종)에 더해 `_firstEditedAt`(최초 작성 시각)을 기록한다. 이미 있으면 보존하고, 없을 때만 현재 시각으로 채운다.

```js
// saveDraft() 안, draft._savedAt = Date.now(); 직전에 추가
let prevFirst = null;
try { prevFirst = JSON.parse(localStorage.getItem(getDraftKey()) || 'null')?._firstEditedAt || null; }
catch (e) { prevFirst = null; }
draft._firstEditedAt = prevFirst || Date.now();
draft._savedAt = Date.now();
```

**중요**: 이 값은 **화면 표시(1겹 목록·5겹 선택지) 전용**이다. 서버로 전송되는 조사일(`soilRow[3]`)에는 어떤 영향도 주지 않는다. 조사일은 지금처럼 저장 버튼을 누른 날짜로 기록된다.

**장점**: 1·5겹의 표시 품질을 직접 끌어올린다.
**단점**: 단독 효용은 작다.
**상호보완**: 1겹의 그릇에 담기는 내용물. 이 겹이 없으면 1겹 목록은 번호 나열, 5겹 선택지는 근거 없는 양자택일이 된다.

---

### 4.8 [7겹] 수명 관리 — 2차

**의도**:
1. 저장 완료 시 해당 draft가 확실히 제거되는지 검증한다(현재 `saveData()` 4231행에서 `clearDraft()` 호출 중 — 동작 확인 필요).
2. 1겹 목록에서 각 항목에 "작성 취소(삭제)" 버튼을 제공한다. **확인 창을 반드시 거치고**, 자동 삭제는 하지 않는다.
3. 이미 정식 저장이 끝났는데 남아 있는 잔재 draft는 목록에서 "정리 가능"으로 표시한다.

**장점**: 위험 H 해결, C를 간접 완화. 목록의 신호 대 잡음비 개선.
**단점**: 잘못 지우면 복구 불가.
**상호보완**: 삭제 직전에 8겹(내보내기)을 권하는 흐름을 넣어 백스톱을 만든다. 또한 2겹-B의 용량 증가를 상쇄한다.

---

### 4.9 [8겹] 내보내기 / 가져오기 — 4차

**의도**: 미완료 draft(사진 포함 여부는 선택)를 파일로 내보내고 다시 불러온다. 기존 ZIP 백업 흐름(`downloadPhotosAsZip()`)에 편입할 수 있다.

**장점**: 위험 C·G를 근본 해결하는 유일한 수단. 기기 교체·초기화·브라우저 데이터 삭제 등 앱이 손쓸 수 없는 상황의 탈출구.
**단점**: 수동 조작 필요. 사진 포함 시 파일이 크다.
**상호보완**: 1~7겹이 모두 실패한 극단 상황 전용. 평상시엔 존재감이 없어야 정상이며, 7겹의 삭제 직전 흐름에 끼워 넣을 때만 일상적으로 쓰인다.

#### 4.9.1 상세 설계 (4차 착수용 · **4차 검토 반영판 — 구현 착수 승인**)

**E-0. 이 겹의 성격 — 다른 겹과 근본적으로 다르다**

1~7겹은 전부 **앱 안에서 자동으로** 도는 장치다. 8겹만 유일하게 **사용자가 손으로 실행하고, 데이터가 앱 밖으로 나간다.** 그래서 위험의 종류도 다르다 — 잃는 사고가 아니라 **새는 사고**와 **섞이는 사고**를 조심해야 한다.

- 내보낸 파일에는 조사 현장 데이터가 그대로 들어 있다. 카톡·메일로 옮기는 순간 앱의 통제 밖이다.
- 다른 조사원의 파일을 가져오면 **남의 조사 내용이 내 기기에 섞인다.** 위험 G를 해결하려다 만드는 새 위험이다.

따라서 이 겹의 설계 원칙은 **"조용히 하지 않는다"**이다. 내보낼 때도 가져올 때도 무엇이 오가는지 화면에 먼저 보여준다.

**E-1. 내보내기 — 텍스트만이면 JSZip을 쓰지 않는다**

- **대상**: 활성 draft(`survey_draft_*`, `_default` 포함) 전부. 유예 보관본은 포함하지 않는다(B-5와 같은 이유).
- **형식 두 가지**:
  - **사진 미포함(기본)** → `.json` 파일 하나를 그대로 내려받는다. **의존성이 없다.**
  - **사진 포함(선택)** → JSZip으로 `.zip`. `drafts.json` + `photos/<조사지번호>/<파일명>`.

**JSZip은 오프라인에서 없다.** CDN에서 로드되고(479행) 서비스워커는 자기 origin이 아닌 요청을 아예 처리하지 않으며(`sw.js` 27행) 캐시 목록에도 없다(`sw.js` 3~6행). 8겹은 §4.9가 "앱이 손쓸 수 없는 상황의 **탈출구**"라고 선언한 장치인데 탈출구가 네트워크에 의존하면 설계 모순이다. 그래서:

1. **기본 경로(텍스트만)를 JSZip 없이 만든다** — 이러면 비행기 모드에서도 내보내기가 동작한다. 현장에서 정말 필요한 건 텍스트다(사진은 어차피 수십 MB라 모바일로 주고받기 어렵다).
2. 사진 포함 경로에는 **진입 가드**를 둔다: `if (typeof JSZip === 'undefined') { showToast('사진 포함 내보내기는 온라인 상태에서만 가능합니다', 'error'); return; }`. 가드가 없으면 버튼을 눌러도 `ReferenceError`만 나고 화면은 무반응이다.
3. `sw.js`에 JSZip을 프리캐시하는 것은 **4차 범위에 넣지 않는다.** 캐시 버전(`watermap-v100`) 갱신을 동반해 앱 전체의 캐싱 동작에 영향을 주므로, 별도 작업으로 분리하고 그때 따로 검증한다.

**내보내기 전에 요약을 보여준다**: "작성 중 3건(5번·12번·번호없음 1건)을 내보냅니다. 사진 포함: 아니오. 예상 크기 약 40KB." 무엇이 나가는지 모른 채 파일을 만들지 않게 한다.

```js
// drafts.json (zip 안이든 단독 .json이든 동일 스키마)
{
  "format": "watermap-draft-export",   // 고정 문자열. 이게 없으면 가져오기 거부
  "version": 1,                        // 모르는 version은 거부
  "exportedAt": 1789285890599,
  "sourceTeam": "<team_code 또는 빈 문자열>",  // 섞임 경고용. 차단 기준은 아니다
  "drafts": { "<조사지번호>": { ...draft 본문 그대로... } }
}
```

**E-2. 가져오기**

가져오기는 **덮어쓰기 사고가 일어나는 유일한 경로**다. 3겹의 원칙을 그대로 적용한다 — **자동 덮어쓰기 금지.**

1. `format`/`version`을 확인한다. 맞지 않으면 **아무것도 하지 않고** 이유를 알린다("이 앱에서 내보낸 파일이 아닙니다").
2. **적용 전에 목록을 보여준다.** 각 항목에 조사지번호, 작성 시각, 채워진 항목 수, 그리고 현재 기기 상태와의 관계:
   - `새 항목` — 같은 번호의 draft도 레코드도 없음
   - `덮어씀` — 같은 번호의 활성 draft가 이미 있음 (양쪽 시각·항목 수를 나란히)
   - `레코드 있음` — 이미 정식 저장된 번호. 가져오면 **5겹 선택 화면이 뜨게 된다**(아래 3번의 시각 갱신 덕분에 반드시 뜬다)
3. **`_savedAt`을 가져온 시각(`Date.now()`)으로 갱신하고, 원래 값은 `_importedSavedAt`에 보존한다.** `_firstEditedAt`은 원본 그대로 둔다(6겹의 "시작 → 작성 중" 표시가 깨지지 않게).
   - 이유: 가져온 `_savedAt`은 **내보낸 기기의 시계**로 찍힌 값이다. 기기 교체가 이 기능의 주 용도인데, 새 기기 레코드의 `savedAtMs`가 더 크면 5겹도 안 뜨고(6093~6096행) 1겹 목록에도 안 나타나(6841~6845행) **가져왔다는 안내만 받고 어디서도 볼 수 없는** 상태가 된다.
4. **항목별 체크박스**, 기본값은 `새 항목`만 켠다. 덮어쓰는 항목은 사용자가 직접 켜야 한다.
   - **`_default`는 예외다**: 대상 기기에 `survey_draft__default`가 이미 있으면 **체크박스를 비활성**하고 절대 덮지 않는다. `_default`는 1겹 목록에 뜨지 않고(6834행) 유예 보관도 거치지 않아 **되돌릴 방법이 전혀 없다.** 굳이 가져와야 하면 `survey_draft__imported_<타임스탬프>` 같은 별도 키로 넣어 목록에 보이게 한다.
5. `sourceTeam`이 현재 `team_code`와 다르면 **눈에 띄게 경고한다** — "다른 조 파일입니다. 남의 조사 내용이 섞일 수 있습니다." 차단하지는 않는다(기기 교체 시 팀코드가 비어 있을 수 있다).
6. 적용은 `localStorage.setItem` + **B-2의 `mirrorDraftToIDB()`를 함께** 부른다. 미러를 빠뜨리면 다음 실행의 병합이 한쪽만 보게 된다.

**E-2-1. 사진 복원 — 슬롯 키를 직접 조립하지 않는다**

```js
// resolvePhotoSlot()(1859행)은 일반/이동을 구분해 서로 다른 키 생성기를 이미 골라준다.
// 반환된 slot.slotOrigKey만 쓴다.
const slot = resolvePhotoSlot(plotNo, filename);
if (!slot) { /* 슬롯 식별 불가 — 건너뛰고 '사진 N장을 복원하지 못했습니다'에 집계 */ }
else await idbSet(slot.slotOrigKey, { base64, filename, captureId });
```

- **`resolvePhotoSlot()`이 돌려준 `slotIdx`를 이동사진에 대해 `idbOrigKey()`로 조립하지 말 것.** 이동사진의 `slotIdx`는 0~3인데 그걸 `idbOrigKey`에 넣으면 `orig_<plotNo>_0`~`_3`, 즉 **일반사진 앞 4장(C_울폐도·N_8M·N_4M_울폐도·E_8M)의 원본 키를 덮어쓴다.** 가져오기 한 번으로 일반사진 4장이 이동사진으로 교체된다(1차 검토 지적). (아래 배열 기준 방식을 따르면 이 실수 자체가 발생할 여지가 없다.)
- **`slot.cacheKey`(`photo_N`/`move_photo_N`)에는 절대 쓰지 않는다.** 그건 현재 작업 중인 조사지 1개 전용 전역 스크래치 캐시라, 가져오기가 쓰면 지금 작업 중인 다른 조사지의 업로드용 원본이 오염된다.
- **저장 값의 형태는 `{ base64, filename, captureId }`** 로 고정한다. 읽는 쪽이 전부 객체를 기대한다(2188행 `stored.base64`, 2323행 `cached.captureId`). **`captureId`는 가져오기 시 새로 만든다** — draft 본문에는 `captureId`가 없고(2079행) 내보내기 스키마에도 실을 자리가 없으므로, "원본에서 그대로 싣는다"는 애초에 불가능하다. 새로 만들어도 무해한 이유는 `captureId`가 **보조 캐시 폴백을 신뢰할지** 판정할 때만 쓰이는데(2176~2180·2323행), 가져오기는 원본 키(`slotOrigKey`)에 쓰므로 복원(2174행)도 유실 집계도 원본을 먼저 찾아 그 판정 경로를 타지 않기 때문이다.
- **슬롯 판정은 draft 본문을 1차 기준으로 한다 — 어느 배열에서 나왔는지가 곧 일반/이동 구분이다.** draft에 이미 인덱스가 들어 있고(2079행 `_photoKeys = [{k, filename}]`), 복원 쪽도 똑같이 하고 있다(2174행 `idbGet(idbOrigKey(plotNo, k))`, 2204행 `idbGet(idbMoveOrigKey(plotNo, k))`).
  → **`_photoKeys` 항목은 `idbOrigKey(plotNo, k)`, `_movePhotoKeys` 항목은 `idbMoveOrigKey(plotNo, k)`로 쓴다. 파일명 역산이 애초에 필요 없다.** `resolvePhotoSlot()`은 **결과가 일치하는지 대조하는 데만** 쓰고, `slot.isMove`가 배열과 어긋나거나 `null`이면 그 장은 건너뛰고 건수에 집계한다.
  (위 스니펫의 `slot.slotOrigKey` 방식은 파일명만 있고 draft 본문이 없을 때의 폴백이다. 두 지시가 충돌하는 것처럼 읽히면 **배열 기준이 우선**이다 — 파일명 역산만 쓰면 `PHOTO_SLOTS` 라벨이 앱 버전 간에 달라졌을 때 `null`이 나와 사진이 조용히 누락된다.)
- `null`이 나온 건수는 반드시 사용자에게 합계로 알린다(조용한 누락 금지).

**E-3. 배치와 조작 경로**

설정 탭에 "미완료 작업 내보내기 / 가져오기" 한 묶음으로 둔다. 저장목록 탭(1겹 카드)에 두지 않는 이유는, 그 카드가 **현장에서 수시로 보는 화면**이라 파일 조작 버튼이 섞이면 오조작 위험이 커지기 때문이다. 8겹은 "평상시엔 존재감이 없어야 정상"인 장치다.

가져오기는 `<input type="file" accept=".json,.zip">`으로 받는다. 파일 선택 자체가 명시적 행위이므로 별도 확인창을 겹치지 않고, E-2의 목록 화면이 확인 역할을 한다.

---

## 5. 위험 ↔ 안전장치 대응표

| 위험 | 1차 방어 | 2차 방어 | 3차 방어 |
|---|---|---|---|
| A 잊어버림 | **1겹 목록** | 6겹 이력 표시 | 7겹 잡음 제거 |
| B 1초 공백 | **2겹-A flush** | 3겹(빈 내용 차단) | — |
| C 저장소 유실 | **2겹-B 이중 저장** | 7겹 용량 관리 | **8겹 내보내기** |
| D 수정분 묻힘 | **5겹 우선순위** | 6겹 시각 근거 | 1겹 목록 노출 |
| E 덮어쓰기 | **3겹 가드** | **유예 보관(§4.6.1 대책 A)** | 8겹 |
| F 사진 유실 | **4겹 검증** | 1겹 상태 표시 | 8겹 |
| G 기기 교체 | **8겹** | — | — |
| H 축적 | **7겹** | 1겹 가시화 | — |

**위험 E에서 2겹-B를 뺀 이유(4차 검토)**: 2겹-B는 localStorage에 쓴 **바로 그 문자열을 같은 순간에** 미러하므로 두 저장소는 항상 같은 버전이다. 미러가 "이전 버전"을 들고 있는 경우는 미러 쓰기가 실패했을 때뿐이고, 그때조차 B-4의 "최신 우선"이 그 이전 버전을 버린다. 즉 덮어쓰기 사고에 대한 2겹-B의 방어력은 **0**이며, 오히려 덮어쓰기를 두 저장소에 동시에 전파한다. 실제로 그 역할을 하는 것은 3차의 유예 보관이다.

**반드시 함께 가야 하는 3쌍**
1. **2겹-A ↔ 3겹**: 저장을 강화할수록 잘못된 내용을 저장할 위험도 커진다.
2. **2겹-B ↔ 7겹**: 이중 저장은 용량을 쓰고, 용량 압박은 유실을 부른다.
3. **1겹 ↔ 4·6겹**: 목록은 그릇, 무결성 상태와 이력은 내용물.

---

## 6. 구현 순서와 검증 계획

각 차수는 독립 배포 가능하며, 앞 차수 없이 뒤 차수만 적용해도 깨지지 않게 한다.

### 6.1 1차 — 1겹 + 2겹-A + 3겹

**구현 순서**: 3겹(가드) → 2겹-A(flush) → 1겹(목록). 가드를 먼저 넣어야 flush 강화가 사고를 키우지 않는다.

**검증**:
- [ ] 5번에 일부 입력 후 6번으로 전환 → `survey_draft_5`에 내용이 남는다
- [ ] 앱을 완전히 새로고침 → 두 draft 모두 localStorage에 살아 있다
- [ ] 저장목록 탭에 "작성 중 2건"과 각 포인트의 완성도가 보인다
- [ ] 목록에서 5번을 누르면 5번이 복원된다
- [ ] 5번 화면에서 탭을 백그라운드로 보냈다가 돌아와도 직전 입력이 살아 있다(flush 확인)
- [ ] `_doReset()` 직후 자동저장이 돌아도 기존 draft가 빈 내용으로 덮이지 않는다(가드 확인)
- [ ] 5번을 마저 채워 저장하면 목록에서 5번이 사라진다
- [ ] **회귀**: 저장·전송·사진 촬영·초기화 등 기존 동작에 변화가 없다

### 6.2 2차 — 4겹 + 7겹

**검증**:
- [ ] IDB에서 사진 원본을 강제로 지운 뒤 draft를 불러오면 유실 경고가 뜨고 해당 슬롯이 강조된다
- [ ] 1겹 목록에도 유실 건수가 표시된다
- [ ] 목록의 "작성 취소"는 확인 창을 거친 뒤에만 삭제된다
- [ ] 정식 저장 완료 후 해당 draft가 목록에서 사라진다

### 6.3 3차 — 5겹 + 6겹

**구현 항목(2차 검토에서 추가)**: 5겹·6겹 본연의 작업 외에, **§4.1(1겹)의 목록 수집 제외 조건을 `savedAtMs` 기준으로 갱신**하는 작업을 반드시 포함한다 — "정식 레코드가 있으면 무조건 제외"(1~2차의 임시 조건)를 "레코드의 `savedAtMs`보다 draft의 `_savedAt`이 최신이면 목록에 포함"으로 바꾼다. 이 항목이 빠지면 아래 마지막 검증 항목("9/12 시작 → 9/13 작성 중" 표시)이 통과할 수 없다.

**구현 항목(착수 전 추가 설계에서 확정)**: §4.6.1의 **대책 A(유예 보관)를 5겹과 같은 차수에 함께 구현한다.** 선택 UI만 만들고 유예 보관을 빼면, 잘못 고른 경우 되돌릴 방법이 없어 이 겹이 오히려 손실 위험을 키운다. 구체적으로 `trashDraft()`/`restoreTrashedDraft()`/`purgeExpiredTrashedDrafts()` 추가, A-2의 **레코드 메아리 방지 가드** — 암묵적 저장 경로 정리(3205행 `saveDraft()` **호출 제거** + 디바운스 **콜백** 가드 + `flushDraftNow()`·`onPlotSelect()` 잠금, **최종 잠금 지점 3곳**), 사전 작업 5건(`fmtTime` 모듈 상수화 · `setSurveyDate(v, opts)` 헬퍼 신설 · `KEEP_IDS` 모듈 상수화 · `syncPlotNoDisplay()` 추출 · `fetchFromSheet()`의 `_doReset()` 앞 `if (!_recordEchoGuard) saveDraft()` 추가). **앱 시작 자동복원의 `_default` 제외는 철회됐다 — 넣지 말 것**(사유는 §4.6.1 사전 작업 ⑥), `onPlotSelect()` 레코드 분기 교체(A-3), A-4의 조사일 복원, A-5의 저장공간 우선순위, 앱 시작 시 만료 정리 호출, 1겹 목록의 복구 UI와 카드 표시 조건 수정(A-6)까지가 한 묶음이다. **`saveData()`의 `clearDraft()`(4231행)는 교체하지 않는다** — 초판의 그 지시는 §4.6.1에서 철회됐다.

**커밋 전 필수 관문**: §4.6.1의 **대책 B(재현 테스트) 14단계(세부 포함 총 20항목)를 전부 통과하기 전에는 커밋하지 않는다.** 아래 일반 검증 항목과 별개로 반드시 수행한다.

**검증**:
- [ ] 저장된 포인트를 열어 일부 수정 후 다른 포인트로 이동 → 다시 그 포인트를 열면 선택지가 뜬다
- [ ] 선택지에 저장 시각과 작성 시각이 각각 표시된다
- [ ] `savedAtMs`가 없는 기존 레코드는 종전대로 정식 레코드가 우선된다(하위 호환)
- [ ] **§4.1 제외 조건 갱신 후**, 정식 레코드는 있지만 그보다 최신 draft가 있는 포인트가 1겹 목록에 다시 나타난다 — "9/12 시작 → 9/13 작성 중"이 표시된다

### 6.4 4차 — 2겹-B + 8겹

**구현 항목**: §4.4.1의 B-1~B-6(미러 키·쓰기·**삭제 전수 처리**·병합 타이브레이크·보관본 제외·용량 대응)과 §4.9.1의 E-1~E-3(내보내기·가져오기·배치). **B-3(삭제 경로 전수 처리)을 빼면 지운 draft가 되살아나므로, B-2만 먼저 넣고 끝내는 분할 구현은 금지한다.** B-4-3의 이상징후 배지를 화면에 실제로 그리려면 **§4.1의 `summarizeDraft()`/`renderDraftList()` 수정**(`mirrorConflict` 필드 + 별도 경고 요소, §4.4.1 B-4-3 참고)도 이 차수에 함께 넣는다 — 빠지면 플래그가 draft 본문에 저장만 되고 화면에는 안 보인다.

**순서**: 2겹-B를 먼저 끝내고 8겹을 붙인다. 8겹의 가져오기가 B-2의 미러 쓰기를 그대로 재사용하므로(E-4의 5번), 미러가 없는 상태에서 가져오기를 만들면 나중에 두 군데를 고쳐야 한다.

**검증** (1차 검토에서 7항목 추가 — 기존 항목만으로는 절반 이하만 잡혔다):

*2겹-B 본체*
- [ ] localStorage의 draft만 강제로 지워도 IDB 미러에서 복구된다
- [ ] **미러에만 있는 draft가 1겹 목록에 나타나고**, 눌러서 복원된다(B-1의 `idbKeys()` + B-4-1의 앱 시작 승격이 실제로 도는지 — 이게 없으면 "복구된다"가 화면 어디에도 나타나지 않는다)
- [ ] 미러에만 있는 draft가 **앱 시작 자동복원으로 폼에 채워지고** "이전 작업 내용이 복원되었습니다" 토스트가 뜬다(B-4-2의 호출 규약이 실제로 도는지 — 1겹 목록은 나중에 렌더되므로 목록 항목만으로는 이 결함이 통과한다)
- [ ] **IDB를 쓸 수 없는 환경에서도 앱이 정상 기동한다** — 사파리 프라이빗 모드, 같은 앱을 두 창으로 열어 둔 상태(`onblocked`)에서 포인트 목록·온라인 상태·팀코드가 모두 뜬다(B-4-2가 만든 신규 위험)
- [ ] 두 저장소가 어긋난 채로 재시작해도 **확인창이 한 번도 뜨지 않고**, 탈락한 쪽이 "최근 정리된 작성분"에서 복구 가능하다(B-4-3)
- [ ] **정식 레코드가 있는 포인트**에서 localStorage draft만 지운 뒤 그 포인트를 열면, 승격이 끝나 있어 **5겹 선택 UI가 정상적으로 뜬다**(B-4-1의 2번 사유 — 안 되면 레코드가 폼을 덮고 메아리가 미러까지 없앤다)
- [ ] 두 저장소가 어긋날 때 **최신 쪽이 채택된다**(타이브레이크 확정 규칙)
- [ ] 이상징후일 때만 **탈락한 쪽이 "최근 정리된 작성분"에 보관된다** — 사진 여러 장 삭제·임목 행 다수 삭제 같은 **정상 삭제로는 보관본이 생기지 않는다**(B-4의 0.5 문턱 실측. 확인창은 어떤 경우에도 뜨지 않는다 — B-4-3)

*삭제 내구성 (이 겹의 최대 위험)*
- [ ] **삭제 전수 회귀**: `clearDraft()`·"작성 취소"·`trashDraft()` 각각으로 지운 draft가 다음 실행에서 되살아나지 않는다
- [ ] **강제 종료 회귀**: 위 각 삭제 **직후 앱을 강제 종료**하고 재실행해도 되살아나지 않는다(묘비 방식이 실제로 동작하는지 — 정상 종료만 시험하면 이 버그를 못 잡는다)
- [ ] 포인트를 여러 번 전환한 뒤 재실행했을 때 **버려진 `_default`가 자동복원되지 않는다**(`_doReset()`→`clearDraft()`가 가장 잦은 삭제 경로다)
- [ ] `restoreTrashedDraft()`로 되살린 draft가 미러에도 다시 쓰인다

*8겹*
- [ ] 내보내기 전에 무엇이 나가는지 요약이 보인다(건수·사진 포함 여부·예상 크기)
- [ ] **비행기 모드**에서 텍스트만 내보내기가 **동작한다**(JSZip 없이), 사진 포함은 명확한 안내가 뜬다(무반응 아님)
- [ ] 내보낸 파일을 다른 브라우저에서 가져오면 목록에 나타난다
- [ ] 같은 번호의 **레코드가 이미 있는 기기**로 가져왔을 때도 1겹 목록에 보이고 그 포인트를 열면 5겹이 뜬다(E-2 3번의 `_savedAt` 갱신 — 시계 차이로 묻히지 않는지)
- [ ] 같은 번호가 이미 있는 항목은 **기본 체크가 꺼져 있고**, 켜야만 덮인다
- [ ] **`_default`는 대상 기기에 이미 있으면 체크 자체가 불가능하다**(되돌릴 방법이 없는 유일한 슬롯)
- [ ] 이동사진을 포함해 내보낸 뒤 가져오면 **일반사진 1~4번 슬롯이 바뀌지 않는다**(E-2-1의 `slotOrigKey` 사용 — 직접 조립하면 덮어쓴다)
- [ ] 슬롯을 식별하지 못한 사진이 있으면 **건수가 사용자에게 보고된다**(조용한 누락 금지)
- [ ] 이 앱과 무관한 파일은 **아무 변화 없이** 거부된다
- [ ] 다른 조 파일(`sourceTeam` 불일치)은 경고가 뜨되 차단되지는 않는다
- [ ] 가져오기로 만든 draft가 **미러에도 쓰였다**

*회귀*
- [ ] 저장·전송·사진 촬영·초기화·3차의 5겹 선택 흐름에 변화가 없다
- [ ] **`fetchFromSheet()`(구글시트 불러오기)** — `saveDraft()` → `_doReset()` → `_restoreRecordData()`를 연달아 타는 유일한 흐름이라 미러 쓰기와 묘비가 수백 ms 안에 교차한다. 불러온 뒤 draft 상태가 정상인지 확인한다

---

## 7. 알려진 한계

- **7일 이상 미사용 시 iOS Safari가 저장소를 정리할 수 있다.** 2겹-B로 완화되지만 완전히 막을 수는 없다. 장기간 방치될 미완료 작업은 8겹(내보내기)으로 대비해야 한다.
- **draft는 기기 로컬이다.** 8겹 없이는 기기 간 이어 작업이 불가능하다.
- **조사일은 여전히 "저장 버튼을 누른 날짜"로 기록된다.** 6겹은 시작 시각을 별도로 보존할 뿐이며, 서버 전송 값은 바꾸지 않는다. 이는 의도된 설계다.
- **1겹의 완성도 표시는 draft 내용 기준의 근사치다.** 사진 실제 존재 여부는 4겹이 적용되기 전까지 반영되지 않는다.
- **3겹의 점수 비교는 "값이 있는 항목 수"라는 단순 기준이다.** 항목 수는 같은데 내용만 나빠진 경우(예: 긴 특이사항을 한 글자로 교체)는 막지 못한다. 이는 의도된 단순화이며, 그런 변경은 사용자의 정상적인 편집으로 간주한다.
- **§4.4(2겹-B)의 타이브레이크는 "최신 우선 + 이상징후 경고"로 확정됐다(사용자 결정).** 상세 규칙은 §4.4.1. 요지: 기본은 `_savedAt`이 큰 쪽을 채택하고, 다만 최신 쪽이 비정상적으로 비어 있으면 자동 채택하지 않고 양쪽을 보여준 뒤 사용자가 고르게 한다. "충실도 우선"을 기본으로 삼지 않은 이유는 **삭제가 되돌려지기 때문**이다 — 사진이나 입력을 지운 뒤 저장하면 "지우기 전" 기록이 항상 더 충실해 보이고, 그러면 5475~5477행 주석이 기록한 "방금 지운 사진이 되살아나는" 버그가 저장소 병합 층에서 되살아난다.
- **§4.6.1의 유예 보관(대책 A)은 draft 쪽만 보호하고, 덮어써지는 이전 레코드 내용은 보관하지 않는다.** 이는 의도된 범위 결정이다 — draft는 레코드를 화면에 띄운 뒤 편집해 만들어지므로 레코드 내용의 상위집합이고(따라서 "draft를 골라 저장 → 이전 레코드 손실"은 실질 위험이 낮다), 반대로 "레코드를 골라 저장 → draft 손실"만이 그 편집분이 어디에도 남지 않는 진짜 손실이기 때문이다. 레코드 스냅샷까지 하려면 `saveData()`의 레코드 쓰기 경로 자체를 건드려야 해서 위험도가 크게 올라가므로 이번 범위에서 제외한다.
- **`survey_draft__default`(조사지번호를 고르기 전에 입력한 내용)의 복구 경로는 앱 시작 자동복원 하나뿐이다.** 1겹 목록은 `_default`를 제외하고(6834행), 포인트를 하나 고르면 `_doReset()` → `clearDraft()`가 그것을 지운다(6061·6671행). 앱 시작 자동복원은 전체 draft 중 `_savedAt`이 가장 큰 1건만 복원하므로, `_default`가 최신이 아니면 그 내용은 사실상 닿을 수 없다. 이를 "자동복원에서 `_default`를 제외"로 고치려던 시도는 **오히려 유일한 복구 경로를 없애 영구 손실을 만들기 때문에 철회했다**(7차 검토). 목록에 노출하는 방향이 옳은 해법이지만, `_doReset()`이 수행사·유역명을 자동으로 채워 "빈 슬롯"과 "진짜 작업"을 내용만으로 가르기 어려워 별도 설계가 필요하다 — 이번 범위 밖으로 남긴다.
- **유예 보관본은 IndexedDB에 미러하지 않는다(§4.4.1 B-5).** 그 결과 **5겹에서 "저장된 내용"을 고른 그 시점부터 탈락한 편집분은 localStorage 단일 사본이 되며, 8겹 내보내기에도 포함되지 않는다** — 이 겹은 그 순간 보호 수준을 스스로 한 단계 낮춘다. localStorage가 통째로 정리되면 보관본은 함께 사라진다. 활성 draft만 2겹-B의 보호를 받는다.
- **내보내기 파일은 앱의 통제 밖이다(§4.9.1 E-0).** 조사 데이터가 그대로 들어 있으므로 공유 경로는 사용자 책임이며, 앱은 무엇이 나가는지 보여주는 것까지만 한다.
- **유예 보관은 포인트당 최신 2건(최신·직전)만 유지한다.** 세 번째부터는 가장 오래된 것이 밀려난다. 무한 증식을 막기 위한 의도된 단순화이며, 2건을 두는 이유는 A-2의 메아리 가드가 뚫렸을 때 진짜 편집분이 한 칸 뒤에 남게 하기 위해서다.
- **유예 보관본의 사진은 14일을 보장하지 못한다.** draft에는 파일명만 있고 실물은 IndexedDB에 있는데, 원본은 ① 전송 성공 확정 직후 삭제되고(`idbDelete(slot.slotOrigKey)` 3379행), ② `deleteRecord()`가 해당 조사지의 16슬롯을 모두 지우며(7440행), ③ 포인트 전환 시 보조 캐시 `photo_N`/`move_photo_N`이 정리된다(6056행). 따라서 이미 전송을 마친 포인트를 복구하면 4겹이 “사진 N장 유실”을 경고하고 저장 단계에서 막힌다. 복구 UI에 이 사실을 표기한다(A-6). 사진까지 14일 보존하려면 IDB 원본의 수명을 바꿔야 하는데, 이는 전송 경로를 건드리는 일이라 이번 범위에서 제외한다.
