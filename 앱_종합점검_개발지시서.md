# watermap-app-personal — 종합 버그 점검 및 보완 계획 (v4)

## 1. 배경과 목표

`watermap_V100.html`(약 6,900줄 단일 HTML 파일, GitHub Pages로 서빙되는 개인 포크)의
`loaded_plots`/`cached_teams` 안전 파싱 작업(커밋 `60281c8`, `c25441a`)이 끝난 뒤,
**그 범위를 벗어나 앱 전체에 다른 버그가 있는지** 두 차례 점검했다 — 한 번은 이 문서를
작성하는 주체(이하 "1차 점검")가, 한 번은 Codex(이하 "2차 점검")가 독립적으로 수행했다.
이 문서는 두 점검 결과를 **실제 코드로 재대조해 검증한 뒤** 하나의 계획으로 합친 것이다.

**중요한 전제 정정**: 2차 점검(Codex)은 코드베이스가 커밋 `60281c8`(loaded_plots/cached_teams
v5 문서의 7개 이슈가 아직 미구현) 상태라고 가정하고 작성됐다. 하지만 **그 7개 이슈는 이미
커밋 `c25441a`로 구현·배포까지 완료됐다**(로컬-배포본 SHA-256 해시 일치까지 확인됨). 그래서
2차 점검의 "우선 수정해야 할 문제 1번"(loaded_plots/cached_teams 7개)은 **이미 해결된
상태**이며, 이번 계획에서 제외한다. 아래 내용은 전부 **커밋 `c25441a` 기준**으로 재검증했다.

**목표**: 두 점검에서 실제 코드로 재확인된 문제만 추려 수정한다. 추측이나 재현 불가능한
항목은 "알려진 한계"로만 기록하고 수정 대상에서 뺀다.

**v2 변경 사항**: 이 문서를 Codex에게 3라운드째 검토받은 결과 이슈 D의 수정 설계에
중요한 허점이 있었다(기존에 이미 공백이 섞여 저장된 `soilRow[6]`이 업로드 시점에
정리되지 않는 문제) — 아래 이슈 D에 반영해 정정했다. 이슈 A는 낮은 위험도로 제외했던
`t[6]/t[7]/t[8]`/`soilRow` 미리보기 값도 "비용이 낮으니 함께 고치라"는 재확인을 받아
구현 범위에 포함시켰다.

**v3 변경 사항**: 4라운드 검토에서 이슈 D의 `.trim()` 수정식이 `rec.soilRow[6]`이
문자열이 아닌 truthy 값(숫자·객체·배열 — 정상 UI로는 생성되지 않지만 손상된
`localStorage`/구버전 데이터에서는 가능)일 때 `TypeError`를 던질 수 있다는 지적을
받아, 문자열 타입 가드를 추가했다.

**v4 변경 사항**: 5라운드 검토에서 v3의 타입 가드가 "예외는 막지만, `soilRow[6]`이
비문자열 truthy 값일 때 유효한 `localStorage.team_code` fallback까지 함께 버린다"는
지적을 받았다 — `||` 체인이 `soilRow[6]`(비문자열이라도 truthy)에서 멈춰버려 fallback
분기 자체에 도달하지 못하기 때문이다. "문자열이고 trim 후에도 비어있지 않은
`soilRow[6]`만 우선 사용, 그 외 전부(비문자열·빈 문자열·공백뿐인 문자열 포함)는
fallback"으로 판단 순서를 재구성했다.

## 2. 범위

**포함**: 아래 5개 이슈(A~E). 전부 커밋 `c25441a` 기준 실제 코드를 읽어 확인했다.

**명시적 제외 — 그리고 왜 제외하는지**:

1. **loaded_plots/cached_teams 7개 이슈** — 이미 커밋 `c25441a`로 완료. (2차 점검이 이걸
   "우선순위 1"로 다시 올렸지만, 이는 2차 점검이 구버전 코드를 기준으로 작업했기 때문 —
   실제 코드에는 이미 없다.)
2. **`flushOfflineQueue()`가 온라인 복구 이벤트에서 호출되어 대기 업로드가 중단된다는
   2차 점검의 주장 — 사실이 아니다.** 실제로 확인한 결과 `online` 이벤트 리스너
   (`watermap_V100.html:2881`)는 `updateOnlineStatus()`와 `syncCompletedPlots()`만
   호출하고, `flushOfflineQueue()`는 파일 전체에서 **자기 자신의 정의 외에 호출하는
   곳이 단 한 곳도 없다**(grep으로 확인). 즉 이 함수는 죽은 코드이고, 이슈 B(아래)에서
   "안전성 보강"으로만 다루지 "실제 업로드 중단을 막는 긴급 수정"으로는 다루지 않는다.
3. **`savedAt` 값이 XSS 벡터라는 2차 점검의 주장 — 사실이 아니다.** `savedAt`은
   `watermap_V100.html:3822`에서 `new Date().toLocaleString('ko-KR')`로 **앱이 직접
   생성하는 값**이며 사용자 입력이 개입할 여지가 없다. 이스케이프 누락 목록에서 뺀다.
4. **(v2에서 "제외"에서 "이슈 A에 포함"으로 변경) 임목 미리보기의 `t[6]`(수종)/`t[7]`
   (흉고직경)/`t[8]`(수고), 입지·토양 미리보기의 `soilRow` 값** — 실제 입력 요소를
   확인한 결과 전부 `<select>` 드롭다운(`soil-erosion`, `soil-color-a`, `soil-type`
   등) 또는 숫자 피커에서만 나오고, `TREE_SPECIES`(`watermap_V100.html:4379`)도 17개
   고정 수종명 배열이라 자유 텍스트 입력 경로가 없다 — 그래서 `plotNo`/`basin`/`date`
   보다 위험도가 낮다는 판단 자체는 유지한다. 다만 3라운드 검토에서 "localStorage의
   기존 레코드, 과거 버전 데이터, devtools, 향후 입력 방식 변경까지 고려하면 완전히
   신뢰 경계 밖은 아니다"는 지적을 받았고, 마침 이슈 A를 구현하는 김에 같은 패턴이라
   비용이 거의 들지 않으므로 **제외하지 않고 이슈 A 구현에 포함**시킨다(아래 이슈 A
   참고).
5. **요청 중 팀 전환 UI race, 다중 탭 `localStorage` 경쟁** — 둘 다 이미 이전 작업에서
   의식적으로 "알려진 한계"로 남기기로 판단한 항목이다(각각 `loaded_plots_잔여이슈_
   개발지시서.md` 7절, 그리고 이 세션 초반의 업로드 재시도 하드닝 작업의 "향후 별도
   고려사항"). 2차 점검도 별도 과제로 분리하는 게 맞다고 재확인했으므로 이번에도
   범위에서 뺀다.
6. **valid JSON이지만 형식이 틀린 캐시(`{}`, `[null]`)가 조용히 빈 목록으로 보이는 문제**
   — `loaded_plots_잔여이슈_개발지시서.md` v5에서 이미 "알려진 한계"로 문서화하고 범위
   제외를 결정한 항목. 재론하지 않는다.

## 3. 현재 상태 (전제 조건)

- 현재 커밋: `c25441a` (원격 `origin/main`에 push 완료, GitHub Pages 배포 확인됨).
- `loaded_plots`/`cached_teams` 관련 9+7=16개 호출 지점은 전부 안전 파싱·정규화·팀
  식별자 trim이 적용된 상태다.
- `survey_records`는 `getSurveyRecords()`(`watermap_V100.html:1782`)가 **최상위 JSON
  파싱 실패만** 방어하고, 배열 내부 개별 원소(`null` 등)는 검증하지 않는다 — 이 특성은
  이번 문서의 이슈 C와 D 둘 다에 영향을 준다.

## 4. 핵심 설계 결정

| # | 위치 | 문제 | 근거 | 수정 방향 |
|---|---|---|---|---|
| A | `renderRecordsList()`의 "임목조사"(`watermap_V100.html:6329` 부근)·"사진촬영"(`6359`) 탭 | 두 탭만 `escapeHtml()`를 빠뜨림. "전체"(`6234`)·"입지·토양"(`6284`) 탭은 이미 적용돼 있어 불일치. (v2) 같은 두 탭의 `t[6]/t[7]/t[8]`(임목 미리보기)·`soilRow` 값(토양 미리보기)도 위험도는 낮지만 함께 처리 | 실제 코드 대조로 확인: `표준지 ${r.plotNo}`, `${r.basin \|\| '유역 미입력'} · ${r.date \|\| '날짜 미입력'}`(임목조사 탭), `${p.filename}`(사진촬영 탭)이 이스케이프 없이 `innerHTML`에 들어감. `plotNo`/`basin`/`date`는 전부 자유 텍스트 입력(`<input type="text">`류)이고, `filename`은 `plotNo + '(슬롯명).jpg'`로 만들어지므로 결국 `plotNo`에 좌우된다. 조사지번호에 `<img src=x onerror=...>` 를 넣고 저장한 뒤 이 두 탭을 열면 스크립트가 실행된다(같은 기기의 로컬 데이터만 보여주는 화면이라 self-XSS 성격이지만, 이미 다른 두 탭엔 있는 방어가 여기만 빠진 것은 명백한 누락). | 두 탭의 `r.plotNo`/`r.basin`/`r.date`/`p.filename`에 `escapeHtml()` 적용 — "전체"/"입지·토양" 탭과 동일한 패턴. 같은 구현 과정에서 `t[6]/t[7]/t[8]`, `soilRow` 미리보기 값에도 `escapeHtml()`을 씌운다(비용 낮음, 위 "명시적 제외" 4번 참고). |
| B | `getQueue()`(`watermap_V100.html:2941`) | `JSON.parse(localStorage.getItem('upload_queue') \|\| '[]')` 무방비 | 현재는 `flushOfflineQueue()`(유일한 소비자)를 호출하는 곳이 없어 **실행되지 않는 죽은 코드**다(위 "명시적 제외" 2번 참고). 다만 향후 이 큐를 다시 쓰게 되거나, 누군가 `getQueue()`를 다른 곳에서 호출하게 될 경우를 대비한 방어적 수정으로 가치가 있다 — 비용이 매우 낮다(한 줄). | `getSurveyRecords()`와 같은 패턴으로 try/catch + `Array.isArray` 검증 추가 |
| C | `survey_records` 소비 지점 7곳(`onPlotSelect()` 제외) | 배열 내부 `null`/비객체 원소에 무방비 | `loaded_plots_잔여이슈_개발지시서.md`에서 이미 grep으로 확인해둔 목록 그대로 유효: `restoreDraft()`(1942), `loadRecord()`(2149), `patchRecord()`(2955), `freshRecord()`(2979), 저장 시 기존 레코드 탐색(3708), 그리고 `deleteRecord`/CSV 내보내기 등(6368, 6584) — `patchRecord`/`freshRecord`는 업로드 재시도(soil/tree/사진 지문 비교)의 핵심 헬퍼라 잘못 건드리면 파급이 크다. | 이번 배치에서 구현하지 않는다 — 별도 지시서로 분리(아래 "7. 별도 과제" 참고). 리스크가 낮고 범위가 좁은 이슈 A/B/D/E와 한 배치에 넣지 않는다. |
| D | `getSoilData()`(`watermap_V100.html:6052`), `autoRetryUpload()`(`3258`), `reuploadRecord()`(`6429`)의 `teamCode` | `loaded_plots`/`cached_teams` v5가 고친 "포인트 조회용 teamCode" trim과 별개로, **업로드 payload에 들어가는 teamCode는 여전히 trim 안 됨**. (v2) 최초 설계는 fallback 분기만 trim해서 **이미 `rec.soilRow[6]`에 공백이 저장된 기존 레코드는 여전히 오염된 채 업로드됨** — 3라운드 검토로 발견, 수정 방향에 반영 | `getSoilData()`가 `localStorage.getItem('team_code') \|\| ''`를 trim 없이 그대로 조사 기록의 `soilRow[6]`(G열 조사책임자)에 저장한다 — 이게 서버로 나가는 실제 payload 필드다. `autoRetryUpload()`/`reuploadRecord()`는 `rec.soilRow[6]`을 우선 쓰고(이미 오염됐을 수 있음), 그마저 없으면 다시 `localStorage.getItem('team_code') \|\| ''`(역시 trim 안 됨)로 폴백한다. **`\|\|`는 첫 번째 truthy 값에서 멈추므로, `rec.soilRow[6]`이 `" A팀 "`처럼 이미 공백을 포함한 채 truthy면 fallback 분기(아무리 trim해도) 자체에 도달하지 않는다** — fallback만 trim하는 최초 설계로는 레거시 레코드가 전혀 정리되지 않는다. `team_code`에 과거 공백이 섞여 저장된 적이 있으면 서버 시트의 G열에 `" A팀 "`처럼 공백 포함 값이 들어가 관리자 쪽 팀별 집계·필터링이 깨질 수 있다. `getSoilData()`의 `localStorage.getItem('team_code') \|\| ''`에 `.trim()` 추가(근본 원인 — `localStorage.getItem()`은 항상 문자열 또는 `null`만 반환하므로 `\|\| ''` 뒤에 바로 `.trim()`해도 안전, 타입 가드 불필요). `autoRetryUpload()`/`reuploadRecord()`는 (v4, 5라운드 검토로 확정) **단순히 `\|\|` 표현식 전체를 감싸는 것만으로는 부족하다** — `soilRow[6]`이 비문자열 truthy 값(숫자·객체·배열)이면 `\|\|` 체인이 거기서 멈춰버려 멀쩡한 `localStorage.team_code` fallback까지 함께 버리게 된다(예외는 안 나지만 빈 팀 코드로 업로드됨). **"문자열이고 trim 후에도 비어있지 않은 `soilRow[6]`만 우선 사용, 그 외(비문자열·빈 문자열·공백뿐인 문자열 포함)는 전부 fallback"으로 우선순위를 재구성**한다:
```js
const recordTeam = rec.soilRow && rec.soilRow[6];
const rawTeam = (typeof recordTeam === 'string' && recordTeam.trim())
  ? recordTeam
  : (localStorage.getItem('team_code') || '');
const teamCode = typeof rawTeam === 'string' ? rawTeam.trim() : '';
```
이렇게 하면 `rec.soilRow[6]`이 레거시 공백을 갖고 있어도 **payload로 나가는 순간에는 항상 trim된 문자열**이 되고, `localStorage`에 저장된 원본 `soilRow[6]` 자체는 건드리지 않는다(원본 보존 원칙 유지 — 표시용/전송용 값만 정규화, 커밋 `c25441a`에서 이미 여러 번 쓴 것과 같은 패턴 — `typeof x === 'string' ? x.trim() : ''` 형태도 `normalizeLoadedPlots`/`findLoadedPlot` 등에서 이미 쓰던 것과 동일). |
| E | `markPlotDoneInDropdown()` → `applyLoadedPlots()` → `buildCustomPlotList()`(`watermap_V100.html:5504` 부근) | 업로드 성공 후 `custom-plot-display`가 매번 "포인트 선택" placeholder로 리셋됨 | 지난 점검에서 발견: `buildCustomPlotList()`가 함수 끝에서 무조건 `display.textContent = '포인트 선택'`을 실행한다. `markPlotDoneInDropdown()`은 이 호출 **다음에** display 텍스트를 "✓ plotNo"로 갱신하려 시도하지만, 그 시점엔 이미 placeholder로 초기화된 뒤라 비교(`display.textContent.replace(...) === target`)가 항상 실패한다 — 사실상 죽은 코드. 업로드 성공 후 사용자가 보는 커스텀 드롭다운 라벨이 실제 선택 상태와 어긋나 보일 수 있다(데이터 손상 아님, 화면 표시만). | `buildCustomPlotList()`가 현재 선택된 plotNo를 알고 있다면(예: `document.getElementById('soil-plot-no')?.value`) 리셋 대신 그 값을 유지하도록 하거나, `markPlotDoneInDropdown()`의 display 갱신을 `buildCustomPlotList()` 호출 **전에** 값을 저장해뒀다가 호출 후 다시 적용하는 방식. 구체적 구현은 실제 작업 시 `buildCustomPlotList()`의 다른 호출부(그냥 목록 갱신 목적)에 회귀가 없는지 확인 필요 — 이번 문서에서는 "우선순위 낮음, 구현 시 주의 필요"로만 표시. |

## 5. 알려진 함정

- **2차 점검(Codex)의 전제가 구버전이었다는 점을 항상 먼저 확인할 것**: 앞으로도 Codex
  검토를 받을 때는 검토 시점의 실제 `git log`/코드 상태를 먼저 명시해서 전달해야
  이런 시차 문제가 재발하지 않는다.
- **자유 텍스트 vs 고정 선택지 구분이 XSS 우선순위를 가른다**: 이슈 A(자유 텍스트
  `plotNo`/`basin`/`date`)와 "명시적 제외 4번"(고정 `<select>`/숫자 피커 값)의 차이가
  바로 이것 — 입력 요소의 실제 타입을 반드시 확인하고 우선순위를 매겨야 한다.
- **이슈 D는 "원본 보존" 원칙과 "이미 발생한 데이터 오염"이 공존하는 경우다**: 앞으로
  저장되는 레코드는 trim으로 깨끗해지지만, 이미 `soilRow[6]`에 공백이 저장된 레코드는
  이번 수정으로 소급 정리되지 않는다. `getSurveyRecords()` 원본을 건드리지 않는다는
  기존 원칙과 일관된 선택이다.
- **이슈 C는 이번 배치에 포함하지 않는다**: `loaded_plots_잔여이슈_개발지시서.md`에서
  이미 같은 결론(별도 지시서로 분리)을 내렸고, 이번 2차 점검도 같은 결론에 도달했다 —
  두 번 독립적으로 같은 판단이 나온 것이니 신뢰도가 높다고 보고 그대로 유지한다.
- **(v2, 3라운드 검토가 제안, 채택하지 않음) 팀 코드 정규화를 `getNormalizedTeamCode()`
  같은 공용 헬퍼로 중앙화하는 것.** 이슈 D가 필요한 지점은 딱 3곳(`getSoilData()`,
  `autoRetryUpload()`, `reuploadRecord()`)이고, 수정 자체가 표현식을 `.trim()`으로
  감싸는 한 줄짜리 변경이라 별도 함수로 뺄 만큼의 반복이 아니다. `loaded_plots`
  작업에서 헬퍼(`getLoadedPlotItems()` 등)를 만들었던 건 호출부가 7곳 이상이고 각자
  파싱+정규화라는 여러 단계 로직을 반복했기 때문 — 이번엔 그 정도 반복이 아니라서
  헬퍼를 새로 만들면 오히려 과한 추상화가 된다. 3곳 각각에 `.trim()`을 직접 적용한다.

## 6. 체크리스트

- [ ] 이슈 A: "임목조사"/"사진촬영" 탭의 `r.plotNo`/`r.basin`/`r.date`/`p.filename`에
      `escapeHtml()` 적용 (전체/입지·토양 탭과 동일한 패턴)
- [ ] 이슈 B: `getQueue()`에 안전 파싱(`try/catch` + `Array.isArray`) 추가
- [ ] 이슈 D: `getSoilData()`(`6052`)의 `team_code` 읽기에 `.trim()`. `autoRetryUpload()`
      (`3258`)/`reuploadRecord()`(`6429`)는 (v4) **"문자열이고 trim 후 비어있지 않은
      `soilRow[6]`만 우선 사용, 그 외는 전부 `localStorage.team_code` fallback"** 순서로
      재구성(단순 `\|\|` 표현식 전체 trim만으로는 비문자열 truthy 값일 때 fallback을
      못 씀 — v3에서 v4로 정정된 부분, 위 상세 코드 참고)
- [ ] 이슈 E: 별도 조사 후 구현 여부 결정(다른 호출부 회귀 확인 필요 — 우선순위 낮음)
- [ ] 이슈 C: 이번 배치에서 구현하지 않음, 별도 지시서 작성 여부만 결정. **단, 이슈 D
      (자동 업로드/재업로드)가 `getSurveyRecords()`로 가져온 레코드를 순회하므로, C가
      해결되기 전까지는 `survey_records` 배열에 손상된(`null` 등) 원소가 섞여 있으면
      D 경로 자체가(개별 레코드의 teamCode 계산 이전에) 중단될 수 있다는 의존 관계를
      기억해둔다(5라운드 검토 지적) — 이번 배치에서 C를 고치지는 않지만, D 구현 시
      "레코드 순회 자체는 살아있는 항목만 대상으로 한다"는 기존 안전장치가 있는지
      확인은 필요하다.
- [ ] 구문 검사: `node -e`로 4개 `<script>` 블록 파싱 확인
- [ ] 회귀 테스트(로컬 `python3 -m http.server` + 브라우저 콘솔):
  - [ ] 조사지번호에 `<img src=x onerror=alert(1)>` 입력 후 저장 → "임목조사"/"사진촬영"
        탭을 열어도 스크립트가 실행되지 않고 텍스트 그대로(이스케이프된 채) 표시되는지
        확인. "전체"/"입지·토양" 탭도 함께 확인(회귀 없는지)
  - [ ] `localStorage.setItem('upload_queue', '{bad json')` 상태에서 `getQueue()` 호출 →
        예외 없이 빈 배열 반환하는지 확인
  - [ ] `localStorage.setItem('team_code', ' A팀 ')`(공백 포함) 상태에서 새 조사 기록을
        저장 → 저장된 레코드의 `soilRow[6]`이 공백 없는 `'A팀'`인지 확인
  - [ ] 위 상태에서 "미전송 전송"/재업로드 실행 → 실제 서버로 나가는(또는 목 서버가
        받는) payload의 팀 코드 필드가 공백 없는지 확인
  - [ ] **(v2, 이슈 D 핵심 회귀)** devtools로 `survey_records`의 기존 레코드 하나를
        직접 열어 `soilRow[6]`을 `' B팀 '`(공백 포함, 레거시 오염 시뮬레이션)로 덮어쓴
        뒤 "미전송 전송"/재업로드 실행 → payload의 팀 코드 필드가 공백 없는 `'B팀'`으로
        나가는지 확인(fallback만 trim했다면 이 케이스는 여전히 실패했을 것)
  - [ ] **(v3, 이슈 D 타입 가드 회귀)** devtools로 `soilRow[6]`을 문자열이 아닌 값
        (`123` 또는 `{}`)으로 덮어쓴 뒤 "미전송 전송"/재업로드 실행 → `TypeError` 없이
        예외가 나지 않는지 확인
  - [ ] **(v4, 이슈 D fallback 복구 회귀 — 이번 라운드 핵심)** 위와 같은 상태
        (`soilRow[6] = {}`, 비문자열)에서 **`localStorage.setItem('team_code', 'C팀')`
        처럼 유효한 fallback 값이 있는 경우** → payload의 팀 코드가 빈 문자열이 아니라
        `'C팀'`으로 정상 채워지는지 확인(v3 설계로는 이 케이스에서 빈 문자열이
        나갔을 것 — 이 테스트가 실패하면 fallback 복구 로직이 빠진 것)
  - [ ] **(v4)** `soilRow[6] = '   '`(공백만 있는 문자열)에서도 위와 동일하게
        `localStorage.team_code` fallback을 정상적으로 쓰는지 확인
  - [ ] 기존 회귀(loaded_plots/cached_teams v5, 커밋 `60281c8`/`c25441a`에서 검증된
        시나리오들)가 이번 수정으로 깨지지 않는지 재확인
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)

## 7. 별도 과제 (참고용)

- **`survey_records` 소비 지점 전수 안전화** (이슈 C): `patchRecord`/`freshRecord`
  포함 7개 지점. 업로드 재시도 핵심 로직과 얽혀 있어 별도 지시서로 분리해 독립적으로
  검증하는 것을 권장한다.
- **`markPlotDoneInDropdown()`의 display 리셋 문제**(이슈 E): 다른 호출부 영향을
  조사한 뒤 별도로 진행.
- **요청 중 팀 전환 UI race**, **다중 탭 `localStorage` read-modify-write 경쟁**: 이미
  이전 문서들에서 "알려진 한계"로 남기기로 합의된 항목. 재론하지 않되, 여기 다시
  링크해 추적을 놓치지 않게 한다.
- **(v2) `flushOfflineQueue()`/`upload_queue`를 다시 활성화할 계획이 생기면, 이슈 B의
  최상위 안전 파싱만으로는 부족하다.** 항목별로 `type`/필수 필드를 검증하는 스키마
  체크를 추가해야 한다 — 그렇지 않으면 무효 항목이 큐에 남아 계속 재시도·실패를
  반복할 수 있다(3라운드 검토 지적). 지금은 이 큐 자체가 죽은 코드라 급하지 않지만,
  재활성화 시점에 반드시 함께 고려한다.
