# watermap-app-personal — "팀별 누적 현황이 안 보이고 스크롤도 안 됨" 버그 개발지시서

**최종 결론(2026-08-11, 4라운드 검토 완료)**: 아래 2~7절의 SW/스크롤락 가설은
**틀렸다.** 배포 후에도 사용자가 완전 종료·재실행, 로컬 `file://` 직접 열기로도
동일 증상을 재현했고, 결정적으로 **"저장목록" 탭에서도 같은 카드가 나타난다**는
것을 확인해줘서 순수 HTML 구조 버그임이 드러났다. 진짜 원인과 수정은 8절 참고.
2~7절의 SW 응답 완결성 검증·스크롤 잠금 안전망 자체는 이번 버그의 원인은
아니었지만, 각각 독립적으로 유효한 방어적 개선이라 되돌리지 않고 그대로 둔다.

## 1. 배경 — 사용자 보고

사용자가 실제 현장(스크린샷에 "LTE"와 매우 약한 신호 막대, 배터리 38% 표시 —
전형적인 현장 약전계 상황)에서 설정탭의 "팀별 누적 현황" 카드를 열었더니, 카드
제목과 설명 문구만 보이고 그 아래 비밀번호 입력창·버튼이 전혀 안 보였다. 화면을
아래로 스크롤해도 더 내려가지 않았다(카드가 설명 문구에서 끊긴 채로 바로
전역 고정 하단바 "초기화/저장"으로 이어짐).

## 2. 조사 과정과 결론

### 2.1 카드 자체는 코드상 정상이다 (재현 실패로 확인)

배포된 실제 URL(`https://bimil2ya.github.io/watermap-app-personal/watermap_V100.html`)을
모바일 뷰포트(375×812)로 직접 열어 확인했다:
- `team-summary-lock-box`/`load-team-summary-btn` 등 관련 DOM 요소가 정확한
  HTML 그대로 존재함을 `outerHTML`로 직접 확인.
- 설정탭으로 이동해 스크롤한 결과, "🔒 비밀번호" 입력창·확인 버튼·힌트 문구까지
  전부 정상적으로 렌더링되고 스크롤도 정상 작동함을 스크린샷으로 확인.

즉 지난 턴에 커밋한 비밀번호 게이트 코드 자체에는 문제가 없다 — **사용자가 겪은
문제는 코드 로직 버그가 아니라, 사용자 기기에 그 시점에 실제로 받아진 페이지가
불완전했거나(가장 유력) 별개의 상태 누수가 겹친 것으로 보인다.**

### 2.2 가장 유력한 원인 — Service Worker가 "끊긴 응답"을 완결된 응답처럼 취급한다

`sw.js`의 fetch 핸들러(`watermap-app/sw.js:26-39`):
```js
self.addEventListener('fetch', event => {
  if (!event.request.url.startsWith(self.location.origin)) return;
  event.respondWith(
    fetch(event.request)
      .then(response => {
        if (response && response.status === 200) {
          const clone = response.clone();
          caches.open(CACHE_NAME).then(cache => cache.put(event.request, clone));
        }
        return response;
      })
      .catch(() => caches.match(event.request))
  );
});
```

**문제**: 브라우저의 `fetch()` Promise는 **응답 헤더(status, content-length 등)가
도착하는 즉시 resolve**되고, 본문(body)은 별도의 스트림으로 그 이후 비동기로
전달된다. 즉 `response.status === 200`이 참이어도, 그 시점에 본문이 전부
도착했다는 보장이 전혀 없다 — 약한 신호에서 연결이 본문 전송 도중 끊기면,
`status`는 여전히 200으로 남지만 실제로 브라우저가 받는 바이트는 파일 중간에서
잘린다.

이 앱은 `watermap_V100.html` 하나가 약 340KB짜리 단일 파일이고(코드 분할 없음),
이 요청도 위 fetch 핸들러를 그대로 거친다. 약전계에서 이 요청의 본문 전송이
중간에 끊기면:
- **페이지 자체가 잘린 HTML을 그대로 렌더링한다** — 브라우저 HTML 파서는 받은
  바이트까지만 파싱하고 별다른 오류 없이 그 자리에서 문서를 마무리한다. 이번
  경우 "팀별 누적 현황" 카드의 설명 문구 직후에서 끊겼다면, 사용자가 본 것과
  정확히 일치한다(카드 제목+설명까지만 존재, 그 뒤 내용은 애초에 브라우저에
  도착하지 않았으므로 스크롤해도 나올 수 없다 — "스크롤이 안 된다"가 아니라
  "스크롤할 대상 자체가 없다"에 더 가깝다).
- `cache.put(event.request, clone)`도 같은(끊긴) 스트림을 저장하려다 실패하거나,
  실패해도 `.catch()`가 없어 조용히 무시된다 — 다음에 오프라인이거나 이 요청이
  다시 캐시로 폴백될 때 **손상된 캐시가 계속 재사용될 위험**도 있다.

이 설명이 "카드 제목·설명까지는 정상, 그 이후는 아예 없음, 스크롤 불가"라는
증상과 정확히 들어맞고, 스크린샷에 찍힌 매우 약한 LTE 신호와도 일치한다.

### 2.3 보조 후보 — 배경 스크롤 잠금(owner set) 누수 (근거는 약하지만 저비용 방어 가치 있음)

이 앱은 사진 액션시트·사진 뷰어·숫자 선택 시트가 열릴 때 `lockBodyScroll(owner)`로
`document.body.style.overflow = 'hidden'`을 걸고, 마지막 오버레이가 닫힐 때만
푸는 owner-set 방식을 쓴다(`watermap_V100.html:1661-1673`, 사용처 3곳:
`number-picker`/`photo-sheet`/`photo-viewer`). 각 오버레이마다 lock/unlock이
정확히 한 쌍씩만 있고, 닫는 경로가 어떤 이유로든(예외 발생, 예상 못한 종료 경로)
그 unlock 호출을 건너뛰면 **body 전체가 영구히 스크롤 불가 상태로 남는다.**

이 세션에서 카드 안 보임을 재현하지 못했으므로(2.1) 이번 사고의 직접 원인일
가능성은 2.2보다 낮게 본다 — 하지만 원인 규명과 무관하게, "언젠가 owner unlock이
누락되면 앱 전체가 영구히 안 스크롤된다"는 취약점 자체는 실재하므로, 저비용
안전망을 이번 기회에 함께 추가한다(3.2).

## 3. 설계

### 3.1 (주 수정) Service Worker — 응답 완결성 검증 후에만 캐시·반환

대상: `watermap-app/sw.js`. fetch 핸들러를 "본문을 끝까지 안전하게 읽어서 완결
여부를 확인한 뒤에만 캐시·페이지에 반환"하는 방식으로 바꾼다. 본문 읽기 자체가
끊긴 스트림에서는 예외를 던지므로, 이 예외를 완결성 판정 신호로 활용한다:

```js
self.addEventListener('fetch', event => {
  if (!event.request.url.startsWith(self.location.origin)) return;
  event.respondWith(
    fetch(event.request).then(async response => {
      if (!response || response.status !== 200) return response;

      let buf;
      try {
        // 본문을 끝까지 읽어본다 — 연결이 중간에 끊긴 응답이면 여기서 reject된다
        buf = await response.clone().arrayBuffer();
      } catch (e) {
        // 본문이 불완전함 — 캐시에 이전에 받은 완전한 버전이 있으면 그걸로 대체
        const cached = await caches.match(event.request);
        if (cached) return cached;
        throw e; // 캐시도 없으면 원래 오류를 그대로 전달(기존 동작과 동일)
      }

      const declaredLength = response.headers.get('content-length');
      if (declaredLength && buf.byteLength !== Number(declaredLength)) {
        // 헤더가 알려준 길이와 실제 받은 바이트 수가 다름 — 불완전한 응답으로 간주
        const cached = await caches.match(event.request);
        if (cached) return cached;
        // 캐시가 없으면(최초 설치 등) 어쩔 수 없이 이 응답을 그대로 쓴다 — 완전히
        // 막으면 그 어떤 요청도 처리 못 하게 될 수 있으므로 아래로 진행.
      }

      const rebuilt = () => new Response(buf, {
        status: response.status,
        statusText: response.statusText,
        headers: response.headers
      });
      caches.open(CACHE_NAME).then(cache => cache.put(event.request, rebuilt()));
      return rebuilt();
    }).catch(() => caches.match(event.request))
  );
});
```

**핵심 아이디어**: 원래 스트리밍 응답을 그대로 페이지에 돌려주는 대신, **일단
전체를 메모리 버퍼(`ArrayBuffer`)로 다 받은 뒤 그 버퍼로 새 `Response`를 만들어
페이지와 캐시 양쪽에 동일하게 준다.** 이렇게 하면:
- 본문이 중간에 끊기면(`response.clone().arrayBuffer()`가 reject) 그 자리에서
  잡아내 캐시로 폴백한다 — 페이지가 잘린 HTML을 렌더링할 일이 없어진다.
- `Content-Length`와 실제 수신 바이트 수를 한 번 더 대조해, reject는 안
  됐지만 그래도 길이가 안 맞는 애매한 경우까지 방어한다.
- 손상된 응답을 `cache.put()`하지 않으므로, 캐시가 오염돼 오프라인 상태에서도
  계속 잘린 페이지가 나오는 2차 피해를 막는다.
- 340KB 정도의 단일 HTML/작은 아이콘 파일들만 다루는 이 앱 규모에서, 응답을
  메모리에 한 번 더 버퍼링하는 오버헤드는 무시할 수준이다.

### 3.2 (보조 수정) 스크롤 잠금 owner-set에 자동 복구 안전망 추가

대상: `watermap_V100.html`의 `switchTab(id, btn)`(`watermap_V100.html:2873`)과
`document`의 `visibilitychange` 이벤트. "사용자가 탭을 눌러 화면을 전환할 수
있었다"는 사실 자체가 이미 "전체 화면을 막는 진짜 모달이 열려있지 않다"는
증거이므로, 탭 전환 시점에 잠금이 남아있다면 그건 누수로 간주하고 강제로
정리한다:

```js
// switchTab() 안, 기존 로직 실행 전에 추가
function switchTab(id, btn) {
  // 탭을 직접 눌러 전환할 수 있었다는 것 자체가 진짜 모달이 열려있지 않다는
  // 뜻이므로, 이 시점에 스크롤 잠금이 남아있다면 누수로 보고 강제 해제한다.
  if (_bodyScrollLocks.size > 0) {
    console.warn('스크롤 잠금 누수 감지, 강제 해제:', [..._bodyScrollLocks]);
    _bodyScrollLocks.clear();
    document.body.style.overflow = _bodyScrollOverflowBeforeLock || '';
  }
  // ...기존 로직...
}
```

추가로, iOS Safari가 앱을 백그라운드로 보냈다가 복귀시키는 시점(오버레이가
열린 채로 정지됐다가 그 오버레이의 닫기 이벤트가 유실되는 경우를 대비)에도
같은 정리를 한 번 더 시도한다:
```js
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'visible' && _bodyScrollLocks.size > 0) {
    // 복귀 시점에 실제로 열려있는 오버레이가 하나도 없는데 잠금만 남아있으면 정리
    const anyOverlayOpen =
      document.getElementById('number-picker-backdrop')?.style.display === 'flex' ||
      document.getElementById('photo-action-sheet')?.style.display !== 'none' ||
      document.getElementById('photo-viewer')?.style.display !== 'none';
    if (!anyOverlayOpen) {
      _bodyScrollLocks.clear();
      document.body.style.overflow = _bodyScrollOverflowBeforeLock || '';
    }
  }
});
```
(정확한 조건문은 구현 시 각 오버레이의 실제 `display` 기본값·시트 조건을
`watermap_V100.html`에서 다시 확인해 맞춘다 — 위는 설계 의도를 보여주는
스케치다.)

## 4. 범위와 제외

**포함**: `sw.js`의 fetch 핸들러(3.1), `switchTab()`/`visibilitychange`의 스크롤
잠금 안전망(3.2).

**명시적 제외**:
- "팀별 누적 현황" 카드 자체의 HTML/JS — 2.1에서 확인했듯 정상이므로 손대지
  않는다.
- 이 SW 수정은 "네트워크가 끊겨도 무조건 페이지를 보여준다"는 걸 목표로 하지
  않는다 — 여전히 캐시도 없고 네트워크도 끊기면 로드가 실패할 수 있다(기존
  동작과 동일한 한계). 이번 수정의 목표는 오직 "완결되지 않은 응답을 완결된
  것처럼 취급해 잘린 페이지를 보여주는 것"을 막는 것.

## 5. 알려진 한계

- **완전한 재현 확인은 못 했다.** 2.1에서 밝혔듯 이 세션의 자동화 브라우저
  환경에서는 문제가 재현되지 않았다 — 이 계획은 스크린샷 증거(약전계 LTE)와
  `sw.js` 코드의 실제 취약점을 근거로 한 가장 유력한 가설에 기반한다. 3.1은
  가설이 맞든 아니든 그 자체로 정당한 방어적 수정(불완전한 응답을 완결된
  것처럼 다루지 않는다)이므로, 재현 실패와 무관하게 반영할 가치가 있다고
  판단한다.
- 3.2는 근거가 3.1보다 약하지만, 코드 변경 비용이 낮고("탭을 누를 수 있었다면
  모달이 없다"는 안전한 전제 위에서만 동작) 부작용 위험이 거의 없어 함께
  포함한다.

## 6. 체크리스트

- [x] `sw.js` fetch 핸들러 수정(3.1)
- [x] `switchTab()` 스크롤 잠금 안전망 추가(3.2)
- [x] `visibilitychange` 스크롤 잠금 안전망 추가(3.2)
- [x] 구문 검사: `sw.js`는 `node --check`, `watermap_V100.html`은 5개
      `<script>` 블록 기존 방식대로 확인 — 둘 다 통과
- [x] 로컬 브라우저 회귀 테스트 — 아래 7절 참고
- [ ] `git commit` (push는 사용자 승인 후 별도 진행)

## 7. 구현·검증 결과 (서브에이전트 3라운드 검토 완료)

**`switchTab()`/`visibilitychange` 스크롤 잠금 안전망**: 로컬 브라우저에서
`lockBodyScroll()`로 누수 상태를 인위적으로 만든 뒤 각각 검증. 구현 첫 시도에서
`visibilitychange`의 오버레이 감지 로직에 버그를 직접 발견했다 —
`number-picker-backdrop`은 CSS 클래스 기본값(`display:none`)에 의존하고 열릴
때만 인라인 style을 붙이는 요소라, 인라인 `el.style.display`만 확인하면
초기(한 번도 안 연) 상태를 "열려있음"으로 오판했다. `getComputedStyle()`로
바꿔 해결하고 재검증 완료. 서브에이전트 검토(1라운드)에서도 이 두 안전망은
문제없다고 확인했다(`switchTab()` 호출부 전수 확인 — 실제로 존재하는
호출부는 nav 버튼 5개뿐이며, `loadRecord()`의 `.click()` 기반 프로그래매틱
호출은 현재 죽은 코드라 당장 리스크는 없지만 나중에 되살아나면 "switchTab
호출 = 모달 없음" 전제가 깨질 수 있다는 점은 참고로 남겨둠).

**`sw.js` — 서브에이전트가 실제 배포 환경(gzip)을 재현해 Critical 회귀를
잡아냈다.** 최초 구현은 Node 격리 유닛테스트로 4개 시나리오만 확인했는데,
실제 배포처(GitHub Pages/Fastly)가 이 HTML을 gzip으로 서빙한다는 사실을
반영하지 못했다. `Content-Length`(와이어/압축 크기)와 `arrayBuffer()`가
돌려주는 압축 해제된 바이트 수를 그대로 비교하는 바람에, **정상적으로 완결된
응답도 매번 "불일치"로 오판돼 캐시가 영원히 갱신되지 않는** 심각한 회귀가
있었다 — 서브에이전트가 실제 gzip 로컬 서버 + 브라우저로 직접 재현해 확인.

수정: ① `Content-Encoding` 헤더가 있으면 길이 비교 자체를 건너뛰고(진짜 끊김
감지는 `arrayBuffer()` reject에만 의존), ② 재구성한 `Response`의 헤더에서
`content-encoding`을 제거하고 `content-length`를 실제 압축 해제 바이트 수로
교체(WebKit에서 이미 풀린 바이트를 또 gunzip하려다 실패할 위험 제거), ③
"길이 불일치 + 캐시 없음" 경로도 `arrayBuffer()` reject 경로와 대칭적으로
`throw`하도록 통일(서브에이전트의 저비용 개선 제안 반영).

2·3라운드에서 서브에이전트가 **실제 소켓을 강제로 끊는 방식까지 재현**해
① 정상 gzip 응답이 신선하게 캐시·반영되는지, ② 진짜 스트림 중단 시 캐시로
안전하게 폴백하는지(캐시 없으면 안전하게 실패하는지)를 모두 확인했다. 로컬
Node 유닛테스트도 최종 7개 시나리오(정상/끊김+캐시있음/끊김+캐시없음/
길이불일치(비압축)+캐시있음/**gzip+정상+캐시있음(회귀 재현)**/gzip+캐시없음/
길이불일치(비압축)+캐시없음)로 확장해 전부 통과 확인.

**최종 결론(3라운드, SW/스크롤락 한정)**: 더 이상 지적사항 없음. Critical
1건·High 1건·Low 2건(1건 반영, 1건 — `Content-Encoding: identity`, 이 스택에서
발생 불가능 — 의도적 보류) 전부 처리 완료. **다만 위 최종 결론(문서 맨 위)에서
밝혔듯, 이 SW/스크롤락 작업 자체는 이번 "팀별 누적 현황 안 보임" 사고의 원인이
아니었던 것으로 나중에 밝혀졌다 — 8절 참고.**

## 8. 진짜 근본 원인 (2026-08-11 추가) — HTML 구조 버그, SW/스크롤락과 무관

배포 후에도 사용자가 실기기에서 완전 종료·재실행해 재현했고, 결정적으로
**로컬 `file://` 직접 열기와 "저장목록" 탭에서도 동일 카드가 나타난다**는
사실을 사용자가 알려줘서 네트워크/캐시 가설 전체가 틀렸음이 드러났다.

### 원인

`watermap_V100.html`의 "관리자 전용 섹션"(`tab-setting` 안, 1085번 줄~) 내부에
**여분의(orphan) `</div>`**가 있었다:

```
<div id="apps-script-body" ...>...</div>   <!-- 1149~1545: 정상적으로 닫힘 -->
<div class="card">...구글시트에서 불러오기...</div>  <!-- 1549~1568: 정상 -->

  </div><!-- /apps-script section -->   ← 여분! apps-script-body는 이미 위에서 닫혔음
</div><!-- /admin-content -->
</div><!-- /admin 섹션 -->
```

이 orphan 태그가 그 뒤의 닫는 태그들을 한 칸씩 밀어서, **`tab-setting` 자체가
"/admin 섹션" 주석이 붙은 지점에서 조기 종료**됐다. 그 뒤에 있던 "팀별 누적
현황" 카드(6절에서 추가한 것)와 진짜 `</div><!-- /tab-setting -->`는 `tab-setting`의
형제가 되어 `<body>` 최상위로 새어나갔다 — `.tab-panel { display:none }` 규칙
자체가 `.tab-panel` 클래스가 있는 요소에만 적용되는데, 새어나간 카드는 그
클래스가 없어 규칙 대상이 아니었으므로 **모든 탭에서 항상 DOM에 존재**했다
(그래서 "저장목록" 탭에서도 보였음). 고정 하단바(`.save-bar`)와 `.tab-panel`의
`padding-bottom:100px` 여유 공간 밖에 위치하게 되면서, 카드 하단(비밀번호
입력창·버튼)이 하단바에 가려지고 문서 전체 스크롤 여유도 그 근처에서 끝나
보였다 — 신고된 "설명 문구까지만 보이고 스크롤도 안 됨" 증상과 정확히 일치.

`git blame` 확인 결과 이 orphan `</div>`는 개인 포크의 **최초 커밋**(`4da6a4d`,
2026-08-01)부터 있던 잠재 버그였다. 그 자리 뒤에 아무 내용이 없어 오랫동안
증상이 없다가, 6절에서 "팀별 누적 현황" 카드를 그 바로 뒤에 추가하면서 처음
눈에 보이는 문제로 드러났다.

### 수정

1570번 줄(`</div><!-- /apps-script section -->`) 한 줄 삭제. 그게 전부다.

### 검증

- Python `html.parser`(정규식이 아닌 실제 HTML 토크나이저, `<script>`/`<style>`
  CDATA 자동 스킵)로 전체 7147줄 태그 균형을 재검사: 수정 전 정확히 1건의
  불일치(1605번 줄에서 body까지 새어나감) 발견, 수정 후 0건.
- 5개 `tab-panel` 전부(soil/tree/photo/list/setting)가 정확히 같은 depth에서
  열리고 닫혀 완벽한 형제 관계임을 확인 — 같은 패턴의 다른 누수는 없음.
- 브라우저에서 `team-summary-lock-box`의 실제 부모 체인이
  `... → card-body → card → tab-setting → BODY`로 정상 회귀했는지 확인.
- "저장목록" 탭 활성화 시 이 카드가 안 보이는지(`offsetParent` 체크)로 회귀 확인.
- 이 orphan div가 "관리자 전용 섹션" 한가운데 있었으므로, 관리자 잠금 기능
  (`checkAdminPw`) 자체가 깨지지 않았는지도 확인 — 오답/정답 동작, `admin-content`
  펼침, `apps-script-body`가 여전히 `admin-content` 안에 있는지 전부 정상.
- 구문 검사 통과, 콘솔 에러 없음, 모바일 뷰포트 스크린샷으로 카드 전체(비밀번호
  입력창+힌트 포함) 정상 렌더링 확인.
- 서브에이전트 검토(4라운드): 위 진단·수정·검증 전부 독립적으로 재확인, 다른
  곳에 같은 패턴 없음, 부작용 없음, SW/스크롤락 수정은 이번 원인은 아니었지만
  각각 독립적으로 유효한 개선이라 되돌릴 이유 없음 — **지적사항 없음.**
